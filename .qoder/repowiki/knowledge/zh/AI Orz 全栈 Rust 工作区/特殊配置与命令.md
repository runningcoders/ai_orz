统一入口：./start.sh dev（同时启动后端 cargo run 与前端 dx serve）、./start.sh build（编译前端 release 并复制 .wasm/.js 到 dist/pkg/，再编译后端 release）、./start.sh prod（构建后运行 target/release/ai_orz 监听 0.0.0.0:3000）；CI 通过 rust.yml 执行 cargo fmt/clippy/cargo test --lib --test '*' 与 wasm32-unknown-unknown 检查，并在 push main 时要求行覆盖率 ≥ 45%（PR 为 38%）。