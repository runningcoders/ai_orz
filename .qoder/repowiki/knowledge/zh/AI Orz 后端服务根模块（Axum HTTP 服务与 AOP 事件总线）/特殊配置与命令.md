工作区包含四个成员：根包、frontend（Dioxus WASM）、common、ai-orz-macros；集成测试通过 Cargo `[[test]]` 段声明为独立 binary（如 `auth_sysinit_test`、`message_delivery_test`、`vector_degradation_test` 等），需配合 `tests/common/app.rs` 提供的测试应用实例运行。