- 设计文档以「目标—架构—实现细节—文件索引」四段式结构撰写，并在顶部标注最后更新日期与作者。
- 对跨模块依赖使用 ASCII 架构图（┌─┐│─┘）和表格对比来描述分层职责与字段注解规则。
- 所有涉及代码约定的条目均以「observed pattern」形式陈述（如「优先级 path > query > body」「两阶段初始化原则」），不使用 must/never 等强制措辞。
- 文档间通过交叉引用文件名与模块路径（如 src/pkg/aop/core/event.rs、common/src/api/*.rs）建立知识关联，而非重复粘贴代码片段。