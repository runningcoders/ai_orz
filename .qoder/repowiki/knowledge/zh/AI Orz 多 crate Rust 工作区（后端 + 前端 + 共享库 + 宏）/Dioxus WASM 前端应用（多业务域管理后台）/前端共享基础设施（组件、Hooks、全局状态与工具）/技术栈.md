Dioxus（`dioxus::prelude::*`，使用 `Signal`、`use_context_provider`、`use_effect`、`spawn`）、Tailwind CSS + DaisyUI（通过 class 字符串如 `modal modal-open`、`btn btn-sm`）、`dioxus_router`（`use_navigator` 路由跳转）、`web_sys`（浏览器 localStorage）。