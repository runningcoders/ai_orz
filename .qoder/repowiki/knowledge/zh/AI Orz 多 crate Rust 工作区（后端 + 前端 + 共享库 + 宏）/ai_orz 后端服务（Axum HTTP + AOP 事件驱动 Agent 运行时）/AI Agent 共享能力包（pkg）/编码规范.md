- 每个子模块对外暴露独立的 `init` / `init_all` 函数，由 pkg_root 的 `init_all` 统一编排调用顺序
- 全局可变状态通过 crate 级单例（如 tool_registry 的 Registry、tool_tracing 的 ToolCallLogger）暴露，避免参数透传
- 配置项统一从 `common::config::AppConfig` 或环境变量读取，不在子模块内自行解析 CLI 参数
- 测试代码通过 `#[cfg(test)]` 条件编译隔离，并与被测模块同目录组织