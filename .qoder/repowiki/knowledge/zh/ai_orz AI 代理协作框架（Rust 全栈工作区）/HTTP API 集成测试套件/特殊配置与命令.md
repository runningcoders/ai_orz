所有集成测试通过 `cargo test --test <name>` 或 `cargo test a2a_flow` 运行；每个测试函数需先调用 `crate::common::init_full_test_env(pool).await` 完成全局单例初始化，再构造 `TestApp::new(pool).await` 发起请求；向量存储强制使用 `InMemory` 类型以避免多进程阻塞问题。