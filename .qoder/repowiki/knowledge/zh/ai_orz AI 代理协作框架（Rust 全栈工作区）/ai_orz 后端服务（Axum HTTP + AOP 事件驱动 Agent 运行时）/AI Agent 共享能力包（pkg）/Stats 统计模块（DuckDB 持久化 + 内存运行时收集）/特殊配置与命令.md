启动时需调用 `Stats::open(path, batch_size)` 打开 DuckDB 文件，再调用 `initialize_default()` 注册默认六张表（default/model_call/tool_call/agent_awake/project/task），或通过 `register_table` 注册自定义事件表；AOP 侧通过 `init_global_stats` 设置全局单例以便无 ctx 场景访问。