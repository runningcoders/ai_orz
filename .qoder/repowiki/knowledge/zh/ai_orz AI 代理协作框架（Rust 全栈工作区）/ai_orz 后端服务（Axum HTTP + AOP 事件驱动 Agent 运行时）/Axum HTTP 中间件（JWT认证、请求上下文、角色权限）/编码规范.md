- 每个中间件文件定义一个 `pub async fn xxx_middleware` 函数，并在 `mod.rs` 中 `pub use` 导出，形成统一的中间件命名约定。
- 未认证/无权限场景统一通过 `ApiResponse::<()>::error(code, msg)` 返回 JSON，浏览器请求则重定向到登录页，区分客户端类型决定响应形式。
- 跨中间件传递的用户身份信息采用「上游写入请求头 + 下游从请求头读取」的模式，而非直接共享内存结构。