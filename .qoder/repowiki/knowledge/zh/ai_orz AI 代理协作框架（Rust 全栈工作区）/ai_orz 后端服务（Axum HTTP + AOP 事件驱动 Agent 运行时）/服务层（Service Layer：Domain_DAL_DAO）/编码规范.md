- 每个领域子模块均提供 init() 同步初始化与可选的 async init_base_data() 两阶段入口，由 domain/mod.rs 集中编排。
- DAL 与 DAO 按实体目录一一对应（如 agent、task、message），DAO 内 sqlite/vector/stats_duckdb/http 等多实现通过 mod.rs 统一导出。
- 测试文件与源文件同名 + _test 后缀并列存放于同一目录，便于定位。
- DAL 组合多个 DAO 对外暴露聚合接口，Domain 层只依赖 DAL 而不直接触碰 DAO。