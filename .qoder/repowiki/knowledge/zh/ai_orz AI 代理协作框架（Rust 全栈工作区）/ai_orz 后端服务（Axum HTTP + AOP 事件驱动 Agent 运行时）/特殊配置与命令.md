进程入口统一为 `ai_orz::run()`（bin target 调用），内部按固定阶段执行：config → pkg::init_all → service::init → producer::init → consumer::init → service::init_base_data → AOP stats hook → aop::init_all → axum::serve；前端静态目录可通过环境变量 `FRONTEND_DIST_DIR` 覆盖。