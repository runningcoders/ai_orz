构建时启用不同功能开关以裁剪依赖：`cargo build --no-default-features` 仅保留基础类型；添加 `--features axum-integration,sqlx-integration,jwt-integration,tokio-integration` 启用 HTTP/数据库/JWT/异步运行时集成。配置文件默认位于工作目录下的 `.ai_orz/ai_orz.toml`，可通过设置 `AI_ORZ_BASE_PATH` 环境变量覆盖根目录。