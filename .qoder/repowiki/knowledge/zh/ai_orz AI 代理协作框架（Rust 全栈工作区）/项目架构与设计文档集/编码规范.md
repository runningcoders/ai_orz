- 每个 superpowers/specs 下的特性采用 spec.md + tasks.md + checklist.md 三件套结构化文档，便于追踪需求到任务的映射。
- 迭代计划文件以 YYYY-MM-DD-主题.md 命名并置于 superpowers/plans/，按时间顺序线性演进。
- 跨文档通过相对路径链接（如 ./ARCHITECTURE.md、./design/unified-idl-http-handler.md）保持可移植性，避免绝对路径。
- 架构说明在关键约束处使用 ✅ / ❌ 符号标注允许与禁止的设计原则，便于快速扫描决策边界。