构建时需选择 feature 以启用特定集成：默认仅 `std`；添加 `sqlx` 启用 SQLX 类型；添加 `axum-integration` 启用 Axum 错误转换；添加 `jwt-integration`/`base64-integration`/`reqwest-integration`/`toml-integration`/`tokio-integration` 分别启用对应功能。