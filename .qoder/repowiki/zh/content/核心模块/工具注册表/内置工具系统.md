# 内置工具系统

<cite>
**本文引用的文件**
- [src/pkg/tool_registry/mod.rs](file://src/pkg/tool_registry/mod.rs)
- [src/pkg/tool_registry/builtin.rs](file://src/pkg/tool_registry/builtin.rs)
- [src/pkg/tool_registry/fs_read.rs](file://src/pkg/tool_registry/fs_read.rs)
- [src/pkg/tool_registry/fs_write.rs](file://src/pkg/tool_registry/fs_write.rs)
- [src/pkg/tool_registry/shell_exec.rs](file://src/pkg/tool_registry/shell_exec.rs)
- [src/pkg/tool_registry/http_fetch.rs](file://src/pkg/tool_registry/http_fetch.rs)
- [src/pkg/tool_registry/tool_security.rs](file://src/pkg/tool_registry/tool_security.rs)
- [src/models/tool.rs](file://src/models/tool.rs)
- [src/models/agent.rs](file://src/models/agent.rs)
- [common/src/api/tool.rs](file://common/src/api/tool.rs)
- [src/handlers/finance/tool/list_tool_tags.rs](file://src/handlers/finance/tool/list_tool_tags.rs)
- [src/consumer/tool_exec_log_consumer.rs](file://src/consumer/tool_exec_log_consumer.rs)
- [src/pkg/tool_tracing/mod.rs](file://src/pkg/tool_tracing/mod.rs)
</cite>

## 更新摘要
**变更内容**
- 新增标签分组机制：为内置工具添加 http、fs、shell 等标签，支持工具包安装与发现
- 增强工具注册表：支持按标签筛选和批量安装工具包
- 完善工具元数据：ToolPo 增加 tags 字段，支持 JSON 数组存储标签列表
- 新增标签管理 API：提供获取所有可用标签的接口，用于前端工具包选择

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能与资源限制](#性能与资源限制)
8. [故障排查指南](#故障排查指南)
9. [结论](#结论)
10. [附录：自定义内置工具开发指南](#附录自定义内置工具开发指南)

## 简介
本文件面向内置工具系统的实现与使用，重点说明工厂模式设计、文件系统读写工具（fs_read、fs_write）的功能与参数、元数据定义、权限控制与沙箱隔离机制、测试策略、错误处理与日志记录，并提供扩展新内置工具的实践指南与常见问题解决方案。

**更新** 新增标签分组机制，支持按 http、fs、shell 等标签对工具进行分组管理和批量安装。

## 项目结构
内置工具系统位于 src/pkg/tool_registry 下，采用"注册表 + 工厂"的解耦设计：
- 全局注册表 ToolRegistry 维护各协议的工厂集合，按协议分发创建实例。
- 内置工具通过 BuiltinToolFactory 接口提供默认 ToolPo 与运行时实例构造。
- 安全能力集中在 tool_security 模块，统一约束路径、网络、环境变量等。
- 执行结果与调用轨迹通过 AOP 事件与 ToolCallLogger 持久化。
- **新增** 标签分组机制：每个内置工具携带特定标签，支持工具包安装与发现。

```mermaid
graph TB
A["ToolRegistry<br/>全局注册表"] --> B["BuiltinToolFactory<br/>内置工具工厂接口"]
B --> C["FsReadToolFactory<br/>读取文件 (fs标签)"]
B --> D["FsWriteToolFactory<br/>写入文件 (fs标签)"]
B --> E["HttpFetchToolFactory<br/>HTTPS 抓取 (http标签)"]
B --> F["ShellExecToolFactory<br/>命令执行 (shell标签)"]
A --> G["tool_security<br/>路径/网络/环境安全"]
H["CoreTool<br/>统一执行接口"] --> I["AOP 事件<br/>agent.tool.executed"]
I --> J["ToolExecLogConsumer<br/>写入 JSONL 日志"]
K["Agent<br/>installed_tags"] --> L["工具包安装<br/>按标签批量启用"]
```

**图表来源**
- [src/pkg/tool_registry/mod.rs:29-102](file://src/pkg/tool_registry/mod.rs#L29-L102)
- [src/pkg/tool_registry/builtin.rs:17-43](file://src/pkg/tool_registry/builtin.rs#L17-L43)
- [src/pkg/tool_registry/tool_security.rs:314-486](file://src/pkg/tool_registry/tool_security.rs#L314-L486)
- [src/consumer/tool_exec_log_consumer.rs:26-50](file://src/consumer/tool_exec_log_consumer.rs#L26-L50)
- [src/models/agent.rs:135-149](file://src/models/agent.rs#L135-L149)

**章节来源**
- [src/pkg/tool_registry/mod.rs:29-102](file://src/pkg/tool_registry/mod.rs#L29-L102)
- [src/pkg/tool_registry/builtin.rs:17-43](file://src/pkg/tool_registry/builtin.rs#L17-L43)

## 核心组件
- CoreTool 统一执行接口：所有工具必须实现 call(ctx, args) 与 po()，便于上层以统一方式调度与追踪。
- ToolRegistry 全局注册表：存储工厂而非实例，按协议分发创建；支持内置、MCP、HTTP 三类工具。
- BuiltinToolFactory 内置工厂接口：每个内置工具提供 create_po() 生成默认元数据，create(po) 构建可执行实例。
- tool_security 安全库：提供路径校验、敏感文件名拦截、SSRF 防护、响应体大小限制、环境变量白名单等。
- ToolTracing + Consumer：通过 AOP 事件 agent.tool.executed 将工具调用轨迹写入 JSONL 日志。
- **新增** 标签分组系统：每个内置工具携带特定标签，支持 Agent 通过 installed_tags 批量安装工具包。

**章节来源**
- [src/models/tool.rs:16-34](file://src/models/tool.rs#L16-L34)
- [src/pkg/tool_registry/mod.rs:29-102](file://src/pkg/tool_registry/mod.rs#L29-L102)
- [src/pkg/tool_registry/builtin.rs:17-43](file://src/pkg/tool_registry/builtin.rs#L17-L43)
- [src/pkg/tool_registry/tool_security.rs:1-231](file://src/pkg/tool_registry/tool_security.rs#L1-L231)
- [src/consumer/tool_exec_log_consumer.rs:26-50](file://src/consumer/tool_exec_log_consumer.rs#L26-L50)
- [src/models/agent.rs:135-149](file://src/models/agent.rs#L135-L149)

## 架构总览
下图展示了从请求到工具执行的完整链路：注册表根据协议选择工厂，工厂基于数据库中的 ToolPo 构建具体工具实例，执行时受安全层约束，结果经 AOP 事件落盘为 JSONL 日志。**新增** 标签分组机制支持按标签批量安装和管理工具。

```mermaid
sequenceDiagram
participant Caller as "调用方"
participant Reg as "ToolRegistry"
participant Fac as "BuiltinToolFactory"
participant Tool as "CoreTool(具体实现)"
participant Sec as "tool_security"
participant AOP as "AOP 事件中心"
participant Log as "ToolCallLogger"
participant Tags as "标签分组"
Caller->>Reg : create_tool(ToolPo)
Reg->>Fac : create(po)
Fac-->>Reg : Box<dyn CoreTool>
Caller->>Tool : call(ctx, args)
Tool->>Sec : 路径/网络/环境校验
Sec-->>Tool : 允许或拒绝
Tool-->>Caller : 返回结果
Tool->>AOP : 发布 agent.tool.executed
AOP->>Log : 写入 JSONL 日志
Tags->>Caller : 按标签过滤工具包
```

**图表来源**
- [src/pkg/tool_registry/mod.rs:81-102](file://src/pkg/tool_registry/mod.rs#L81-L102)
- [src/pkg/tool_registry/builtin.rs:27-43](file://src/pkg/tool_registry/builtin.rs#L27-L43)
- [src/consumer/tool_exec_log_consumer.rs:26-50](file://src/consumer/tool_exec_log_consumer.rs#L26-L50)
- [src/models/agent.rs:135-149](file://src/models/agent.rs#L135-L149)

## 详细组件分析

### 工厂模式与注册表
- 全局注册表 ToolRegistry 持有内置工厂映射，按 ToolPo.protocol 分派创建逻辑。
- 内置工具通过 GENERIC_BUILTIN_TOOLS 集中声明，启动时批量注册。
- **新增** 标签分组：每个工厂在 create_po() 中设置特定的 tags 字段，如 fs_read/fs_write 标记为 "fs"，http_fetch 标记为 "http"，shell_exec 标记为 "shell"。
- 优点：新增内置工具只需实现 BuiltinToolFactory 并加入列表，无需改动调度逻辑。

```mermaid
classDiagram
class ToolRegistry {
+register_builtin_factory(factory)
+create_tool(po) Option<Box<dyn CoreTool>>
+list_builtin_ids() Vec<String>
}
class BuiltinToolFactory {
<<trait>>
+create_po() ToolPo
+create(po) Box<dyn CoreTool>
}
class FsReadToolFactory {
+tags : ["fs"]
}
class FsWriteToolFactory {
+tags : ["fs"]
}
class HttpFetchToolFactory {
+tags : ["http"]
}
class ShellExecToolFactory {
+tags : ["shell"]
}
ToolRegistry --> BuiltinToolFactory : "管理"
BuiltinToolFactory <|.. FsReadToolFactory
BuiltinToolFactory <|.. FsWriteToolFactory
BuiltinToolFactory <|.. HttpFetchToolFactory
BuiltinToolFactory <|.. ShellExecToolFactory
```

**图表来源**
- [src/pkg/tool_registry/mod.rs:46-102](file://src/pkg/tool_registry/mod.rs#L46-L102)
- [src/pkg/tool_registry/builtin.rs:17-43](file://src/pkg/tool_registry/builtin.rs#L17-L43)
- [src/pkg/tool_registry/fs_read.rs:54-100](file://src/pkg/tool_registry/fs_read.rs#L54-L100)
- [src/pkg/tool_registry/fs_write.rs:48-96](file://src/pkg/tool_registry/fs_write.rs#L48-L96)
- [src/pkg/tool_registry/http_fetch.rs:23-48](file://src/pkg/tool_registry/http_fetch.rs#L23-L48)
- [src/pkg/tool_registry/shell_exec.rs:96-153](file://src/pkg/tool_registry/shell_exec.rs#L96-L153)

**章节来源**
- [src/pkg/tool_registry/mod.rs:46-102](file://src/pkg/tool_registry/mod.rs#L46-L102)
- [src/pkg/tool_registry/builtin.rs:27-43](file://src/pkg/tool_registry/builtin.rs#L27-L43)

### 标签分组机制
**新增功能** 内置工具现在支持按标签分组，便于工具发现和批量管理：

- **标签定义**：每个内置工具在 create_po() 方法中设置 tags 字段
  - fs_read 和 fs_write：标签为 ["fs"]
  - http_fetch：标签为 ["http"]  
  - shell_exec：标签为 ["shell"]
- **工具包安装**：Agent 可通过 installed_tags 字段安装工具包
  - install_tag()：幂等安装指定标签的工具包
  - uninstall_tag()：卸载指定标签的工具包
  - has_tag()：检查是否已安装某标签
- **API 支持**：提供 GET /api/v1/finance/tools/tags 接口获取所有可用标签
- **前端集成**：支持搜索下拉框选择工具包，已安装包以卡片网格展示

**章节来源**
- [src/pkg/tool_registry/fs_read.rs:96](file://src/pkg/tool_registry/fs_read.rs#L96)
- [src/pkg/tool_registry/fs_write.rs:92](file://src/pkg/tool_registry/fs_write.rs#L92)
- [src/pkg/tool_registry/http_fetch.rs:43](file://src/pkg/tool_registry/http_fetch.rs#L43)
- [src/pkg/tool_registry/shell_exec.rs:148](file://src/pkg/tool_registry/shell_exec.rs#L148)
- [src/models/agent.rs:135-149](file://src/models/agent.rs#L135-L149)
- [src/handlers/finance/tool/list_tool_tags.rs:11-27](file://src/handlers/finance/tool/list_tool_tags.rs#L11-L27)

### 文件系统读取工具（fs_read）
- 功能特性
  - 支持全文读取、按行号范围读取、grep 风格匹配并附带上下文行。
  - 输出包含行号、匹配位置、前后上下文，便于编辑与定位。
- 参数配置
  - path：必填，相对工作目录的文件路径。
  - start_line/end_line：可选，1 起始的行范围。
  - grep：可选，正则/子串匹配模式。
  - context_lines：可选，匹配前后上下文行数，默认 2。
- 使用场景
  - 代码审查、日志分析、文档检索、变更影响面评估。
- 权限与安全
  - 路径解析与校验：仅允许 base_data_path 或 additional_allowed_paths 下的文件，拒绝符号链接与敏感文件名。
  - 大文件保护：超过硬限制（如 10MB）直接拒绝。
  - 越界访问：返回 require_confirmation=true 提示，要求用户确认后再操作。
- 错误处理
  - 参数解析失败、IO 异常均转换为统一错误类型，错误信息脱敏避免泄露绝对路径。

```mermaid
flowchart TD
Start(["调用 fs_read"]) --> Parse["解析参数 path/start_line/end_line/grep/context_lines"]
Parse --> ValidatePath["resolve_and_validate_path(base_path, path, additional_allowed)"]
ValidatePath --> |NeedConfirmation| Confirm["返回 require_confirmation=true"]
ValidatePath --> |Valid| CheckSize["检查文件大小是否超限"]
CheckSize --> |超限| Reject["返回错误：文件过大"]
CheckSize --> |OK| OpenFile["打开文件并逐行读取"]
OpenFile --> Mode{"是否 grep?"}
Mode --> |是| Grep["查找匹配并收集上下文"]
Mode --> |否| Range["计算行范围并格式化内容"]
Grep --> Return["返回 matches 列表"]
Range --> Return
Confirm --> End(["结束"])
Reject --> End
Return --> End
```

**图表来源**
- [src/pkg/tool_registry/fs_read.rs:125-216](file://src/pkg/tool_registry/fs_read.rs#L125-L216)
- [src/pkg/tool_registry/tool_security.rs:314-419](file://src/pkg/tool_registry/tool_security.rs#L314-L419)

**章节来源**
- [src/pkg/tool_registry/fs_read.rs:15-100](file://src/pkg/tool_registry/fs_read.rs#L15-L100)
- [src/pkg/tool_registry/fs_read.rs:125-216](file://src/pkg/tool_registry/fs_read.rs#L125-L216)
- [src/pkg/tool_registry/tool_security.rs:314-419](file://src/pkg/tool_registry/tool_security.rs#L314-L419)

### 文件系统写入工具（fs_write）
- 功能特性
  - 多种原子写入模式：覆盖写入、追加、插入指定行后、删除行范围、替换行范围。
  - 所有修改在内存中完成后再一次性写回，保证一致性。
- 参数配置
  - path：必填，目标文件路径。
  - mode：必填，枚举值包括 overwrite、append、insert_after、delete_range、replace_range。
  - content：部分模式必填（如 overwrite、append、insert_after、replace_range）。
  - after_line/start_line/end_line：按模式需要传入。
- 使用场景
  - 代码生成、配置文件更新、日志归档、批量文本编辑。
- 权限与安全
  - 基于 Agent 数据目录进行路径隔离，防止跨 Agent 干扰。
  - 同样遵循路径白名单、敏感文件名拦截、符号链接拒绝。
- 错误处理
  - 参数校验失败、IO 异常均返回结构化错误；范围非法会给出明确提示。

```mermaid
flowchart TD
S(["调用 fs_write"]) --> P["解析参数 path/mode/content/range"]
P --> V["validate_args(mode 相关必填项校验)"]
V --> |失败| Err["返回错误"]
V --> |成功| Path["resolve_and_validate_path(agent_data_dir, path)"]
Path --> |NeedConfirmation| Conf["返回 require_confirmation=true"]
Path --> |Valid| Read["读取现有文件行(若存在)"]
Read --> Apply{"mode"}
Apply --> |overwrite| Overwrite["替换全部行"]
Apply --> |append| Append["追加到末尾"]
Apply --> |insert_after| Insert["在 after_line 后插入"]
Apply --> |delete_range| Delete["删除 start..end 行"]
Apply --> |replace_range| Replace["替换 start..end 行为新内容"]
Overwrite --> Write["写回文件并 flush"]
Append --> Write
Insert --> Write
Delete --> Write
Replace --> Write
Write --> Done(["返回变更统计"])
```

**图表来源**
- [src/pkg/tool_registry/fs_write.rs:121-275](file://src/pkg/tool_registry/fs_write.rs#L121-L275)
- [src/pkg/tool_registry/tool_security.rs:314-419](file://src/pkg/tool_registry/tool_security.rs#L314-L419)

**章节来源**
- [src/pkg/tool_registry/fs_write.rs:16-100](file://src/pkg/tool_registry/fs_write.rs#L16-L100)
- [src/pkg/tool_registry/fs_write.rs:121-275](file://src/pkg/tool_registry/fs_write.rs#L121-L275)
- [src/pkg/tool_registry/fs_write.rs:277-323](file://src/pkg/tool_registry/fs_write.rs#L277-L323)

### HTTP 抓取工具（http_fetch）
- 功能特性
  - 仅允许 HTTPS 公共 URL，禁止重定向、关闭代理、限制响应体大小。
  - 对响应头进行敏感字段脱敏，返回状态码、头部摘要、内容长度与主体。
- 安全机制
  - SSRF 防护：默认拒绝本地网络与私有 IP，支持域名白/黑名单与 DNS 地址钉扎。
  - 超时与大小限制：默认 30s 超时，响应体最大 1MB（硬上限 10MB）。
- 使用场景
  - 获取公开 API 数据、远程配置、在线文档等。

**章节来源**
- [src/pkg/tool_registry/http_fetch.rs:19-143](file://src/pkg/tool_registry/http_fetch.rs#L19-L143)
- [src/pkg/tool_registry/tool_security.rs:1-231](file://src/pkg/tool_registry/tool_security.rs#L1-L231)

### Shell 执行工具（shell_exec）
- 功能特性
  - 支持同步执行（带超时与输出截断）与后台运行（PID 与日志路径返回）。
  - 工作目录限制在白名单内，输出按 trace_id 落盘至 tools/shell_exec/logs。
- 安全机制
  - 环境变量白名单过滤，屏蔽敏感变量名；工作目录校验拒绝越权路径。
  - 输出大小限制，超长输出保存为附件并在响应中提示。
- 使用场景
  - 构建脚本、数据处理、系统信息查询、自动化任务。

**章节来源**
- [src/pkg/tool_registry/shell_exec.rs:20-149](file://src/pkg/tool_registry/shell_exec.rs#L20-L149)
- [src/pkg/tool_registry/shell_exec.rs:256-466](file://src/pkg/tool_registry/shell_exec.rs#L256-L466)

### 元数据定义与控制模式
- ToolPo 承载工具元数据：id、name、description、protocol、control_mode、config、parameters_schema、**tags**、status 等。
- 内置工具通过 fill_defaults_for_builtin 确保 protocol=Builtin 且 control_mode 合理默认。
- 控制模式
  - Auto：由框架自动执行（如 fs_read、http_fetch）。
  - Manual：需人工确认或编排（如 shell_exec）。
- **新增** 标签字段：tags 为 JSON 字符串数组，支持工具分类和批量管理。

**章节来源**
- [src/models/tool.rs:57-88](file://src/models/tool.rs#L57-L88)
- [src/models/tool.rs:244-283](file://src/models/tool.rs#L244-L283)
- [src/models/tool.rs:259-265](file://src/models/tool.rs#L259-L265)
- [src/pkg/tool_registry/fs_read.rs:54-100](file://src/pkg/tool_registry/fs_read.rs#L54-L100)
- [src/pkg/tool_registry/fs_write.rs:48-96](file://src/pkg/tool_registry/fs_write.rs#L48-L96)
- [src/pkg/tool_registry/shell_exec.rs:93-144](file://src/pkg/tool_registry/shell_exec.rs#L93-L144)

### 权限控制与沙箱隔离
- 文件系统
  - resolve_and_validate_path：基路径 + 额外白名单校验，拒绝符号链接与敏感文件名。
  - fs_write 基于 agent_data_dir 隔离不同 Agent 的数据目录。
- 网络
  - validate_target_url：默认拒绝本地网络，支持域名白/黑名单与 DNS 钉扎。
- 环境变量
  - filter_inherited_environment：仅传递白名单环境变量，屏蔽敏感键名。

**章节来源**
- [src/pkg/tool_registry/tool_security.rs:314-486](file://src/pkg/tool_registry/tool_security.rs#L314-L486)
- [src/pkg/tool_registry/fs_write.rs:132-144](file://src/pkg/tool_registry/fs_write.rs#L132-L144)
- [src/pkg/tool_registry/shell_exec.rs:210-254](file://src/pkg/tool_registry/shell_exec.rs#L210-L254)

### 测试策略
- 单元测试
  - 路径校验与敏感文件名检测：验证正常路径通过、敏感路径拒绝。
  - 参数校验：覆盖各 mode 的必填项组合。
  - HTTP 抓取：验证 HTTPS 允许、HTTP 拒绝、本地/私有 IP 拒绝。
  - **新增** 标签分组测试：验证内置工具携带正确的标签。
- 集成测试
  - 工具调用流程、AOP 事件消费、JSONL 日志落盘。
  - **新增** 工具包安装测试：验证按标签批量安装工具。
- 建议
  - 针对边界条件（空输入、超大文件、超时、权限不足）补充用例。
  - 使用临时目录与模拟网络环境提升稳定性。

**章节来源**
- [src/pkg/tool_registry/fs_tests.rs:12-81](file://src/pkg/tool_registry/fs_tests.rs#L12-L81)
- [src/pkg/tool_registry/fs_write.rs:325-426](file://src/pkg/tool_registry/fs_write.rs#L325-L426)
- [src/pkg/tool_registry/http_fetch.rs:145-243](file://src/pkg/tool_registry/http_fetch.rs#L145-L243)
- [src/pkg/tool_registry/builtin.rs:49-67](file://src/pkg/tool_registry/builtin.rs#L49-L67)

### 错误处理与日志记录
- 错误处理
  - 统一使用 common::error::Result 包装错误，关键错误信息脱敏（如路径）。
  - 越权访问返回 require_confirmation=true，交由上层引导用户确认。
- 日志记录
  - 工具执行完成后发布 agent.tool.executed 事件。
  - ToolExecLogConsumer 订阅事件并写入 ToolCallLogger（每日 JSONL），便于审计与回溯。

**章节来源**
- [src/pkg/tool_registry/fs_read.rs:127-176](file://src/pkg/tool_registry/fs_read.rs#L127-L176)
- [src/pkg/tool_registry/fs_write.rs:123-171](file://src/pkg/tool_registry/fs_write.rs#L123-L171)
- [src/consumer/tool_exec_log_consumer.rs:26-50](file://src/consumer/tool_exec_log_consumer.rs#L26-L50)
- [src/pkg/tool_tracing/mod.rs:1-13](file://src/pkg/tool_tracing/mod.rs#L1-L13)

## 依赖关系分析
- 低耦合：ToolRegistry 不感知具体工具实现，仅依赖 BuiltinToolFactory 接口。
- 安全内聚：tool_security 被多个工具复用，避免重复实现。
- 可观测性：CoreTool 执行结果通过 AOP 事件解耦日志写入。
- **新增** 标签依赖：Agent 通过 installed_tags 依赖工具包的标签分组。

```mermaid
graph LR
TR["ToolRegistry"] --> BF["BuiltinToolFactory"]
BF --> FR["FsReadToolFactory(fs标签)"]
BF --> FW["FsWriteToolFactory(fs标签)"]
BF --> HF["HttpFetchToolFactory(http标签)"]
BF --> SE["ShellExecToolFactory(shell标签)"]
FR --> SEC["tool_security.fs"]
FW --> SEC
HF --> SEC_NET["tool_security.http"]
SE --> SEC_ENV["tool_security.env"]
CORE["CoreTool"] --> AOP["AOP 事件"]
AOP --> LOG["ToolExecLogConsumer -> ToolCallLogger"]
AG["Agent.installed_tags"] --> TAGS["标签分组"]
TAGS --> TOOLS["工具包批量安装"]
```

**图表来源**
- [src/pkg/tool_registry/mod.rs:46-102](file://src/pkg/tool_registry/mod.rs#L46-L102)
- [src/pkg/tool_registry/tool_security.rs:1-231](file://src/pkg/tool_registry/tool_security.rs#L1-L231)
- [src/consumer/tool_exec_log_consumer.rs:26-50](file://src/consumer/tool_exec_log_consumer.rs#L26-L50)
- [src/models/agent.rs:135-149](file://src/models/agent.rs#L135-L149)

**章节来源**
- [src/pkg/tool_registry/mod.rs:46-102](file://src/pkg/tool_registry/mod.rs#L46-L102)
- [src/pkg/tool_registry/tool_security.rs:1-231](file://src/pkg/tool_registry/tool_security.rs#L1-L231)

## 性能与资源限制
- 文件读取
  - 硬限制：单次读取最大 10MB，避免 OOM。
  - 流式读取：按行缓冲，减少内存占用。
- 文件写入
  - 内存中先构建新行集再一次性写回，保证原子性；注意大文件写入时的内存峰值。
- HTTP 抓取
  - 默认响应体限制 1MB，硬上限 10MB；禁用重定向与代理，降低攻击面。
- Shell 执行
  - 默认超时 5 分钟，输出上限 10MB；超长输出落盘并以摘要返回。
- **新增** 标签分组
  - 标签查询使用 SQL DISTINCT 去重，避免重复计算。
  - 工具包安装采用幂等操作，避免重复安装开销。
- 建议
  - 对超大文件启用分页/范围读取。
  - 对长耗时任务优先使用 background 模式并监控日志。

## 故障排查指南
- 路径访问被拒
  - 检查是否命中敏感文件名规则或不在白名单路径内；必要时添加 additional_allowed_paths。
- 文件过大
  - fs_read 超过 10MB 会被拒绝；改用 grep 或范围读取。
- 网络请求失败
  - 确认目标为 HTTPS 且非本地/私有 IP；如需访问内网，需在配置中显式允许。
- Shell 执行超时
  - 调整 timeout_ms 或改为 background 模式；查看 tools/shell_exec/logs/{trace_id}.log。
- 日志缺失
  - 确认 AOP 事件已发布且 ToolExecLogConsumer 已注册；检查 JSONL 日志目录权限。
- **新增** 工具包安装问题
  - 检查 Agent 的 installed_tags 是否正确设置。
  - 确认工具确实带有对应的标签。
  - 验证标签 API 是否正常返回可用标签列表。

**章节来源**
- [src/pkg/tool_registry/fs_read.rs:150-165](file://src/pkg/tool_registry/fs_read.rs#L150-L165)
- [src/pkg/tool_registry/http_fetch.rs:74-90](file://src/pkg/tool_registry/http_fetch.rs#L74-L90)
- [src/pkg/tool_registry/shell_exec.rs:281-287](file://src/pkg/tool_registry/shell_exec.rs#L281-L287)
- [src/consumer/tool_exec_log_consumer.rs:40-50](file://src/consumer/tool_exec_log_consumer.rs#L40-L50)

## 结论
内置工具系统通过工厂模式与注册表实现了高内聚、低耦合的工具生态。fs_read 与 fs_write 提供了安全可控的文件操作能力，配合严格的路径与权限校验，满足多 Agent 协作场景下的数据安全需求。HTTP 抓取与 Shell 执行进一步扩展了外部交互能力，并通过 AOP 与 JSONL 日志保障可观测性。**新增的标签分组机制**使得工具管理更加灵活，支持按功能域（http、fs、shell）进行批量安装和组织，提升了工具发现和管理的效率。遵循本文档的开发指南与最佳实践，可高效扩展新的内置工具并保持系统稳定与安全。

## 附录：自定义内置工具开发指南
- 步骤概览
  1. 实现 BuiltinToolFactory：提供 create_po() 定义默认元数据（id/name/description/parameters_schema/config/control_mode/**tags**），以及 create(po) 返回 Box<dyn CoreTool>。
  2. 实现 CoreTool：在 call(ctx, args) 中解析参数、执行安全校验、执行业务逻辑并返回 JSON 结果。
  3. 注册工具：在 GENERIC_BUILTIN_TOOLS 中添加 (id, factory)。
  4. 编写测试：覆盖参数校验、安全边界、错误分支与日志落盘。
- 最佳实践
  - 始终使用 tool_security 提供的校验函数，避免自行实现安全逻辑。
  - 对敏感信息进行脱敏，避免在错误消息中泄露路径或密钥。
  - 对长耗时或大数据量操作设置超时与大小限制，必要时采用后台模式。
  - 使用 RequestContext 传递 agent_id、trace_id 等上下文，便于隔离与追踪。
  - **新增** 为工具选择合适的标签：根据功能域选择 http、fs、shell 等标签，便于后续批量管理。
- 常见问题
  - 无法访问路径：检查 base_data_path 与 additional_allowed_paths 配置是否正确。
  - 参数校验失败：核对 parameters_schema 与实际入参类型是否一致。
  - 日志未写入：确认 AOP 事件已发布且消费者已注册。
  - **新增** 工具包安装失败：检查工具标签是否与 Agent 安装的标签匹配。
- 示例参考（路径引用）
  - 工厂与注册：[src/pkg/tool_registry/builtin.rs:27-43](file://src/pkg/tool_registry/builtin.rs#L27-L43)
  - 读取工具实现：[src/pkg/tool_registry/fs_read.rs:54-100](file://src/pkg/tool_registry/fs_read.rs#L54-L100)
  - 写入工具实现：[src/pkg/tool_registry/fs_write.rs:48-96](file://src/pkg/tool_registry/fs_write.rs#L48-L96)
  - 安全校验：[src/pkg/tool_registry/tool_security.rs:314-486](file://src/pkg/tool_registry/tool_security.rs#L314-L486)
  - 日志记录：[src/consumer/tool_exec_log_consumer.rs:26-50](file://src/consumer/tool_exec_log_consumer.rs#L26-L50)
  - **新增** 标签分组：[src/pkg/tool_registry/builtin.rs:49-67](file://src/pkg/tool_registry/builtin.rs#L49-L67)
  - **新增** 标签 API：[src/handlers/finance/tool/list_tool_tags.rs:11-27](file://src/handlers/finance/tool/list_tool_tags.rs#L11-L27)