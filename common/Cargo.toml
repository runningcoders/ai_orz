[package]
name = "common"
version = "0.1.0"
edition = "2024"

# Common types shared between backend and frontend
[dependencies]
serde = { version = "1.0", features = ["derive"] }
sqlx = { version = "0.8.6", features = ["macros"], default-features = false, optional = true }

