# 记忆系统架构设计文档

## 核心设计思想

### 分层记忆架构

**短期记忆** - 多条相关的、有逻辑联系的对话记录被聚合压缩为一条短期记忆，在当前对话会话中保持上下文。**不是**每条对话细节单独作为一条记忆。

**长期记忆/知识图谱** - 短期记忆经过沉淀（每日睡眠阶段），被消化吸收到知识图谱中长期保存，形成结构化的长期记忆。

这样的设计优势：

1. **当前会话上下文简洁**：短期记忆只保留聚合后的关键信息，不会膨胀导致上下文溢出
2. **长期知识结构化**：知识图谱结构方便检索和扩展，持久化保留历史知识
3. **自然沉淀**：每日自动沉淀，不需要手动操作
