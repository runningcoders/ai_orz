# A2A Server 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 让 ai_orz 作为 A2A Server 对外暴露 JSON-RPC 2.0 端点，外部注册用户持 JWT 通过 A2A 协议调用前台 Agent。

**Architecture:** 只在 handler 层完成 A2A 协议实体 ↔ ai_orz 内部实体的转换，domain 层零侵入。复用现有 JWT 认证、前台 Agent 路由、awaken 链路、project/message 管理。无新增表。

**Tech Stack:** Rust, Axum, serde, tokio, sqlx

**设计文档:** [docs/a2a_server_design.md](../../a2a_server_design.md)

---

## 文件结构

### 新增文件（11 个）

| 文件 | 职责 |
|------|------|
| `common/src/api/a2a.rs` | A2A 协议实体定义（AgentCard / JsonRpc / A2aTask / A2aMessage 等） |
| `common/src/api/a2a_test.rs` | 协议实体序列化/反序列化测试 |
| `src/handlers/a2a/mod.rs` | A2A handler 模块入口 |
| `src/handlers/a2a/mapper.rs` | A2A ↔ ai_orz 实体转换（纯函数，核心） |
| `src/handlers/a2a/mapper_test.rs` | mapper 单元测试 |
| `src/handlers/a2a/agent_card.rs` | `GET /.well-known/agent.json` 端点 |
| `src/handlers/a2a/jsonrpc.rs` | `POST /a2a` JSON-RPC 入口 + 方法分发 |
| `src/handlers/a2a/send_task.rs` | `tasks/send` 异步提交 |
| `src/handlers/a2a/get_task.rs` | `tasks/get` 查询任务 |
| `src/handlers/a2a/cancel_task.rs` | `tasks/cancel` 取消任务 |
| `src/handlers/hr/agent/get_reception_agent.rs` | `GET /api/v1/hr/agents/reception` handler（Task 0.1） |

### 修改文件（15 个）

| 文件 | 改动 |
|------|------|
| `common/src/api/mod.rs` | 注册 `a2a` 模块 |
| `common/src/api/project.rs` | `CreateProjectRequest` 新增 `owner_agent_id` 字段（Task 0.2） |
| `common/src/api/neural_tools.rs` | `SendMessageToAgentParams.to_agent_id` 改为 `Option<String>`（Task 0.3） |
| `common/src/config.rs` | 新增 `A2aServerConfig` 配置段 |
| `src/handlers/mod.rs` | 注册 `a2a` handler 模块 |
| `src/handlers/hr/agent/mod.rs` | 注册 `get_reception_agent` handler（Task 0.1） |
| `src/handlers/project/project/create_project.rs` | handler 纯粹透传 `owner_agent_id`（Task 0.2） |
| `src/handlers/finance/message/send_message_to_agent.rs` | handler 支持默认对话框（无 project）+ to_agent_id 未指定时 resolve_agent 兜底（Task 0.3） |
| `src/service/domain/hr/mod.rs` | `HrDomain` trait 新增 `resolve_agent` 方法 + `#[async_trait]`（Task 0.1） |
| `src/service/domain/project/mod.rs` | `ProjectManage::create` trait 新增 `owner_agent_id` 参数（Task 0.2） |
| `src/service/domain/project/project.rs` | `ProjectDomainImpl::create` 直接绑定 `owner_agent_id`（Task 0.2） |
| `src/consumer/adapter.rs` | `find_reception_agent_id` 改为调用 `resolve_agent(ctx)`（Task 0.1） |
| `src/router.rs` | 注册 A2A 路由 + `GET /api/v1/hr/agents/reception`（Task 0.1, 10） |
| `frontend/src/api/mod.rs` | 新增 `get_reception_agent_api` 函数（Task 0.3） |
| `frontend/src/pages/message/chat.rs` | 默认对话框不创建 project，直接发消息（Task 0.3） |

### 不改动

- `src/service/domain/message/` — 业务逻辑零改动
- `src/service/domain/runtime/` — 业务逻辑零改动
- 数据库 schema — 零改动

---

## Task 0.1: HrDomain 统一路由方法 `resolve_agent` + 前台 Agent 查询 HTTP API

**目标**：在 `HrDomain` trait 新增统一 Agent 路由方法 `resolve_agent(ctx) -> Result<Option<Agent>>`，**只接受 ctx，不耦合 project**（agent 与 project 是两个维度，由上层按需组合，不在 hr domain 中融合）。同时新增 HTTP API `GET /api/v1/hr/agents/reception` 让前端显式查询可用前台 Agent。

**关键约束：**
- `resolve_agent` 加到 `HrDomain` trait（不是 `AgentManage` trait）
- `HrDomain` trait 当前没有 `#[async_trait]`，新增 async 方法需要加上
- 实现在 `mod.rs` 的 `impl HrDomain for HrDomainImpl` 块（[mod.rs:79](../../src/service/domain/hr/mod.rs#L79)）中
- **`resolve_agent` 只接受 `ctx`，不接受 project 参数** — agent 与 project 是两个维度，不耦合到 hr domain 的选举逻辑中
- 复用现有 `agent_manage().query()` 能力
- `FEISHU_RECEPTION_ROLE` 常量已是 `pub`（[adapter.rs:25](../../src/consumer/adapter.rs#L25)），无需修改
- **新增 HTTP API**：`GET /api/v1/hr/agents/reception`（JWT 保护），调用 `resolve_agent(ctx)` 返回 agent 信息，供前端显示推荐前台 Agent
- **Handler 层职责**（按场景）：
  - 飞书 IM 接收消息：直接调 `resolve_agent(ctx)`
  - chat 默认对话框（前端无 project）：前端不创建 project，直接调 `send_message_to_agent`（`to_agent_id` 用户选定时显式传入，未选定时后端走 `resolve_agent(ctx)` 兜底）
  - chat Project 对话框（有 project）：handler 查 project，用 `project.owner_agent_id`；若为 None 则调 `resolve_agent(ctx)` 兜底
  - A2A `tasks/send`：handler 调 `resolve_agent(ctx)` 获取 agent → 用 `agent.id` 作为 `owner_agent_id` 创建 project
- 此 Task 是后续 Task 0.2/0.3/6 的基础

**Files:**
- Modify: `src/service/domain/hr/mod.rs` (trait 加 `#[async_trait]` + 新增 `resolve_agent` 方法 + impl 块实现)
- Modify: `src/consumer/adapter.rs:94` (`find_reception_agent_id` 改为调用 `resolve_agent(ctx)`)
- Create: `src/handlers/hr/agent/get_reception_agent.rs` (HTTP API handler)
- Modify: `src/handlers/hr/agent/mod.rs` (注册新 handler)
- Modify: `src/router.rs` (注册 `GET /api/v1/hr/agents/reception` 路由，JWT 保护)

- [x] **Step 1: 在 `src/service/domain/hr/mod.rs` 的 `HrDomain` trait 加 `#[async_trait]` 并新增方法**

找到 `pub trait HrDomain: Send + Sync {`（约 93 行），改为：

```rust
#[async_trait::async_trait]
pub trait HrDomain: Send + Sync {
    /// Agent 管理能力
    fn agent_manage(&self) -> &dyn AgentManage;
    /// Skill 管理能力
    fn skill_manage(&self) -> &dyn SkillManage;

    /// 统一前台 Agent 路由方法（供 Chat 页面、A2A、飞书 IM 共用）
    ///
    /// **agent 与 project 是两个维度，不在 hr domain 中融合**：
    /// - 本方法只负责返回当前可用的前台 Agent，不查询/感知 project
    /// - 调用方（handler 层）按需组合：先调本方法拿 agent，再创建/查询 project 绑定
    ///
    /// 路由优先级：
    /// 1. 优先 `feishu_reception` 角色 Onboarded Agent
    /// 2. fallback 任意 Onboarded Agent
    ///
    /// 返回 None 表示无可用 Agent。
    async fn resolve_agent(&self, ctx: RequestContext) -> Result<Option<Agent>>;
}
```

- [x] **Step 2: 在 `mod.rs` 的 `impl HrDomain for HrDomainImpl` 块加 `#[async_trait]` 并实现方法**

找到 `impl HrDomain for HrDomainImpl {`（约 79 行），改为：

```rust
#[async_trait::async_trait]
impl HrDomain for HrDomainImpl {
    fn agent_manage(&self) -> &dyn AgentManage {
        self
    }
    fn skill_manage(&self) -> &dyn SkillManage {
        self
    }

    async fn resolve_agent(&self, ctx: RequestContext) -> Result<Option<Agent>> {
        use crate::service::dao::agent::AgentQuery;
        use crate::consumer::adapter::FEISHU_RECEPTION_ROLE;

        // 优先级 1：feishu_reception 角色 Onboarded Agent
        let query = AgentQuery {
            roles: Some(vec![FEISHU_RECEPTION_ROLE.to_string()]),
            status: Some(AgentStatus::Onboarded),
            limit: Some(1),
            ..Default::default()
        };
        let agents = self.agent_manage().query(ctx.clone(), query).await?;
        if let Some(agent) = agents.into_iter().next() {
            return Ok(Some(agent));
        }

        // 优先级 2：任意 Onboarded Agent
        let query = AgentQuery {
            status: Some(AgentStatus::Onboarded),
            limit: Some(1),
            ..Default::default()
        };
        let agents = self.agent_manage().query(ctx, query).await?;
        Ok(agents.into_iter().next())
    }
}
```

**注意：**
- `resolve_agent` 只接受 `ctx`，**不接受 project 参数** — agent 与 project 是两个维度，不耦合
- `AgentStatus` 已在 mod.rs 顶部 import（第 27 行），`Agent` 已在第 17 行 import，`RequestContext` 已在第 19 行 import，`Result` 已在第 16 行 import
- 不再需要 `crate::models::project::Project` 类型导入

**关键依赖：** 需要确认 `AgentManage` trait 有 `get(ctx, id) -> Result<Option<Agent>>` 方法（Task 0.3 handler 层判断 owner_agent_id 后查询 agent 时需要）。若无，需要先添加（见 Step 3）。

- [x] **Step 3: 确认 `AgentManage::get` 方法存在**

Run: `grep -n "async fn get" src/service/domain/hr/mod.rs`

如果输出包含类似 `async fn get(&self, ctx: RequestContext, id: &str) -> Result<Option<Agent>>` 的方法，则继续下一步。如果不存在，需要在 `AgentManage` trait 新增：

```rust
async fn get(&self, ctx: RequestContext, id: &str) -> Result<Option<Agent>>;
```

并在 `impl AgentManage for HrDomainImpl` 块中实现（调用 `agent_dal().get(ctx, id).await`）。

- [x] **Step 4: 修改 `src/consumer/adapter.rs` 的 `find_reception_agent_id`，改为调用 `resolve_agent`**

将 `ConsumerMessageCallback` 的 `find_reception_agent_id` 方法体（约 94-128 行）替换为：

```rust
    /// 查找路由目标 Agent ID（委托给 HrDomain.resolve_agent）
    async fn find_reception_agent_id(&self, ctx: RequestContext) -> Result<Option<String>> {
        let agent = self.hr_domain.resolve_agent(ctx).await?;
        Ok(agent.map(|a| a.po.id))
    }
```

- [x] **Step 5: 创建 `src/handlers/hr/agent/get_reception_agent.rs` HTTP API handler**

```rust
//! Handler: GET /api/v1/hr/agents/reception
//!
//! 返回当前可用的前台 Agent（供前端显示推荐前台 Agent，如默认对话框顶部显示"当前前台：XXX"）。
//! JWT 保护，从 ctx 提取用户身份。

use axum::{Extension, Json};
use common::api::ApiResponse;
use common::error::Result;

use crate::pkg::RequestContext;
use crate::service::domain::hr::domain as hr_domain;

pub async fn get_reception_agent(
    Extension(ctx): Extension<RequestContext>,
) -> Result<Json<ApiResponse<ReceptionAgentResponse>>> {
    let agent = hr_domain()
        .resolve_agent(ctx)
        .await?
        .ok_or_else(|| common::error::Error::not_found("无可用的前台 Agent"))?;

    Ok(Json(ApiResponse::success(ReceptionAgentResponse {
        agent_id: agent.po.id.clone(),
        agent_name: agent.po.name.clone(),
    })))
}

#[derive(serde::Serialize)]
pub struct ReceptionAgentResponse {
    pub agent_id: String,
    pub agent_name: String,
}
```

**说明：**
- 响应只返回 `agent_id` 和 `agent_name`，避免暴露内部细节
- 若无可用 agent，返回 404 让前端处理
- 路由注册在 Step 7 中完成（与 `request_context_middleware` + `jwt_auth_middleware` 一起挂载）

- [x] **Step 6: 在 `src/handlers/hr/agent/mod.rs` 注册新 handler**

在 mod.rs 中新增：

```rust
pub mod get_reception_agent;
```

- [x] **Step 7: 在 `src/router.rs` 注册 `GET /api/v1/hr/agents/reception` 路由**

在受保护路由组（JWT + RequestContext 中间件）中新增：

```rust
.route(
    "/api/v1/hr/agents/reception",
    axum::routing::get(handlers::hr::agent::get_reception_agent::get_reception_agent),
)
```

- [x] **Step 8: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 9: 运行现有测试确保不破坏**

Run: `cargo test --lib consumer && cargo test --lib service::domain::hr`
Expected: 所有现有测试 PASS

- [x] **Step 10: 提交**

```bash
git add src/service/domain/hr/mod.rs src/consumer/adapter.rs src/handlers/hr/agent/get_reception_agent.rs src/handlers/hr/agent/mod.rs src/router.rs
git commit -m "feat: 新增 resolve_agent 路由方法 + 前台 Agent 查询 HTTP API"
```

---

## Task 0.2: 修复 chat 缺陷 — `CreateProjectRequest` 支持 `owner_agent_id`（handler 纯粹接收参数，不做兜底路由）

**目标**：修复 `owner_agent_id` 无法通过 API 设置的缺陷。`CreateProjectRequest` 新增 `owner_agent_id` 字段，**create_project handler 纯粹接收参数，不在内部调用 resolve_agent 兜底**。Project 创建由 Agent 内部决策触发（不在本次范围），create_project API 供 A2A tasks/send 等场景使用，handler 纯粹透传 `owner_agent_id`。

**关键约束：**
- `CreateProjectRequest` 新增 `owner_agent_id: Option<String>` 字段
- `ProjectManage::create` trait 接收 `owner_agent_id: Option<String>` 参数
- `ProjectDomainImpl::create` 实现只负责将 `owner_agent_id` 绑定到 Project，**不内部调用 resolve_agent**
- **create_project handler 纯粹接收 `params.owner_agent_id`，不做兜底路由** — Project 创建由 Agent 内部决策触发（不在本次范围），create_project API 供 A2A tasks/send 等场景使用
- create_project handler **不依赖 hr domain**（解耦）
- 若未传 `owner_agent_id`，project 的 `owner_agent_id` 即为 None（后续 send_message_to_agent 会走 `resolve_agent(ctx)` 兜底路由）

**Files:**
- Modify: `common/src/api/project.rs:8` (`CreateProjectRequest` 新增 `owner_agent_id` 字段)
- Modify: `src/service/domain/project/mod.rs:80` (`ProjectManage::create` trait 新增 `owner_agent_id` 参数)
- Modify: `src/service/domain/project/project.rs:21` (`ProjectDomainImpl::create` impl 接收 `owner_agent_id`，去掉硬编码 None，直接绑定)
- Modify: `src/handlers/project/project/create_project.rs:23` (handler 纯粹透传 `owner_agent_id`，不调 resolve_agent)

- [x] **Step 1: 在 `common/src/api/project.rs` 的 `CreateProjectRequest` 新增字段**

在 `pub tags: Option<Vec<String>>,` 后新增：

```rust
    /// 负责人 Agent ID（可选，由前端显式查询 `GET /api/v1/hr/agents/reception` 后传入）
    pub owner_agent_id: Option<String>,
```

- [x] **Step 2: 在 `src/service/domain/project/mod.rs` 的 `ProjectManage::create` trait 方法新增参数**

找到 `async fn create(`（约 83 行），在 `tags: Vec<String>,` 后、`root_user_id: String,` 前新增：

```rust
        tags: Vec<String>,
        owner_agent_id: Option<String>,
        root_user_id: String,
```

- [x] **Step 3: 在 `src/service/domain/project/project.rs` 的 `ProjectDomainImpl::create` impl 修改签名和实现**

找到 `impl super::ProjectManage for ProjectDomainImpl`（约 18 行），将 `create` 方法签名改为：

```rust
    async fn create(
        &self,
        ctx: RequestContext,
        name: String,
        description: String,
        priority: i32,
        tags: Vec<String>,
        owner_agent_id: Option<String>,
        root_user_id: String,
        created_by: String,
    ) -> Result<Project> {
```

在实现体内，**直接绑定 `owner_agent_id`，不内部调用 resolve_agent**：

```rust
        let project_id = Uuid::now_v7().to_string();

        let project = Project::new(
            project_id,
            name,
            description,
            None, // workflow
            None, // guidance
            priority,
            tags,
            root_user_id,
            owner_agent_id,  // ← 直接绑定，由前端显式传入
            None, // start_at
            None, // due_at
            None, // end_at
            created_by.clone(),
        );

        self.project_dal.create(ctx.clone(), &project).await?;
```

- [x] **Step 4: 在 `src/handlers/project/project/create_project.rs` 的 handler 中纯粹透传 `owner_agent_id`**

找到 `domain().project_manage().create(` 调用（约 29 行），改为：

```rust
    // handler 纯粹透传 owner_agent_id，不做兜底路由
    // Project 创建由 Agent 内部决策触发（不在本次范围），
    // create_project API 供 A2A tasks/send 等场景使用，handler 纯粹透传 owner_agent_id
    let project = domain()
        .project_manage()
        .create(
            ctx,
            params.name,
            params.description.unwrap_or_default(),
            params.priority.unwrap_or_default(),
            params.tags.unwrap_or_default(),
            params.owner_agent_id,  // ← 纯粹透传，未传则为 None
            current_user_id.clone(),
            current_user_id,
        )
        .await?;
```

- [x] **Step 5: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 6: 运行现有测试确保不破坏**

Run: `cargo test --lib handlers::project && cargo test --lib service::domain::project`
Expected: 所有现有测试 PASS

- [x] **Step 7: 提交**

```bash
git add common/src/api/project.rs src/service/domain/project/mod.rs src/service/domain/project/project.rs src/handlers/project/project/create_project.rs
git commit -m "fix: 修复 owner_agent_id 无法设置的缺陷，handler 纯粹透传不兜底路由"
```

---

## Task 0.3: 修复 chat 缺陷 — `send_message_to_agent` 支持默认对话框（无 project）+ to_agent_id 保持 Option + 后端 resolve_agent 兜底

**目标**：
1. 让 chat 页面后端 handler 支持**两种对话上下文**（协作关系类比）：
   - **默认对话框**：与前台 Agent 直接沟通（无 project_id），后端 `resolve_agent(ctx)` 兜底路由
   - **Project 对话框**：在 Project 上下文中沟通（有 project_id），后端从 `project.owner_agent_id` 取
2. `to_agent_id` 保持 `Option<String>`：用户选定 Agent 时前端显式传入；未指定时后端走 `resolve_agent(ctx)` 兜底
3. **不在前端创建默认 project**：Project 创建由 Agent 内部决策触发（识别复杂需求后创建），不在本次 A2A Server 范围

**协作关系类比（核心理念）**：
- 类比找公司团队帮忙做事：(a) 前台接待了解需求 → (b) 简单需求前台直接处理 → (c) 复杂需求交 PMO 创建 Project
- 对应前端两种对话框：(a) 默认对话框=与前台直接沟通（无 project）→ (b) Project 对话框=Project 上下文沟通
- 默认对话框不创建 project，直接发消息（to_agent_id 未指定时后端走 resolve_agent 兜底）

**关键约束：**
- handler 层支持 `project_id=None`（默认对话框场景）
- `to_agent_id` 显式指定 → 直接用（用户选定 Agent 优先）
- `to_agent_id` 未指定 + `project_id` 存在 → 从 `project.owner_agent_id` 取；若为 None 则调 `resolve_agent(ctx)` 兜底
- `to_agent_id` 未指定 + `project_id=None`（默认对话框）→ 调 `resolve_agent(ctx)` 兜底
- **handler 层调 `resolve_agent(ctx)` 是合理的** — resolve_agent 本就是给 handler 用的统一路由方法
- **不在前端创建默认 project** — Project 创建由 Agent 内部决策触发（不在本次范围）
- `to_agent_id` 保持 `Option<String>`，不强制必填

**Files:**
- Modify: `common/src/api/neural_tools.rs:153` (`SendMessageToAgentParams.to_agent_id` 改为 `Option<String>`)
- Modify: `src/handlers/finance/message/send_message_to_agent.rs:22` (handler 支持默认对话框 + resolve_agent 兜底)
- Modify: `frontend/src/pages/message/chat.rs:298-309` (chat 默认对话框不创建 project，直接发消息)

- [x] **Step 1: 在 `common/src/api/neural_tools.rs` 修改 `SendMessageToAgentParams.to_agent_id` 为 Option**

找到 `pub struct SendMessageToAgentParams {`（约 155 行），将 `to_agent_id` 字段改为：

```rust
    /// 接收 Agent ID（可选，未指定时从 project.owner_agent_id 取）
    #[serde(default)]
    pub to_agent_id: Option<String>,
```

- [x] **Step 2: 在 `src/handlers/finance/message/send_message_to_agent.rs` 修改 handler，支持默认对话框 + resolve_agent 兜底**

将 handler 实现改为：

```rust
pub async fn send_message_to_agent(
    ctx: RequestContext,
    params: SendMessageToAgentParams,
) -> Result<SendMessageToAgentResponse> {
    // 判断发送者身份：优先 Agent，其次 User，最后 System
    let (from_id, from_role) = if let Some(aid) = ctx.agent_id() {
        (aid.to_string(), MessageRole::Agent)
    } else if !ctx.uid().is_empty() {
        (ctx.uid(), MessageRole::User)
    } else {
        ("system".to_string(), MessageRole::System)
    };

    // 路由 to_agent_id（协作关系类比）：
    // 1. 显式指定优先（用户在前端选定 Agent）
    // 2. 否则查 project（Project 对话框场景），用 project.owner_agent_id
    // 3. 若 project.owner_agent_id 为 None 或 project_id 未指定（默认对话框场景）→ 调 resolve_agent(ctx) 兜底
    //
    // 协作关系类比：
    // - 默认对话框=与前台直接沟通（无 project_id），后端走 resolve_agent 兜底
    // - Project 对话框=Project 上下文沟通（有 project_id），后端从 project.owner_agent_id 取
    // - Project 创建由 Agent 内部决策触发，不在本次范围
    let to_agent_id = match params.to_agent_id {
        Some(id) if !id.is_empty() => id,
        _ => {
            // 优先从 project.owner_agent_id 取（Project 对话框场景）
            if let Some(project_id) = params.project_id.as_deref() {
                let project = crate::service::domain::project::domain()
                    .project_manage()
                    .get(ctx.clone(), project_id)
                    .await?
                    .ok_or_else(|| {
                        common::error::Error::not_found(format!("Project {} not found", project_id))
                    })?;

                if let Some(agent_id) = project.po.owner_agent_id {
                    agent_id
                } else {
                    // project 未绑定 agent，走 resolve_agent 兜底
                    let agent = crate::service::domain::hr::domain()
                        .resolve_agent(ctx.clone())
                        .await?
                        .ok_or_else(|| common::error::Error::not_found("无可用前台 Agent"))?;
                    agent.po.id
                }
            } else {
                // 默认对话框场景（无 project_id），走 resolve_agent 兜底
                let agent = crate::service::domain::hr::domain()
                    .resolve_agent(ctx.clone())
                    .await?
                    .ok_or_else(|| common::error::Error::not_found("无可用前台 Agent"))?;
                agent.po.id
            }
        }
    };

    let cmd = SendToAgentCommand {
        from_id: &from_id,
        from_role,
        to_agent_id: &to_agent_id,
        content: &params.content,
        project_id: params.project_id.as_deref(),
        task_id: params.task_id.as_deref(),
        reply_to_id: params.reply_to_id.as_deref(),
        attachment_ids: params.attachment_ids.as_deref(),
    };

    let message = message::domain().delivery().send_to_agent(ctx, cmd).await?;

    Ok(SendMessageToAgentResponse {
        message_id: message.po.id,
    })
}
```

- [x] **Step 3: 同步更新前端 chat.rs，默认对话框不创建 project，直接发消息**

找到 `frontend/src/pages/message/chat.rs:298-309`，将 `to_agent_id` 派生逻辑和发送逻辑改为：

```rust
// 两种对话上下文（协作关系类比）：
// (a) 默认对话框：与前台 Agent 直接沟通（无 project_id）
//     - 用户选定 Agent 时 to_agent_id 显式传入（用户指定优先）
//     - 用户未选定 Agent 时 to_agent_id 不传，后端走 resolve_agent(ctx) 兜底
// (b) Project 对话框：在 Project 上下文中沟通（有 project_id）
//     - to_agent_id 可不传，后端从 project.owner_agent_id 取
//
// 关键：不在前端创建默认 project！
// Project 创建由 Agent 内部决策触发（识别复杂需求后创建），不在本次 A2A Server 范围。
// 用户在默认对话框中选定 Agent 时，以用户指定为准（to_agent_id 显式传入）。

// 发送消息
let req = SendMessageToAgentParams {
    to_agent_id: selected_agent_id.clone(),  // Option<String>，用户选定 Agent 时传入，否则后端走 resolve_agent 兜底
    content: text.clone(),
    project_id: project_id.clone(),  // Option<String>，默认对话框为 None，Project 对话框为 Some
    task_id: None,
    reply_to_id: None,
    attachment_ids: attachment_ids_opt,
};
```

**关键说明：**
- **默认对话框不创建 project**：`project_id` 为 `None`，`to_agent_id` 由用户选择决定（可选）
- **Project 对话框**：`project_id` 为 `Some`，`to_agent_id` 可不传，后端从 `project.owner_agent_id` 取
- **用户选定 Agent 优先**：前端通过 UI（如下拉选择）让用户选定 Agent，选定时 `to_agent_id=Some(agent_id)`，未选定时 `to_agent_id=None`，后端走 `resolve_agent(ctx)` 兜底
- **不创建默认 project**：Project 创建由 Agent 内部决策触发（识别复杂需求后创建），不在本次范围
- 前端 UI 需支持切换「默认对话框」和「Project 对话框」（具体 UI 实现根据现有 chat 页面结构调整）

- [x] **Step 4: 在前端 API 客户端新增 `get_reception_agent_api` 函数（可选，用于显示推荐前台 Agent）**

在前端 API 客户端文件中（与 `create_project_api` 同一位置）新增：

```rust
/// 查询当前可用的前台 Agent（用于前端显示推荐 Agent，或用户选定 Agent 时作为默认选项）
pub async fn get_reception_agent_api() -> Result<ReceptionAgentResponse, common::error::Error> {
    let resp = get::<ApiResponse<ReceptionAgentResponse>>("/api/v1/hr/agents/reception").await?;
    Ok(resp.data)
}

#[derive(Debug, Clone, serde::Deserialize)]
pub struct ReceptionAgentResponse {
    pub agent_id: String,
    pub agent_name: String,
}
```

**说明：**
- 此 API 对应 Task 0.1 新增的 `GET /api/v1/hr/agents/reception` 端点
- **用途**：前端显示推荐前台 Agent（如默认对话框顶部显示"当前前台：XXX"），或作为用户选定 Agent 的默认选项
- **不再用于"前端显式两步"流程**（第四轮调整后默认对话框不创建 project）
- 具体 HTTP 工具函数名和错误类型根据前端现有模式调整

- [x] **Step 5: 验证编译**

Run: `cargo check && cd frontend && cargo check`
Expected: 编译通过

- [x] **Step 6: 运行现有测试确保不破坏**

Run: `cargo test --lib handlers::finance::message`
Expected: 所有现有测试 PASS

- [x] **Step 7: 提交**

```bash
git add common/src/api/neural_tools.rs src/handlers/finance/message/send_message_to_agent.rs frontend/src/pages/message/chat.rs frontend/src/api/mod.rs
git commit -m "fix: send_message_to_agent 支持默认对话框（无 project）+ resolve_agent 兜底"
```

---

## Task 1: A2A 协议实体定义

**Files:**
- Create: `common/src/api/a2a.rs`
- Create: `common/src/api/a2a_test.rs`
- Modify: `common/src/api/mod.rs:96` (在 `pub mod external_agent;` 后新增 `pub mod a2a;`)

- [x] **Step 1: 在 `common/src/api/mod.rs` 注册模块**

在 `pub mod external_agent;`（约第 96 行）后新增：

```rust
pub mod a2a;
#[cfg(test)]
mod a2a_test;
```

并在 re-exports 区（约第 127 行 `pub use external_agent::*;` 附近）新增：

```rust
pub use a2a::*;
```

- [x] **Step 2: 创建 `common/src/api/a2a.rs`，定义协议实体**

```rust
//! A2A (Agent-to-Agent) Protocol API 类型
//!
//! 定义 A2A 协议 v0.3.0 的核心实体，前后端共享。
//! 参考：https://github.com/google/A2A

use schemars::JsonSchema;
use serde::{Deserialize, Serialize};
use serde_json::Value;

// ===== Agent Card =====

/// Agent Card — 对外暴露的组织能力描述
///
/// 通过 `GET /.well-known/agent.json` 公开访问，无需认证。
/// 对外只暴露一个统一入口（组织级），不列具体内部 Agent。
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct AgentCard {
    /// 组织名称
    pub name: String,
    /// 组织描述
    #[serde(skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// 协议版本（如 "0.3.0"）
    pub version: String,
    /// 协议端点 URL（如 "http://host/a2a"）
    pub url: String,
    /// 能力声明
    pub capabilities: AgentCapabilities,
    /// 对外技能列表（组织级，非具体 Agent）
    #[serde(default)]
    pub skills: Vec<AgentSkill>,
    /// 默认输入模式
    pub default_input_modes: Vec<String>,
    /// 默认输出模式
    pub default_output_modes: Vec<String>,
}

/// Agent 能力声明
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct AgentCapabilities {
    /// 是否支持 SSE 流式
    #[serde(default)]
    pub streaming: bool,
    /// 是否支持推送通知
    #[serde(default)]
    pub push_notifications: bool,
}

/// Agent 技能描述
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct AgentSkill {
    /// 技能 ID
    pub id: String,
    /// 技能名称
    pub name: String,
    /// 技能描述
    #[serde(skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// 标签列表
    #[serde(default)]
    pub tags: Vec<String>,
}

// ===== JSON-RPC 2.0 =====

/// JSON-RPC 2.0 请求
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct JsonRpcRequest {
    /// 协议版本，固定 "2.0"
    pub jsonrpc: String,
    /// 请求 ID（string | number | null）
    pub id: Value,
    /// 方法名
    pub method: String,
    /// 参数
    #[serde(default)]
    pub params: Value,
}

/// JSON-RPC 2.0 响应
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct JsonRpcResponse {
    /// 协议版本
    pub jsonrpc: String,
    /// 请求 ID（与请求对应）
    pub id: Value,
    /// 成功结果（与 error 互斥）
    #[serde(skip_serializing_if = "Option::is_none")]
    pub result: Option<Value>,
    /// 错误信息（与 result 互斥）
    #[serde(skip_serializing_if = "Option::is_none")]
    pub error: Option<JsonRpcError>,
}

impl JsonRpcResponse {
    /// 成功响应
    pub fn success(id: Value, result: Value) -> Self {
        Self {
            jsonrpc: "2.0".to_string(),
            id,
            result: Some(result),
            error: None,
        }
    }

    /// 错误响应
    pub fn error(id: Value, code: i32, message: String) -> Self {
        Self {
            jsonrpc: "2.0".to_string(),
            id,
            result: None,
            error: Some(JsonRpcError { code, message, data: None }),
        }
    }
}

/// JSON-RPC 错误
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct JsonRpcError {
    /// 错误码
    pub code: i32,
    /// 错误消息
    pub message: String,
    /// 附加数据
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data: Option<Value>,
}

/// JSON-RPC 标准错误码
pub mod error_codes {
    /// 解析错误
    pub const PARSE_ERROR: i32 = -32700;
    /// 无效请求
    pub const INVALID_REQUEST: i32 = -32600;
    /// 方法未找到
    pub const METHOD_NOT_FOUND: i32 = -32601;
    /// 参数无效
    pub const INVALID_PARAMS: i32 = -32602;
    /// 内部错误
    pub const INTERNAL_ERROR: i32 = -32603;
}

// ===== Task =====

/// A2A Task — 对应 ai_orz 的 Project
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct A2aTask {
    /// Task ID（= ai_orz project id）
    pub id: String,
    /// 会话 ID（用于多轮对话关联）
    #[serde(skip_serializing_if = "Option::is_none")]
    pub session_id: Option<String>,
    /// 任务状态
    pub status: A2aTaskStatus,
    /// 消息流
    #[serde(default)]
    pub messages: Vec<A2aMessage>,
    /// 产物列表
    #[serde(default)]
    pub artifacts: Vec<A2aArtifact>,
    /// 元数据
    #[serde(default)]
    pub metadata: Value,
}

/// 任务状态
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct A2aTaskStatus {
    /// 状态枚举
    pub state: A2aTaskState,
    /// 时间戳（ISO 8601）
    pub timestamp: String,
    /// 可选状态消息
    #[serde(skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
}

/// 任务状态枚举
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
#[serde(rename_all = "snake_case")]
pub enum A2aTaskState {
    /// 已提交
    Submitted,
    /// 处理中
    Working,
    /// 需要输入
    InputRequired,
    /// 已完成
    Completed,
    /// 失败
    Failed,
    /// 已取消
    Canceled,
}

// ===== Message =====

/// A2A 消息 — 对应 ai_orz 的 MessagePo
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct A2aMessage {
    /// 角色："user" 或 "agent"
    pub role: String,
    /// 消息内容部分
    pub parts: Vec<A2aMessagePart>,
    /// 消息 ID（= ai_orz message id）
    #[serde(skip_serializing_if = "Option::is_none")]
    pub message_id: Option<String>,
    /// 关联 Task ID
    #[serde(skip_serializing_if = "Option::is_none")]
    pub task_id: Option<String>,
}

/// 消息内容部分
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
#[serde(tag = "type")]
pub enum A2aMessagePart {
    /// 文本
    Text {
        text: String,
    },
    /// 文件
    File {
        file: A2aFilePart,
    },
    /// 结构化数据
    Data {
        data: Value,
    },
}

/// 文件部分
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct A2aFilePart {
    /// 文件名
    pub name: String,
    /// MIME 类型
    #[serde(skip_serializing_if = "Option::is_none")]
    pub mime_type: Option<String>,
    /// base64 编码内容
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bytes: Option<String>,
    /// 文件 URI
    #[serde(skip_serializing_if = "Option::is_none")]
    pub uri: Option<String>,
}

// ===== Artifact =====

/// 任务产物
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct A2aArtifact {
    /// 产物 ID（= ai_orz artifact id）
    pub artifact_id: String,
    /// 产物名称
    pub name: String,
    /// 内容部分
    pub parts: Vec<A2aMessagePart>,
}

// ===== 方法参数 =====

/// `tasks/send` 请求参数
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SendTaskParams {
    /// 客户端生成的 task id（ai_orz 忽略，使用自己生成的 project id）
    pub id: String,
    /// 消息内容
    pub message: A2aMessage,
    /// 会话 ID
    #[serde(skip_serializing_if = "Option::is_none")]
    pub session_id: Option<String>,
    /// 元数据
    #[serde(skip_serializing_if = "Option::is_none")]
    pub metadata: Option<Value>,
}

/// `tasks/get` 请求参数
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetTaskParams {
    /// Task ID
    pub id: String,
    /// 历史消息长度限制
    #[serde(skip_serializing_if = "Option::is_none")]
    pub history_length: Option<i32>,
}

/// `tasks/cancel` 请求参数
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CancelTaskParams {
    /// Task ID
    pub id: String,
}
```

- [x] **Step 3: 创建 `common/src/api/a2a_test.rs`，测试序列化**

```rust
//! A2A 协议实体测试

use super::a2a::*;
use serde_json::json;

#[test]
fn agent_card_serializes_correctly() {
    let card = AgentCard {
        name: "Test Org".to_string(),
        description: Some("test".to_string()),
        version: "0.3.0".to_string(),
        url: "http://localhost/a2a".to_string(),
        capabilities: AgentCapabilities {
            streaming: true,
            push_notifications: false,
        },
        skills: vec![AgentSkill {
            id: "chat".to_string(),
            name: "对话".to_string(),
            description: None,
            tags: vec![],
        }],
        default_input_modes: vec!["text".to_string()],
        default_output_modes: vec!["text".to_string()],
    };

    let json = serde_json::to_value(&card).unwrap();
    assert_eq!(json["name"], "Test Org");
    assert_eq!(json["version"], "0.3.0");
    assert_eq!(json["capabilities"]["streaming"], true);
    assert_eq!(json["skills"][0]["id"], "chat");
}

#[test]
fn jsonrpc_response_success_serializes_correctly() {
    let resp = JsonRpcResponse::success(json!(1), json!({"id": "task-1"}));
    let json_val = serde_json::to_value(&resp).unwrap();
    assert_eq!(json_val["jsonrpc"], "2.0");
    assert_eq!(json_val["id"], 1);
    assert_eq!(json_val["result"]["id"], "task-1");
    assert!(json_val.get("error").is_none() || json_val["error"].is_null());
}

#[test]
fn jsonrpc_response_error_serializes_correctly() {
    let resp = JsonRpcResponse::error(json!(1), -32601, "Method not found".to_string());
    let json_val = serde_json::to_value(&resp).unwrap();
    assert_eq!(json_val["jsonrpc"], "2.0");
    assert_eq!(json_val["error"]["code"], -32601);
    assert_eq!(json_val["error"]["message"], "Method not found");
}

#[test]
fn a2a_message_part_text_tagged_correctly() {
    let part = A2aMessagePart::Text {
        text: "hello".to_string(),
    };
    let json_val = serde_json::to_value(&part).unwrap();
    assert_eq!(json_val["type"], "text");
    assert_eq!(json_val["text"], "hello");
}

#[test]
fn a2a_task_state_serializes_snake_case() {
    let state = A2aTaskState::InputRequired;
    let json_val = serde_json::to_value(&state).unwrap();
    assert_eq!(json_val, "input_required");
}

#[test]
fn send_task_params_deserializes_correctly() {
    let json_str = r#"{
        "id": "client-task-1",
        "message": {
            "role": "user",
            "parts": [{"type": "text", "text": "你好"}]
        }
    }"#;
    let params: SendTaskParams = serde_json::from_str(json_str).unwrap();
    assert_eq!(params.id, "client-task-1");
    assert_eq!(params.message.role, "user");
    assert_eq!(params.message.parts.len(), 1);
    assert!(params.session_id.is_none());
}
```

- [x] **Step 4: 运行测试验证**

Run: `cargo test -p common --lib api::a2a_test`
Expected: 6 tests PASS

- [x] **Step 5: 提交**

```bash
git add common/src/api/a2a.rs common/src/api/a2a_test.rs common/src/api/mod.rs
git commit -m "feat(a2a): 定义 A2A 协议实体类型"
```

---

## Task 2: A2A Server 配置项

**Files:**
- Modify: `common/src/config.rs:23` (在 `AppConfig` struct 新增 `a2a_server` 字段)

- [x] **Step 1: 在 `common/src/config.rs` 的 `AppConfig` 新增字段**

在 `pub lark: LarkConfig,` 后新增：

```rust
    /// A2A Server 配置
    #[serde(default)]
    pub a2a_server: A2aServerConfig,
```

- [x] **Step 2: 在 `common/src/config.rs` 末尾新增 `A2aServerConfig` 定义**

```rust
/// A2A Server 配置
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct A2aServerConfig {
    /// 是否启用 A2A Server
    #[serde(default)]
    pub enabled: bool,
    /// 协议版本
    #[serde(default = "default_a2a_protocol_version")]
    pub protocol_version: String,
    /// JSON-RPC 端点路径
    #[serde(default = "default_a2a_endpoint")]
    pub endpoint: String,
    /// Agent Card 路径
    #[serde(default = "default_a2a_card_path")]
    pub card_path: String,
}

impl Default for A2aServerConfig {
    fn default() -> Self {
        Self {
            enabled: false,
            protocol_version: default_a2a_protocol_version(),
            endpoint: default_a2a_endpoint(),
            card_path: default_a2a_card_path(),
        }
    }
}

fn default_a2a_protocol_version() -> String {
    "0.3.0".to_string()
}

fn default_a2a_endpoint() -> String {
    "/a2a".to_string()
}

fn default_a2a_card_path() -> String {
    "/.well-known/agent.json".to_string()
}
```

- [x] **Step 3: 验证编译**

Run: `cargo check -p common`
Expected: 编译通过，无错误

- [x] **Step 4: 提交**

```bash
git add common/src/config.rs
git commit -m "feat(a2a): 新增 A2aServerConfig 配置段"
```

---

## Task 3: （已合并到 Task 0.1）

原 Task 3「HR Domain 新增 `find_reception_agent` 方法」的内容已被 Task 0.1 的 `resolve_agent` 方法完全涵盖（`resolve_agent` 是更通用的统一路由方法，**只接受 ctx，不耦合 project**）。

后续 Task 6/7/8 中的 A2A handler 路由调用，统一使用 `hr_domain().resolve_agent(ctx)` 而非 `find_reception_agent`。

---

## Task 4: A2A Handler 模块入口 + 映射层

**Files:**
- Modify: `src/handlers/mod.rs:1` (新增 `pub mod a2a;`)
- Create: `src/handlers/a2a/mod.rs`
- Create: `src/handlers/a2a/mapper.rs`
- Create: `src/handlers/a2a/mapper_test.rs`

- [x] **Step 1: 在 `src/handlers/mod.rs` 注册模块**

在 `pub mod user;` 后新增：

```rust
pub mod a2a;
```

- [x] **Step 2: 创建 `src/handlers/a2a/mod.rs`**

```rust
//! A2A Protocol Server Handler
//!
//! 对外暴露 A2A 协议（JSON-RPC 2.0）端点，外部 A2A Client 可通过协议调用前台 Agent。
//!
//! 模块结构：
//! - `agent_card` — Agent Card 发现端点（公开）
//! - `jsonrpc` — JSON-RPC 入口 + 方法分发（JWT 保护）
//! - `mapper` — A2A ↔ ai_orz 实体转换
//! - `send_task` — tasks/send 异步提交
//! - `get_task` — tasks/get 查询
//! - `cancel_task` — tasks/cancel 取消

pub mod agent_card;
pub mod cancel_task;
pub mod get_task;
pub mod jsonrpc;
pub mod mapper;
pub mod send_task;

#[cfg(test)]
mod mapper_test;
```

- [x] **Step 3: 创建 `src/handlers/a2a/mapper.rs`，实现转换函数**

```rust
//! A2A 协议实体 ↔ ai_orz 内部实体转换
//!
//! 所有转换逻辑集中在此模块，domain 层不感知 A2A 概念。

use common::api::a2a::{
    A2aArtifact, A2aMessage, A2aMessagePart, A2aTask, A2aTaskState, A2aTaskStatus,
};
use common::enums::ProjectStatus;
use chrono::Utc;

use crate::models::artifact::Artifact;
use crate::models::message::Message;

/// 将 ai_orz ProjectStatus 转为 A2A TaskState
pub fn project_status_to_a2a_state(status: ProjectStatus) -> A2aTaskState {
    match status {
        ProjectStatus::Active | ProjectStatus::PendingReview => A2aTaskState::Submitted,
        ProjectStatus::InProgress => A2aTaskState::Working,
        ProjectStatus::Completed => A2aTaskState::Completed,
        ProjectStatus::Archived => A2aTaskState::Canceled,
        ProjectStatus::Deleted => A2aTaskState::Failed,
    }
}

/// 将 ai_orz Message 转为 A2A Message
///
/// from_role=User → role="user"，其余 → role="agent"
pub fn message_to_a2a(msg: &Message, task_id: &str) -> A2aMessage {
    use common::enums::MessageRole;
    let role = if msg.from_role() == MessageRole::User {
        "user".to_string()
    } else {
        "agent".to_string()
    };

    A2aMessage {
        role,
        parts: vec![A2aMessagePart::Text {
            text: msg.content().to_string(),
        }],
        message_id: Some(msg.po.id.clone()),
        task_id: Some(task_id.to_string()),
    }
}

/// 将 ai_orz Artifact 转为 A2A Artifact
pub fn artifact_to_a2a(artifact: &Artifact) -> A2aArtifact {
    A2aArtifact {
        artifact_id: artifact.po.id.clone(),
        name: artifact.po.name.clone(),
        parts: vec![A2aMessagePart::Text {
            text: artifact.po.description.clone(),
        }],
    }
}

/// 构建 A2aTask
pub fn build_a2a_task(
    task_id: &str,
    project_status: ProjectStatus,
    messages: &[Message],
    artifacts: &[Artifact],
    session_id: Option<String>,
) -> A2aTask {
    let state = project_status_to_a2a_state(project_status);
    let a2a_messages: Vec<A2aMessage> = messages
        .iter()
        .map(|m| message_to_a2a(m, task_id))
        .collect();
    let a2a_artifacts: Vec<A2aArtifact> = artifacts
        .iter()
        .map(artifact_to_a2a)
        .collect();

    A2aTask {
        id: task_id.to_string(),
        session_id,
        status: A2aTaskStatus {
            state,
            timestamp: Utc::now().to_rfc3339(),
            message: None,
        },
        messages: a2a_messages,
        artifacts: a2a_artifacts,
        metadata: serde_json::Value::Null,
    }
}

/// 从 A2A Message 提取文本内容
///
/// 拼接所有 Text part，忽略 File/Data part
pub fn extract_text_from_a2a_message(msg: &common::api::a2a::A2aMessage) -> String {
    msg.parts
        .iter()
        .filter_map(|part| match part {
            A2aMessagePart::Text { text } => Some(text.as_str()),
            _ => None,
        })
        .collect::<Vec<_>>()
        .join("\n")
}
```

- [x] **Step 4: 创建 `src/handlers/a2a/mapper_test.rs`，测试转换函数**

```rust
//! mapper 单元测试

use super::mapper::*;
use common::api::a2a::{A2aMessage, A2aMessagePart, A2aTaskState};
use common::enums::{MessageRole, MessageType, ProjectStatus};
use crate::models::file::FileMeta;
use crate::models::message::Message;

#[test]
fn project_status_active_maps_to_submitted() {
    let state = project_status_to_a2a_state(ProjectStatus::Active);
    assert!(matches!(state, A2aTaskState::Submitted));
}

#[test]
fn project_status_in_progress_maps_to_working() {
    let state = project_status_to_a2a_state(ProjectStatus::InProgress);
    assert!(matches!(state, A2aTaskState::Working));
}

#[test]
fn project_status_completed_maps_to_completed() {
    let state = project_status_to_a2a_state(ProjectStatus::Completed);
    assert!(matches!(state, A2aTaskState::Completed));
}

#[test]
fn project_status_archived_maps_to_canceled() {
    let state = project_status_to_a2a_state(ProjectStatus::Archived);
    assert!(matches!(state, A2aTaskState::Canceled));
}

#[test]
fn extract_text_from_single_text_part() {
    let msg = A2aMessage {
        role: "user".to_string(),
        parts: vec![A2aMessagePart::Text {
            text: "你好".to_string(),
        }],
        message_id: None,
        task_id: None,
    };
    assert_eq!(extract_text_from_a2a_message(&msg), "你好");
}

#[test]
fn extract_text_from_multiple_text_parts() {
    let msg = A2aMessage {
        role: "user".to_string(),
        parts: vec![
            A2aMessagePart::Text { text: "第一行".to_string() },
            A2aMessagePart::Text { text: "第二行".to_string() },
        ],
        message_id: None,
        task_id: None,
    };
    assert_eq!(extract_text_from_a2a_message(&msg), "第一行\n第二行");
}

#[test]
fn extract_text_ignores_non_text_parts() {
    let msg = A2aMessage {
        role: "user".to_string(),
        parts: vec![
            A2aMessagePart::Text { text: "文本".to_string() },
            A2aMessagePart::Data { data: serde_json::json!({"key": "value"}) },
        ],
        message_id: None,
        task_id: None,
    };
    assert_eq!(extract_text_from_a2a_message(&msg), "文本");
}

/// 构造测试用 Message（使用 new_with_context 构造函数）
fn make_test_message(from_role: MessageRole, content: &str) -> Message {
    Message::new_with_context(
        format!("msg-{}", uuid::Uuid::now_v7()),
        None,                              // project_id
        None,                              // task_id
        "test-user".to_string(),           // from_id
        "test-agent".to_string(),          // to_id
        from_role,                         // from_role
        MessageRole::Agent,                // to_role
        MessageType::Text,                 // message_type
        content.to_string(),               // content
        None,                              // file_type
        FileMeta::default(),               // file_meta
        None,                              // reply_to_id
        None,                              // root_id
        None,                              // organization_id
        "test-user".to_string(),           // created_by
    )
}

#[test]
fn user_message_maps_to_user_role() {
    let msg = make_test_message(MessageRole::User, "用户消息");
    let a2a_msg = message_to_a2a(&msg, "task-1");
    assert_eq!(a2a_msg.role, "user");
    assert_eq!(a2a_msg.task_id, Some("task-1".to_string()));
}

#[test]
fn agent_message_maps_to_agent_role() {
    let msg = make_test_message(MessageRole::Agent, "Agent 回复");
    let a2a_msg = message_to_a2a(&msg, "task-1");
    assert_eq!(a2a_msg.role, "agent");
}

#[test]
fn build_a2a_task_assembles_correctly() {
    let messages = vec![
        make_test_message(MessageRole::User, "问题"),
        make_test_message(MessageRole::Agent, "回答"),
    ];
    let task = build_a2a_task(
        "project-1",
        ProjectStatus::Completed,
        &messages,
        &[],
        Some("session-1".to_string()),
    );
    assert_eq!(task.id, "project-1");
    assert_eq!(task.session_id, Some("session-1".to_string()));
    assert!(matches!(task.status.state, A2aTaskState::Completed));
    assert_eq!(task.messages.len(), 2);
    assert_eq!(task.messages[0].role, "user");
    assert_eq!(task.messages[1].role, "agent");
}
```

- [x] **Step 5: 验证编译和测试**

Run: `cargo test --lib handlers::a2a::mapper_test`
Expected: 10 tests PASS

- [x] **Step 6: 提交**

```bash
git add src/handlers/mod.rs src/handlers/a2a/mod.rs src/handlers/a2a/mapper.rs src/handlers/a2a/mapper_test.rs
git commit -m "feat(a2a): 新增 handler 模块入口和实体映射层"
```

---

## Task 5: Agent Card 端点

**Files:**
- Create: `src/handlers/a2a/agent_card.rs`

- [x] **Step 1: 创建 `src/handlers/a2a/agent_card.rs`**

```rust
//! Agent Card 端点
//!
//! GET /.well-known/agent.json
//! 公开路由，无需认证。返回组织级能力描述。

use axum::response::Json;
use common::api::a2a::{AgentCapabilities, AgentCard, AgentSkill};

/// Agent Card handler
///
/// 返回组织对外能力描述。对外只暴露一个统一入口，不列具体内部 Agent。
/// 配置通过全局单例 `crate::config::get()` 读取。
pub async fn get_agent_card() -> Json<AgentCard> {
    let config = crate::config::get();

    let card = AgentCard {
        name: "ai_orz 组织".to_string(),
        description: Some("ai_orz 组织对外能力入口".to_string()),
        version: config.a2a_server.protocol_version.clone(),
        url: config.a2a_server.endpoint.clone(),
        capabilities: AgentCapabilities {
            streaming: false,
            push_notifications: false,
        },
        skills: vec![AgentSkill {
            id: "chat".to_string(),
            name: "对话协作".to_string(),
            description: Some("与组织前台 Agent 对话".to_string()),
            tags: vec!["chat".to_string()],
        }],
        default_input_modes: vec!["text".to_string()],
        default_output_modes: vec!["text".to_string()],
    };

    Json(card)
}
```

- [x] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 3: 提交**

```bash
git add src/handlers/a2a/agent_card.rs
git commit -m "feat(a2a): 实现 Agent Card 发现端点"
```

---

## Task 6: tasks/send 异步提交

**Files:**
- Create: `src/handlers/a2a/send_task.rs`

- [x] **Step 1: 创建 `src/handlers/a2a/send_task.rs`**

```rust
//! tasks/send — 异步提交任务
//!
//! 异步流程（与飞书/前端聊天的唤醒链路完全一致）：
//! 1. JWT 提取 user_id
//! 2. 调用 resolve_agent(ctx) 获取前台 Agent（agent 与 project 是两个维度，不耦合）
//! 3. 创建 project（对应 A2A task），将 agent.id 作为 owner_agent_id 绑定
//! 4. 创建 message（from=user, to=agent）→ 自动入队 event_queue
//! 5. 立即返回 working 状态的 A2aTask（不等待 Agent 回复）
//!
//! 唤醒由 consumer 异步完成（复用现有链路）：
//! - consumer worker dequeue → handle_agent_message
//! - 内部自动 wake_agent_brain（幂等）+ awaken
//! - Agent 回复 message → 客户端通过 tasks/get 轮询获取结果
//!
//! 关键：handler 层显式组合 agent 与 project 两个维度（先拿 agent，再创建 project 绑定），
//! 不在 domain 层混合。resolve_agent 只接受 ctx，不感知 project。
//! handler 层不调用 wake_agent_brain / awaken，唤醒由 consumer 异步闭环。

use common::api::a2a::{A2aTask, SendTaskParams};
use common::error::{bail_err, Result};
use common::enums::ProjectStatus;

use crate::handlers::a2a::mapper::{build_a2a_task, extract_text_from_a2a_message};
use crate::pkg::RequestContext;
use crate::service::domain::hr::domain as hr_domain;
use crate::service::domain::message::{self, SendToAgentCommand};
use crate::service::domain::project::domain as project_domain;

/// 处理 tasks/send 请求（异步提交）
pub async fn handle_send_task(
    ctx: RequestContext,
    params: SendTaskParams,
) -> Result<A2aTask> {
    let user_id = ctx.uid();
    if user_id.is_empty() {
        bail_err!(InvalidRequest, "A2A 请求缺少用户上下文");
    }

    // 1. handler 层显式查询前台 Agent（agent 与 project 是两个维度，不耦合）
    let agent = hr_domain()
        .resolve_agent(ctx.clone())
        .await?
        .ok_or_else(|| common::error::Error::not_found("无可用前台 Agent"))?;
    let agent_id = agent.po.id.clone();

    // 2. 创建 project（对应 A2A task），将 agent.id 作为 owner_agent_id 绑定
    let content_text = extract_text_from_a2a_message(&params.message);
    // 用 char_indices 做字符级截断，避免 UTF-8 字节切片 panic
    let project_name = match content_text.char_indices().nth(50) {
        Some((idx, _)) => format!("A2A: {}...", &content_text[..idx]),
        None => format!("A2A: {}", content_text),
    };

    let project = project_domain()
        .project_manage()
        .create(
            ctx.clone(),
            project_name,
            format!("A2A 协议任务（session: {:?}）", params.session_id),
            0, // 默认优先级
            vec!["a2a".to_string()],
            Some(agent_id.clone()),  // ← handler 层已查询 agent，直接绑定
            user_id.clone(),
            user_id.clone(),
        )
        .await?;

    let project_id = project.po.id.clone();

    // 3. 启动项目（流转到 InProgress）
    project_domain()
        .project_manage()
        .start(ctx.clone(), &project_id, user_id.clone())
        .await?;

    // 4. 创建 message（from=user, to=agent）→ 自动入队 event_queue
    //    consumer 异步消费 → wake_agent_brain（幂等）+ awaken → Agent 回复
    let cmd = SendToAgentCommand {
        from_id: &user_id,
        from_role: common::enums::MessageRole::User,
        to_agent_id: &agent_id,
        content: &content_text,
        project_id: Some(&project_id),
        task_id: None,
        reply_to_id: None,
        attachment_ids: None,
    };
    let _message = message::domain()
        .delivery()
        .send_to_agent(ctx.clone(), cmd)
        .await?;

    // 5. 立即返回 working 状态的 A2aTask（不等待 Agent 回复）
    //    客户端通过 tasks/get 轮询，直到状态变为 completed
    let task = build_a2a_task(
        &project_id,
        ProjectStatus::InProgress,  // working 状态
        &[],  // 暂无消息（Agent 回复尚未产生）
        &[],
        params.session_id,
    );

    Ok(task)
}
```

**关键说明：**
- `hr_domain()` / `project_domain()` / `message::domain()` 都是各自 domain 模块的 `domain()` 单例获取函数
- **handler 层不调用 `wake_agent_brain` / `awaken`** — 唤醒由 consumer 异步闭环
- **不再需要 `runtime_domain` 导入** — handler 层不感知 runtime
- `agent` 不再需要声明为 `mut`（不调用 `wake_agent_brain`）
- `bail_err!(InvalidRequest, ...)` 是现有错误处理宏
- **handler 层显式组合两个维度**：先调 `resolve_agent(ctx)` 拿 agent，再用 `agent.id` 作为 `owner_agent_id` 创建 project。agent 与 project 在 handler 层组合，不在 domain 层混合
- **resolve_agent 只接受 ctx**：不感知 project，符合「agent 与 project 是两个维度」原则
- **异步流程与飞书/前端聊天完全一致**：send_to_agent 创建 message 入队 → consumer 异步 dequeue → handle_agent_message（内部自动 wake_agent_brain + awaken）→ Agent 回复
- **客户端轮询**：tasks/get 返回 project 当前状态（InProgress=working → Completed=completed），客户端轮询直到 completed
- **Task 0.1 必须先完成**：本 Task 依赖 Task 0.1 的 `resolve_agent` 方法

- [x] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过（可能需要根据实际 API 调整导入路径）

- [x] **Step 3: 提交**

```bash
git add src/handlers/a2a/send_task.rs
git commit -m "feat(a2a): 实现 tasks/send 异步提交"
```

---

## Task 7: tasks/get 查询任务

**Files:**
- Create: `src/handlers/a2a/get_task.rs`

- [x] **Step 1: 创建 `src/handlers/a2a/get_task.rs`**

```rust
//! tasks/get — 查询任务状态
//!
//! 流程：
//! 1. 根据 task_id（= project_id）查询 project
//! 2. 查询关联 messages
//! 3. 查询关联 artifacts
//! 4. 转为 A2aTask 返回

use common::api::a2a::{A2aTask, GetTaskParams};
use common::error::Result;

use crate::handlers::a2a::mapper::build_a2a_task;
use crate::pkg::RequestContext;
use crate::service::domain::message;
use crate::service::domain::project::domain as project_domain;

/// 处理 tasks/get 请求
pub async fn handle_get_task(
    ctx: RequestContext,
    params: GetTaskParams,
) -> Result<A2aTask> {
    // 1. 查询 project
    let project = project_domain()
        .project_manage()
        .get(ctx.clone(), &params.id)
        .await?
        .ok_or_else(|| common::error::Error::not_found(format!("Task {} not found", params.id)))?;

    // 2. 查询 messages
    let messages = message::domain()
        .management()
        .list_by_project_id(ctx.clone(), &params.id)
        .await?;

    // 3. 查询 artifacts
    let artifacts = project_domain()
        .artifact_manage()
        .list_by_project(ctx, &params.id)
        .await?;

    // 4. 转为 A2aTask
    let task = build_a2a_task(
        &params.id,
        project.po.status,
        &messages,
        &artifacts,
        None, // session_id 不持久化，get 时不返回
    );

    Ok(task)
}
```

- [x] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 3: 提交**

```bash
git add src/handlers/a2a/get_task.rs
git commit -m "feat(a2a): 实现 tasks/get 查询"
```

---

## Task 8: tasks/cancel 取消任务

**Files:**
- Create: `src/handlers/a2a/cancel_task.rs`

- [x] **Step 1: 创建 `src/handlers/a2a/cancel_task.rs`**

```rust
//! tasks/cancel — 取消任务
//!
//! 流程：
//! 1. 查询 project
//! 2. 归档 project（对应 A2A canceled 状态）
//! 3. 查询 messages + artifacts
//! 4. 转为 A2aTask 返回

use common::api::a2a::{A2aTask, CancelTaskParams};
use common::error::Result;

use crate::handlers::a2a::mapper::build_a2a_task;
use crate::pkg::RequestContext;
use crate::service::domain::message;
use crate::service::domain::project::domain as project_domain;

/// 处理 tasks/cancel 请求
pub async fn handle_cancel_task(
    ctx: RequestContext,
    params: CancelTaskParams,
) -> Result<A2aTask> {
    // 1. 查询 project
    let project = project_domain()
        .project_manage()
        .get(ctx.clone(), &params.id)
        .await?
        .ok_or_else(|| common::error::Error::not_found(format!("Task {} not found", params.id)))?;

    // 2. 归档 project（对应 A2A canceled）
    let user_id = ctx.uid();
    project_domain()
        .project_manage()
        .archive(ctx.clone(), &params.id, user_id)
        .await?;

    // 3. 重新查询 project 获取最新状态
    let project = project_domain()
        .project_manage()
        .get(ctx.clone(), &params.id)
        .await?
        .ok_or_else(|| common::error::Error::not_found(format!("Task {} not found", params.id)))?;

    // 4. 查询 messages + artifacts
    let messages = message::domain()
        .management()
        .list_by_project_id(ctx.clone(), &params.id)
        .await?;
    let artifacts = project_domain()
        .artifact_manage()
        .list_by_project(ctx, &params.id)
        .await?;

    // 5. 转为 A2aTask
    let task = build_a2a_task(
        &params.id,
        project.po.status,
        &messages,
        &artifacts,
        None,
    );

    Ok(task)
}
```

- [x] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 3: 提交**

```bash
git add src/handlers/a2a/cancel_task.rs
git commit -m "feat(a2a): 实现 tasks/cancel 取消"
```

---

## Task 9: JSON-RPC 入口和方法分发

**Files:**
- Create: `src/handlers/a2a/jsonrpc.rs`

- [x] **Step 1: 创建 `src/handlers/a2a/jsonrpc.rs`**

```rust
//! JSON-RPC 2.0 入口
//!
//! POST /a2a
//! 挂 JWT 中间件，解析 JSON-RPC 请求并按 method 分发。
//!
//! RequestContext 通过 request_context_middleware 注入到 Extension，
//! handler 内用 `Extension(ctx): Extension<RequestContext>` 提取。
//! 配置通过全局单例 `crate::config::get()` 读取。

use axum::Extension;
use axum::response::Json;
use common::api::a2a::{
    error_codes, A2aTask, CancelTaskParams, GetTaskParams, JsonRpcRequest, JsonRpcResponse,
    SendTaskParams,
};
use serde_json::Value;

use crate::handlers::a2a::{cancel_task, get_task, send_task};
use crate::pkg::RequestContext;

/// JSON-RPC 入口 handler
pub async fn handle_jsonrpc(
    Extension(ctx): Extension<RequestContext>,
    Json(req): Json<JsonRpcRequest>,
) -> Json<JsonRpcResponse> {
    let config = crate::config::get();

    // 检查 A2A Server 是否启用
    if !config.a2a_server.enabled {
        return Json(JsonRpcResponse::error(
            req.id,
            error_codes::METHOD_NOT_FOUND,
            "A2A Server 未启用".to_string(),
        ));
    }

    // 验证 jsonrpc 版本
    if req.jsonrpc != "2.0" {
        return Json(JsonRpcResponse::error(
            req.id,
            error_codes::INVALID_REQUEST,
            "Unsupported JSON-RPC version".to_string(),
        ));
    }

    // 按方法分发
    let result = match req.method.as_str() {
        "tasks/send" => dispatch_send(ctx, req.params).await,
        "tasks/get" => dispatch_get(ctx, req.params).await,
        "tasks/cancel" => dispatch_cancel(ctx, req.params).await,
        _ => {
            return Json(JsonRpcResponse::error(
                req.id,
                error_codes::METHOD_NOT_FOUND,
                format!("Method not found: {}", req.method),
            ));
        }
    };

    match result {
        Ok(task) => Json(JsonRpcResponse::success(
            req.id,
            serde_json::to_value(&task).unwrap_or(Value::Null),
        )),
        Err(e) => {
            // Error 实现了 Display，输出形如 "[error_code] msg"
            let message = format!("{}", e);
            Json(JsonRpcResponse::error(
                req.id,
                error_codes::INTERNAL_ERROR,
                message,
            ))
        }
    }
}

async fn dispatch_send(
    ctx: RequestContext,
    params: Value,
) -> common::error::Result<A2aTask> {
    let params: SendTaskParams = serde_json::from_value(params)
        .map_err(|e| common::error::Error::bad_request(format!("Invalid params: {}", e)))?;
    send_task::handle_send_task(ctx, params).await
}

async fn dispatch_get(
    ctx: RequestContext,
    params: Value,
) -> common::error::Result<A2aTask> {
    let params: GetTaskParams = serde_json::from_value(params)
        .map_err(|e| common::error::Error::bad_request(format!("Invalid params: {}", e)))?;
    get_task::handle_get_task(ctx, params).await
}

async fn dispatch_cancel(
    ctx: RequestContext,
    params: Value,
) -> common::error::Result<A2aTask> {
    let params: CancelTaskParams = serde_json::from_value(params)
        .map_err(|e| common::error::Error::bad_request(format!("Invalid params: {}", e)))?;
    cancel_task::handle_cancel_task(ctx, params).await
}
```

**关键说明：**
- handler 用 `Extension(ctx): Extension<RequestContext>` 提取上下文（与 [subscribe_sse.rs](../../src/handlers/finance/message/subscribe_sse.rs#L17) 模式一致）
- 配置通过 `crate::config::get()` 全局单例读取，无需 `State` 提取器
- 参数解析错误用 `common::error::Error::bad_request()`（不是 `invalid_request`）

- [x] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 3: 提交**

```bash
git add src/handlers/a2a/jsonrpc.rs
git commit -m "feat(a2a): 实现 JSON-RPC 入口和方法分发"
```

---

## Task 10: 路由注册

**Files:**
- Modify: `src/router.rs:12` (在 `create_router` 函数中新增 A2A 路由)

**关键约束：**
- `/.well-known/agent.json`：公开路由，只挂 `request_context_middleware`（不挂 JWT）
- `/a2a`：JWT 保护，挂 `jwt_auth_middleware` + `request_context_middleware`（与 protected_routes 一致）
- 配置通过全局单例 `crate::config::get()` 读取，不需要 `State`

- [x] **Step 1: 在 `src/router.rs` 的 `create_router` 函数新增 A2A 路由**

在 `.route("/health", ...)` 之前新增：

```rust
        // A2A Protocol routes
        // Agent Card: 公开发现端点（无需 JWT，只需 RequestContext）
        .route(
            "/.well-known/agent.json",
            get(handlers::a2a::agent_card::get_agent_card)
                .layer(axum::middleware::from_fn(move |req, next| {
                    request_context_middleware(config.clone(), req, next)
                })),
        )
        // JSON-RPC: JWT 保护端点
        .route(
            "/a2a",
            post(handlers::a2a::jsonrpc::handle_jsonrpc)
                .layer(axum::middleware::from_fn(jwt_auth_middleware))
                .layer(axum::middleware::from_fn(move |req, next| {
                    request_context_middleware(config.clone(), req, next)
                })),
        )
```

- [x] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过

- [x] **Step 3: 启动服务，手动测试 Agent Card**

在 `ai_orz.toml` 中设置：

```toml
[a2a_server]
enabled = true
```

Run: `cargo run` 然后在另一个终端：

```bash
curl http://localhost:8080/.well-known/agent.json | jq
```

Expected: 返回 AgentCard JSON，包含 `name`、`version`、`capabilities` 等字段。

- [x] **Step 4: 手动测试 JSON-RPC（无 token 返回 401）**

```bash
curl -X POST http://localhost:8080/a2a \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tasks/send","params":{"id":"t1","message":{"role":"user","parts":[{"type":"text","text":"你好"}]}}}'
```

Expected: 返回 401 未认证。

- [x] **Step 5: 提交**

```bash
git add src/router.rs
git commit -m "feat(a2a): 注册 A2A 路由（Agent Card 公开 + JSON-RPC JWT 保护）"
```

---

## Task 11: 端到端集成测试

**Files:**
- Create: `src/handlers/a2a/integration_test.rs`
- Modify: `src/handlers/a2a/mod.rs` (注册测试模块)

- [x] **Step 1: 在 `src/handlers/a2a/mod.rs` 新增测试模块**

在文件末尾新增：

```rust
#[cfg(test)]
mod integration_test;
```

- [x] **Step 2: 创建 `src/handlers/a2a/integration_test.rs`**

```rust
//! A2A Server 集成测试
//!
//! 测试完整流程：tasks/send → tasks/get → tasks/cancel

use crate::pkg::request_context_test_support::new_test_ctx;
use common::api::a2a::*;
use sqlx::SqlitePool;

/// 初始化测试环境
async fn init_a2a_test_env(pool: SqlitePool) -> crate::pkg::RequestContext {
    let _ = crate::config::init();

    // 初始化所有 DAO
    crate::service::dao::agent::init();
    crate::service::dao::tool::init();
    crate::service::dao::skill::init();
    crate::service::dao::tool_call::init();
    crate::service::dao::model_provider::init();
    crate::service::dao::cortex::init();
    crate::service::dao::memory::init();
    crate::service::dao::mcp_server::init();
    crate::service::dao::project::init();
    crate::service::dao::message::init();
    crate::service::dao::artifact::init();

    // 初始化所有 DAL
    crate::service::dal::agent::init();
    crate::service::dal::tool::init();
    crate::service::dal::skill::init();
    crate::service::dal::model_provider::init();
    crate::service::dal::memory::init();
    crate::service::dal::mcp_tool::init();
    crate::service::dal::brain::init();
    crate::service::dal::project::init();
    crate::service::dal::message::init();
    crate::service::dal::message_channel::init();
    crate::service::dal::attachment::init();
    crate::service::dal::artifact::init();

    // 初始化所有 Domain
    crate::service::domain::hr::init();
    crate::service::domain::message::init();
    crate::service::domain::project::init();
    crate::service::domain::runtime::init();

    new_test_ctx("test-a2a-user", pool)
}

/// 创建测试用前台 Agent
async fn create_test_reception_agent(ctx: &crate::pkg::RequestContext) -> String {
    use crate::models::agent::{Agent, AgentPo};
    use common::enums::AgentStatus;

    let mut po = AgentPo::new(
        "前台 Agent".to_string(),
        vec!["feishu_reception".to_string()],
        "前台接待测试 Agent".to_string(),
        vec!["chat".to_string()],
        "测试灵魂".to_string(),
        "provider-001".to_string(),
        "test-a2a-user".to_string(),
    );
    po.id = format!("reception-{}", uuid::Uuid::now_v7());
    po.status = AgentStatus::Onboarded;

    let agent = Agent::from_po(po);
    crate::service::dal::agent::dal()
        .create(ctx.clone(), &agent)
        .await
        .expect("创建测试 Agent 失败");

    agent.po.id
}

#[sqlx::test]
async fn test_find_reception_agent_returns_onboarded_agent(pool: SqlitePool) {
    let ctx = init_a2a_test_env(pool).await;
    let agent_id = create_test_reception_agent(&ctx).await;

    let found = crate::service::domain::hr::domain()
        .find_reception_agent(ctx)
        .await
        .expect("查找前台 Agent 应该成功");

    assert!(found.is_some(), "应该找到前台 Agent");
    assert_eq!(found.unwrap().po.id, agent_id);
}

#[sqlx::test]
async fn test_tasks_get_returns_not_found_for_nonexistent(pool: SqlitePool) {
    let ctx = init_a2a_test_env(pool).await;

    let result = crate::handlers::a2a::get_task::handle_get_task(
        ctx,
        GetTaskParams {
            id: "nonexistent".to_string(),
            history_length: None,
        },
    )
    .await;

    assert!(result.is_err(), "查询不存在的 task 应该返回错误");
}

#[sqlx::test]
async fn test_tasks_cancel_nonexistent_returns_error(pool: SqlitePool) {
    let ctx = init_a2a_test_env(pool).await;

    let result = crate::handlers::a2a::cancel_task::handle_cancel_task(
        ctx,
        CancelTaskParams {
            id: "nonexistent".to_string(),
        },
    )
    .await;

    assert!(result.is_err(), "取消不存在的 task 应该返回错误");
}
```

- [x] **Step 3: 运行集成测试**

Run: `cargo test --lib handlers::a2a::integration_test`
Expected: 3 tests PASS

- [x] **Step 4: 运行全量测试确保无回归**

Run: `cargo test --lib`
Expected: 所有测试 PASS

- [x] **Step 5: 提交**

```bash
git add src/handlers/a2a/integration_test.rs src/handlers/a2a/mod.rs
git commit -m "test(a2a): 新增 A2A Server 集成测试"
```

---

## Task 12: 文档更新和最终验证

**Files:**
- Modify: `docs/a2a_server_design.md` (更新实现状态)
- Modify: `README.md` (核心能力列表新增 A2A Server)

- [x] **Step 1: 在 `docs/a2a_server_design.md` 顶部新增实现状态**

在 "# A2A Server 设计文档" 标题后新增：

```markdown
## 🚀 实现完成状态（2026-07-19）

### P0 已完成

| 模块 | 状态 | 文件 |
|------|------|------|
| 协议实体定义 | ✅ | `common/src/api/a2a.rs` |
| 配置项 | ✅ | `common/src/config.rs` |
| find_reception_agent 提取 | ✅ | `src/service/domain/hr/` |
| 映射层 | ✅ | `src/handlers/a2a/mapper.rs` |
| Agent Card 端点 | ✅ | `src/handlers/a2a/agent_card.rs` |
| tasks/send 异步提交 | ✅ | `src/handlers/a2a/send_task.rs` |
| tasks/get 查询 | ✅ | `src/handlers/a2a/get_task.rs` |
| tasks/cancel 取消 | ✅ | `src/handlers/a2a/cancel_task.rs` |
| JSON-RPC 分发 | ✅ | `src/handlers/a2a/jsonrpc.rs` |
| 路由注册 | ✅ | `src/router.rs` |
| 集成测试 | ✅ | `src/handlers/a2a/integration_test.rs` |

### P1 待实现

| 模块 | 状态 |
|------|------|
| tasks/sendSubscribe SSE 流式 | ⏳ |
| 长任务异步模式 | ⏳ |
```

- [x] **Step 2: 在 `README.md` 核心能力列表更新 A2A 条目**

将「🔌 外部 Agent 接入」条目更新为：

```markdown
- 🔌 **A2A 协议双向支持**：作为 Client 可注册外部 Agent（CLI/Remote），作为 Server 可对外暴露 A2A 协议端点供外部调用
```

- [x] **Step 3: 最终全量编译验证**

Run: `cargo check && cargo test --lib`
Expected: 全部通过

- [x] **Step 4: 提交并推送**

```bash
git add docs/a2a_server_design.md README.md
git commit -m "docs(a2a): 更新 A2A Server 实现状态和 README"
git push origin main
```

---

## Self-Review

### Spec 覆盖检查

| 设计文档要求 | 对应 Task | 状态 |
|------------|----------|------|
| 统一 Agent 路由（agent 与 project 维度分离，resolve_agent 只接受 ctx） | Task 0.1（resolve_agent(ctx) + HTTP API） | ✅ 新增 |
| 前台 Agent 查询 HTTP API（供前端显示推荐前台 Agent） | Task 0.1（GET /api/v1/hr/agents/reception） | ✅ 新增 |
| 修复 chat 缺陷：CreateProjectRequest 支持 owner_agent_id（handler 纯粹透传） | Task 0.2 | ✅ 新增 |
| 修复 chat 缺陷：send_message_to_agent 支持默认对话框（无 project）+ resolve_agent 兜底 | Task 0.3 | ✅ 新增 |
| 协议实体定义（common 层） | Task 1 | ✅ |
| 配置项 `[a2a_server]` | Task 2 | ✅ |
| 复用 JWT 不加表 | Task 10（路由挂 JWT 中间件） | ✅ |
| 前台 Agent 路由复用 | Task 0.1（resolve_agent 统一方法） | ✅ 合并原 Task 3 |
| Handler 层实体转换 | Task 4（mapper.rs） | ✅ |
| Agent Card 公开端点 | Task 5 | ✅ |
| tasks/send 异步提交（handler 层显式组合 agent + project 两个维度，唤醒由 consumer 异步闭环） | Task 6 | ✅（先 resolve_agent(ctx) 再 create project 绑定，不调 wake/awaken） |
| tasks/get 查询 | Task 7 | ✅ |
| tasks/cancel 取消 | Task 8 | ✅ |
| JSON-RPC 分发 | Task 9 | ✅ |
| 路由注册（公开 + JWT 保护） | Task 10 | ✅ |
| 集成测试 | Task 11 | ✅ |
| 文档更新 | Task 12 | ✅ |
| Domain 层零侵入 | 全部 Task | ✅（只在 hr/mod.rs 新增 resolve_agent(ctx) 方法，不耦合 project） |

### 计划结构调整记录（2026-07-19）

为修复 chat 页面无法发消息的缺陷 + 收敛 Agent 路由逻辑，对计划做了如下结构调整：

#### 第一轮调整（路由收敛）

1. **新增 Task 0.1**：在 `HrDomain` trait 新增统一 `resolve_agent(ctx, project_id)` 方法，三级优先级（project.owner_agent_id → feishu_reception 角色 Onboarded Agent → 任意 Onboarded Agent），供 Chat 页面、A2A、飞书 IM 三场景共用
2. **新增 Task 0.2**：修复 `CreateProjectRequest` 无 `owner_agent_id` 字段的缺陷，`ProjectManage::create` 新增 `owner_agent_id: Option<String>` 参数，None 时由内部自动调用 `resolve_agent(ctx, None)` 绑定
3. **新增 Task 0.3**：修复 `send_message_to_agent` handler 无 fallback 路由的缺陷，`SendMessageToAgentParams.to_agent_id` 改为 `Option<String>`，None 时调用 `resolve_agent(ctx, project_id)` 兜底
4. **删除原 Task 3**：原 Task 3「HR Domain 新增 `find_reception_agent` 方法」已被 Task 0.1 的 `resolve_agent` 完全涵盖（resolve_agent 是 find_reception_agent 的超集）
5. **调整 Task 6**：`tasks/send` 不再独立调用 `find_reception_agent`，改为依赖 Task 0.2 自动路由绑定 + `resolve_agent(ctx, Some(project_id))` 取回

#### 第二轮调整（handler 层负责路由，不在 domain 层内部查 project）

基于「Context 补充只用当前业务已有信息，不为此专门查询」原则，进一步调整：

6. **Task 0.1 签名调整**：`resolve_agent` 接受 `Option<&Project>` 实体而非 `project_id`，**不在 domain 层内部查询 project**。domain 层只负责兜底路由（feishu_reception → 任意 Onboarded），`project` 参数以 `_project` 命名预留扩展
7. **Task 0.2 职责调整**：`ProjectDomainImpl::create` 只负责绑定 `owner_agent_id`，**不内部调用 resolve_agent**。**handler 层**（create_project.rs）在 `owner_agent_id` 为 None 时调用 `resolve_agent(ctx, None)` 获取 agent，将 `agent.id` 传给 `create`
8. **Task 0.3 逻辑调整**：send_message_to_agent handler **先查 project，若 `owner_agent_id` 存在直接用，不调用 `resolve_agent`**；否则调用 `resolve_agent(ctx, Some(&project))` 兜底。chat 首次发消息（无 project）时，**前端先调用 `create_project` API 创建默认 project**（handler 层自动路由绑定 agent），再调用 `send_message_to_agent`
9. **Task 6 流程调整**：`tasks/send` 在创建 project 前先 `resolve_agent(ctx, None)` 获取 agent，然后将 `agent.id` 作为 `owner_agent_id` 传给 `create`（一步到位，不再两步走）
10. **修改文件表更新**：新增修改文件 `common/src/api/project.rs`、`common/src/api/neural_tools.rs`、`src/handlers/project/project/create_project.rs`、`src/handlers/finance/message/send_message_to_agent.rs`、`frontend/src/pages/message/chat.rs`

#### 第三轮调整（agent 与 project 维度分离，handler 层不做兜底路由）

基于用户反馈「hr domain 提供的响应 agent 与 project 完全是两个维度的信息，不需要融合在一个方法中，完全可以由上层按需组合使用」+「chat 首次发消息选择前端显式两步」，进一步调整：

11. **Task 0.1 签名再次调整**：`resolve_agent` 改为 `resolve_agent(ctx) -> Result<Option<Agent>>`，**移除 project 参数**。agent 与 project 是两个维度，不在 hr domain 中融合。同时**新增 HTTP API `GET /api/v1/hr/agents/reception`**（JWT 保护）供前端显式查询可用前台 Agent
12. **Task 0.2 职责再次调整**：create_project handler **纯粹透传 `params.owner_agent_id`**，**不调 resolve_agent，不依赖 hr domain**（完全解耦）。路由由前端显式两步完成
13. **Task 0.3 逻辑再次调整**：send_message_to_agent handler **只从 `project.owner_agent_id` 取 to_agent_id**，若为 None 返回错误（提示前端先调 `GET /api/v1/hr/agents/reception` 查 agent 并绑定），**不调 resolve_agent**。chat 首次发消息由**前端显式两步**完成：先调 `GET /api/v1/hr/agents/reception` 拿 agent_id → 带 `owner_agent_id` 调 `create_project` → 调 `send_message_to_agent`
14. **Task 6 流程再次调整**：`tasks/send` handler 调 `resolve_agent(ctx)`（不传 project），用 `agent.id` 作为 `owner_agent_id` 创建 project。handler 层显式组合 agent 与 project 两个维度
15. **新增文件**：`src/handlers/hr/agent/get_reception_agent.rs`（HTTP API handler）
16. **修改文件表更新**：新增 `src/handlers/hr/agent/mod.rs`、`src/handlers/hr/agent/get_reception_agent.rs`、`frontend/src/api/mod.rs`（新增 `get_reception_agent_api` 函数）

#### 第四轮调整（协作关系类比：默认对话框 + Project 对话框）

基于用户反馈「不应该直接使用默认的 project 来管理流程」+ 协作关系类比（前台接待 → 简单需求直接处理 / 复杂需求创建 Project），进一步调整：

17. **Task 0.3 逻辑再次调整**：send_message_to_agent handler 支持**两种对话上下文**：
    - **默认对话框**（无 project_id）：`to_agent_id` 未指定时后端调 `resolve_agent(ctx)` 兜底
    - **Project 对话框**（有 project_id）：从 `project.owner_agent_id` 取；若为 None 则调 `resolve_agent(ctx)` 兜底
18. **不在前端创建默认 project**：Project 创建由 Agent 内部决策触发（识别复杂需求后创建），不在本次 A2A Server 范围
19. **to_agent_id 保持 Option**：用户选定 Agent 时前端显式传入；未指定时后端走 `resolve_agent(ctx)` 兜底
20. **handler 层调 resolve_agent 是合理的**：resolve_agent 本就是给 handler 用的统一路由方法，第三轮"不调 resolve_agent"的限制过于严格，第四轮恢复 handler 层调 resolve_agent 的能力
21. **get_reception_agent_api 用途调整**：不再用于"前端显式两步"流程，改为前端显示推荐前台 Agent（如默认对话框顶部显示"当前前台：XXX"）
22. **架构原则补充协作关系类比**：默认对话框=与前台直接沟通（无 project），Project 对话框=Project 上下文沟通

### 类型一致性检查

- `SendTaskParams` / `GetTaskParams` / `CancelTaskParams` 在 Task 1 定义，Task 6/7/8/9 使用 ✅
- `A2aTask` 在 Task 1 定义，Task 4/6/7/8/9 使用 ✅
- `build_a2a_task` 在 Task 4 定义，Task 6/7/8 使用 ✅
- `handle_send_task` / `handle_get_task` / `handle_cancel_task` 在 Task 6/7/8 定义，Task 9 引用 ✅
- `resolve_agent(ctx)` 在 Task 0.1 定义（`HrDomain` trait，只接受 ctx 不耦合 project），Task 0.3 handler 引用（默认对话框 + project 未绑定 agent 时兜底）、Task 6 handler 引用 ✅
- `CreateProjectRequest.owner_agent_id` 在 Task 0.2 新增，Task 6 send_task 传 `Some(agent_id)` ✅（Task 0.3 不再需要前端创建默认 project，Project 创建由 Agent 内部决策触发）
- `SendMessageToAgentParams.to_agent_id: Option<String>` 在 Task 0.3 修改，前端 chat.rs 同步（用户选定 Agent 时传入，否则后端走 resolve_agent 兜底）✅
- `get_reception_agent_api` 在 Task 0.3 前端新增，对应 Task 0.1 新增的 `GET /api/v1/hr/agents/reception` HTTP API ✅
- `Extension(ctx): Extension<RequestContext>` 提取模式（Task 9 / Task 0.1 HTTP API），与 [subscribe_sse.rs](../../src/handlers/finance/message/subscribe_sse.rs#L17) 一致 ✅
- `crate::config::get()` 全局配置单例（Task 5/9），与 [config.rs:14](../../src/config.rs#L14) 一致 ✅
- `common::error::Error::bad_request()` / `not_found()`（Task 9 / Task 0.3），与 [error/types.rs:151](../../common/src/error/types.rs#L151) 一致 ✅
- `runtime_domain().awakening()` 返回 `&dyn RuntimeAwakening`（Task 6），与 [runtime/mod.rs:40](../../src/service/domain/runtime/mod.rs#L40) 一致 ✅
- `Message::new_with_context()` 构造函数（Task 4 测试），与 [message.rs:130](../../src/models/message.rs#L130) 一致 ✅

### 架构边界检查

- Domain 层零侵入（A2A 相关）：A2A 协议实体 ↔ ai_orz 实体转换全部在 `mapper.rs`，domain 层不感知 A2A ✅
- 无新增表：认证走用户 JWT，路由走前台 Agent 逻辑，project/message/artifact 复用现有表 ✅
- Handler 层转换：A2A 协议实体 ↔ ai_orz 实体转换全部在 `mapper.rs`，domain 层不感知 A2A ✅
- Agent 与 Project 维度分离：`resolve_agent(ctx)` 只接受 ctx，不耦合 project；两个维度由 handler 层按需组合 ✅

### API 核对修复记录（2026-07-19 第二轮核对）

通过 25 项 API 签名核对 + 10 项潜在问题核对，修复以下 5 处问题：

1. **Task 1 行号修正**：`pub mod external_agent;` 实际在 line 96（非 85），re-exports 在 line 127（非 122）
2. **Task 3 冗余操作移除**：`FEISHU_RECEPTION_ROLE` 已是 `pub`（adapter.rs:25），删除原 Step 1「改为 pub」，后续 Step 重新编号
3. **Task 4 时间戳格式修正**：`utils::current_timestamp()` 返回 Unix 秒数（i64），与 A2A 协议注释的 ISO 8601 不符。改用 `chrono::Utc::now().to_rfc3339()` 生成 RFC 3339 字符串
4. **Task 6 UTF-8 切片 bug 修复**：`&content_text[..50]` 按字节切片对中文内容会 panic。改用 `char_indices().nth(50)` 做字符级安全截断
5. **Task 11 init 函数修正**：`crate::service::dal::message_push::init()` 不存在（该 DAL 用 `OnceLock::get_or_init` 惰性初始化），删除该行
6. **Task 9 错误消息优化**：`format!("{:?}", e)` 改为 `format!("{}", e)`，输出更友好的 `[error_code] msg` 格式

### 关键 API 签名核对（25 项通过）

| API | 文件:行 | 状态 |
|-----|---------|------|
| `HrDomain` trait | hr/mod.rs#L93 | ✅ 无 `#[async_trait]`，需新增 |
| `impl HrDomain for HrDomainImpl` | hr/mod.rs#L79 | ✅ |
| `FEISHU_RECEPTION_ROLE` | consumer/adapter.rs#L25 | ✅ 已是 `pub` |
| `find_reception_agent_id` | consumer/adapter.rs#L100 | ✅ |
| `AgentQuery` (roles/status/limit) | service/dao/agent/mod.rs#L13 | ✅ |
| `AgentManage::query` | hr/mod.rs#L114 | ✅ |
| `AppConfig`（无 a2a_server） | common/config.rs#L22 | ✅ lark 在 line 54 |
| `Artifact` (po.id/name/description) | models/artifact.rs#L53 | ✅ |
| `ProjectStatus` 6 个变体 | common/enums/project.rs#L13 | ✅ |
| `ProjectManage::create/start/complete/archive/get` | service/domain/project/mod.rs#L83-L143 | ✅ |
| `ProjectDomain::artifact_manage` + `list_by_project` | project/mod.rs:74,341 | ✅ |
| `SendToAgentCommand` 8 字段 | message/mod.rs#L104 | ✅ |
| `MessageDelivery::send_to_agent` | message/mod.rs#L251 | ✅ |
| `MessageManagement::list_by_project_id` | message/mod.rs#L340 | ✅ |
| `RuntimeAwakening::wake_agent_brain/awaken` | runtime/mod.rs:168,188 | ✅ |
| `bail_err!(InvalidRequest, ...)` | common/error/macros.rs#L154 | ✅ |
| `jwt_auth_middleware` | middleware/jwt_auth.rs#L36 | ✅ |
| `request_context_middleware` | middleware/request_context.rs#L20 | ✅ |
| `create_router(frontend_dist_dir, config)` | router.rs#L12 | ✅ |
| `new_test_ctx(user_id, pool)` | pkg/request_context_test_support.rs#L12 | ✅ |
| `AgentPo::new` 7 参数 | models/agent.rs#L351 | ✅ |
| `AgentDal::dal()` + `create` | service/dal/agent.rs:23,83 | ✅ |
| `RequestContext::uid() -> String` | pkg/request_context.rs#L363 | ✅ |
| `Message::new_with_context` 15 参数 | models/message.rs#L130 | ✅ |
| `Error::bad_request/not_found` | common/error/types.rs:151,171 | ✅ |

### P1 遗留

`tasks/sendSubscribe` SSE 流式和长任务异步模式留作 P1，设计文档已标注。

---

## 执行说明

本计划已完成设计评审，可进入实施阶段。

**推荐执行方式**：按 `tasks.md` 的任务依赖顺序，逐个 Task 执行，每个 Task 完成后单独提交并运行测试验证。

**关键里程碑**：
- Phase 0 完成（Task 0.1/0.2/0.3）：路由收敛 + chat 缺陷修复
- Phase 1 完成（Task 1/2/4/5）：A2A 基础设施就绪
- Phase 2 完成（Task 6/7/8/9）：A2A 核心方法实现
- Phase 3 完成（Task 10/11/12）：路由 + 测试 + 文档，交付可用
