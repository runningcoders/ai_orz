# 跨组织业务调用方案（Phase 2 主体）

> 状态：**P1–P4 全部完成**（设计稿 v3，2026-09-04；鉴权模型已拍板②，F1 接受）
> 前置：Phase 1 组网（S1–S7）✅、S8 目录推拉 ✅、S9 分层修正 ✅、R2/R3 JWT 地基 ✅

---

## 一、目标与非目标

**目标**：建联的组织之间可以发起跨组织业务调用——第一闭环是**跨组织 Agent 协作**（A 组织的 Agent/用户把任务委派给 B 组织的 Agent 执行），并在此基础上留出计量（哪个组织调了多少）的口子。

**非目标（YAGNI）**：
- 用户级联邦 SSO（A 的用户拿自己的 token 直接浏览 B 的界面）——重协议，先不做
- 计费结算——只留计量字段，结算逻辑延后
- 非对称签名 JWT + 公钥发现——凭证模型复杂度高，本期不做（见 §3 方案对比）

---

## 二、现状盘点（已有资产）

| 资产 | 现状 | 对 Phase 2 的意义 |
|------|------|------------------|
| A2A 出站客户端（`agent_runtime/a2a.rs`） | JSON-RPC `agents/sendTask`，支持 `A2aRuntimeConfig { endpoint, agent_name, auth_token(Bearer), timeout }` | 跨组织调 Agent 的传输层**已经存在**，只差鉴权与发现 |
| A2A 入站（`/a2a` jsonrpc、`/a2a/subscribe`） | root 直挂、**JWT 严格保护**（`jwt_auth_middleware`，无/错 token 一律 401；集成测试口径 "JWT protected"） | 缺口不是「无鉴权」而是「不认联邦凭证」——跨组织调用需双模鉴权（P1+P2 已实现） |
| A2A 入站回调（`/a2a/callback/{task_id}`） | root 直挂、公开（task_id 随机性 + 终态幂等隐式校验） | 跨组织场景下收紧选项见 F3 |
| 机器侧契约凭证鉴权（`authenticate_link_call`） | `peer_token_hash` 匹配 Active 连接，directory 端点在用 | 连接级鉴权直接复用 |
| 目录 + 推拉同步 | 对端组织元信息（不含 Agent 列表） | 缺"对端有哪些 Agent 可调"的能力发现 |
| R2/R3 地基 | Claims iss/aud、`caller_organization_id` 全链路 | 身份声明与计量的落点已铺好 |
| @ 提及协议（`agent:id`） | 组织内 Agent 寻址 | 跨组织寻址需扩展（见 §5） |

---

## 三、鉴权模型（核心决策点）

### 方案对比

| 方案 | 机制 | 优点 | 缺点 |
|------|------|------|------|
| ① token 交换 | A 凭 link access_token 调 B 的 token 端点，换 B 签发的短期 JWT（含 iss/aud/exp） | 短期凭证、标准联邦模式、R2 iss/aud 立即收紧 | 新端点 + token 生命周期管理 + 刷新逻辑；一期凭证量级下收益有限 |
| ② 凭证直传 + 身份声明（**推荐**） | A 直接用 link access_token 调 B（Bearer，连接级鉴权 = Phase 1 管道），另带 `X-Federation-Caller` 头声明发起者（org/user/agent，明文 JSON + 由 A 签名可后补） | 零新协议端点；B 侧校验 = `authenticate_link_call` + 解析声明；R3 管道直接落地 | 声明头无密码学保护（对端节点可信前提下发明文；R1 同源风险）；长期凭证（与 Phase 1 R1 同源接受） |
| ③ 非对称签名 + 公钥发现 | 每节点发布公钥（well-known），JWT RS256/EdDSA | 密码学上最正统 | 密钥生成/轮换/分发/信任锚全套复杂度，明显过度设计 |

**推荐 ②**，理由：
1. 与 Phase 1 的信任模型一致——建联本身（配对码 + 契约凭证）已经建立了"这对组织互相可信"，调用级再叠加 token 交换是重复建设
2. R2 的 iss/aud 地基不浪费：Phase 2 的联邦 JWT 场景（用户级 SSO）真出现时地基直接用；本期 `X-Federation-Caller` 声明是它的明文前身，字段语义对齐（caller_org/caller_user/agent）
3. ①③ 可以后补：鉴权中间件抽象成 trait，直传/交换/签名都是实现

### B 侧鉴权流程（方案 ②）

```
A 侧出站                                B 侧入站
─────────────                          ─────────────
1. 查 link（Active）拿 access_token     1. extract_bearer_credential
2. 组装 X-Federation-Caller:              2. authenticate_link_call(hash 匹配 Active)
   {"caller_org":"A-id",                  3. 解析声明头（缺省 = 连接级匿名调用）
     "caller_user":"u-1",                 4. 能力白名单检查（§4）
     "caller_agent":"ag-9"}               5. RequestContext 注入：
3. POST /a2a (agents/sendTask)               organization_id = B（目标，数据作用域）
                                             caller_organization_id = A（iss 语义）
                                             caller_type = FederationUser/Agent
```

---

## 四、能力发现与白名单

**问题**：A 怎么知道 B 有哪些 Agent 可以调？B 怎么控制"开放给这条连接什么"？

**设计：能力端点 + 连接级白名单（默认全开 Agent 子集）**

1. **能力端点**（机器侧，契约凭证鉴权，复用 §3 流程）：
   `GET /api/v1/organization/links/capabilities` → 返回本节点开放给**调用方这条连接**的能力清单：
   ```json
   { "agents": [ {"id":"ag-9","name":"翻译官","description":"..."} ],
     "capabilities": ["a2a_task"] }
   ```
2. **白名单粒度**：连接级（organization_links 加 `capabilities` JSON 列，默认 `["a2a_task"]`）；Agent 级白名单延后到真需要时（YAGNI）
3. **发现时机**：建联后首次同步拉一次；A 侧 Agent 引用缓存 + cron 对账顺带刷新（S8 管道直接复用）

**A2A 入站鉴权（P1+P2 已实现，双模鉴权）**：v1 设计稿误判 `/a2a` 无鉴权——实际一直挂 `jwt_auth_middleware`。因此原 P1（可选 Agent 级 token）作废，P1/P2 合并为 `/a2a` 双模鉴权：本地 JWT 优先（既有语义不变），失败回退联邦凭证（`authenticate_link_call` 哈希匹配 Active 连接）+ 可选 `X-Federation-Caller` 声明（明文 JSON；缺失 = 连接级匿名，合成身份 `federation:{peer_org_id}`；非法/声明组织与连接归属不符 → 401 fail-closed）。注入：`organization_id` = 本端（目标），`caller_organization_id` = 对端（iss 语义，R3 计量），不注入本地角色。`/a2a/subscribe` 暂维持 JWT-only（YAGNI，需要时换同一中间件）。

---

## 五、发起侧：跨组织 Agent 寻址（P4 已实现）

- @ 提及协议扩展（`common/src/mention.rs`）：`MentionRef.org: Option<String>` + `parse_mention_dest` 支持 `agent:<id>@<org_id>`（仅 Agent；空段/多 @/非 Agent 类型带 @ 一律降级普通链接，既有原则不变）；`format_mention_ref` 对应输出。合法性（org 是否有 Active 连接）由上层校验，协议层只做格式解析
- 直连路由（`src/consumer/message.rs::try_federated_delegation`）：仅用户消息触发（Agent 回复中的 @ 对端提及不外呼，防调用环）；可路由 → 联邦委派并跳过本端 Agent 唤醒，对端回复以对端 Agent id 为 from 落库给原用户；不可路由 → 降级普通提及（仅上下文注入）。组织上下文优先取消息自带 organization_id
- 委派编排（`organization domain::delegate_agent_task`）：路由决策（Active 连接 + `a2a_task` 能力，返回 `Ok(None)` = 降级 / `Ok(Some(reply))` = 成功 / `Err` = 已建联但调用失败）+ `X-Federation-Caller` 声明（caller_org + caller_user）；传输（`organization dal::send_federated_agent_task` → `a2a.rs::execute_federated_agent_call`）：send → 轮询 tasks/get 直到终态（120s 预算，1s 间隔；send 响应已终态带文本则直接返回）
- B 侧收口（`consumer/message.rs`）：tasks/send 创建的一次性 project（tags 含 `a2a`、无 task 关联）在 Agent 回复后自动 complete —— 否则对端轮询 tasks/get 永远 working（修复既有缺口，外部 A2A 客户端同步受益）
- 结果回传：同步路径由 tasks/send + 轮询闭环；长任务异步（callback / a2a_task_id tag + polling producer）留待需要时接入

## 五.5、前端接入与能力发现管道（P5 已实现）

- **联邦 Agent 目录**（用户侧 `GET /links/federation-agents`，JWT）：domain `list_federation_agents` 聚合所有 Active 连接——`FederationHttpClient::fetch_capabilities` 实时向各对端拉能力清单（出站凭证 Bearer），单个对端失败仅 WARN 跳过（部分可用优于整体为空）；返回按对端组织分组（org_id/org_name/agents/capabilities）
- **mention picker 联邦候选**（`frontend/src/components/mention_picker.rs`）：`MentionCandidate.org: Option<String>`，`token()` 走 `format_mention_ref` 输出 `agent:<id>@<org>`；Agent 候选在本地结果后追加联邦分组（subtitle `联邦 · 对端组织名`，本地按关键词过滤），组织内/项目会话均可 @ 联邦 Agent
- **渲染**：联邦提及 chip 叠加 `mention-federated` 修饰类（虚线边框）+ `data-mention-org`；对端 Agent 不在本地目录，名字回退链接文本快照
- 每次提及查询实时拉对端（YAGNI：连接数少；后续有性能压力再引入缓存 + cron 对账刷新）

---

## 六、受理层身份模型：接待员模式（P6 已实施 2026-09-04）

> 拍板：跨组织请求由**被访组织**决定谁来接待与执行，请求侧不可指定服务者；接待落地后的一切行为与「一个本地用户在用我们组织」完全同构。

### 身份三层

| 层 | 身份 | 来源 | 可见性 |
|---|---|---|---|
| 对外 | link 凭证 + 合成访客 `federation:{peer_org_id}` | 建联（Phase 1）+ 双模鉴权（P1+P2） | 仅到受理层为止 |
| 受理 | **接待 user**（对接员身份）+ **接待 Agent**（`ROLE_A2A_GATEWAY`） | 接待 user：当前 = 组织内最高权限管理员（创建者，凭据最全），user 表无新字段；接待 Agent：`ROLE_A2A_GATEWAY` 角色解析 | B 侧全链路可见 |
| 执行 | 内部普通 Agent | 接待 Agent 受理后按既有协作模型委派 | 仅 B 侧内部，对端不可见不可寻址 |

- **接待 user**：被访组织为联邦访客落地一个真实 user 身份（「对接员」）。`tasks/send` 入站时，合成身份 `federation:{peer_org_id}` 映射到该 user——project 的 user_id/owner、消息 from 全部落在他名下。**此后一切（权限、SSE、消息流、记忆）与本地用户路径零差异**，不需要任何特殊分支
- **接待 Agent**（已有 `resolve_agent(by_role)` 解析）：收到需求后按复杂度决策——简单问答直接回复（回复即完成）；复杂事项建 task / @ 协作 Agent 拉起内部委派，执行者以**自己的身份**工作，日志/记忆/权限全走本地体系
- 对端可寻址的永远只有接待 Agent（capabilities 白名单只暴露它即可）；内部 Agent 身份不外泄
- 递归防护不变：内部委派产生的 Agent 回复中的 @ 对端提及不外呼（联邦路由仅用户消息触发）

### A2A task ↔ project 生命周期映射（长任务）

- `tasks/send` 即建 project（已有）→ working（InProgress）；**project 不实时关闭**——关闭与否完全由任务是否做完决定
- 状态映射：project InProgress（含子 task 进行中）→ working；project 完成（task 终态 / 接待侧手动完结）→ completed
- 双通道回传：同步 = `tasks/get` 轮询（现状）；异步 = `notification_url` callback（既有渠道管道）或对端轮询
- **完成判定（已实施 2026-09-04，`consumer/message.rs`）**：Agent 回复后按「做完才关」判定——project 无进行中 task（一次性问答）→ 回复即完成（保留同步轮询路径）；有 Pending/PendingReview/InProgress task（接待员已内部委派）→ 保持 working，等任务完成/接待侧确认后再收口。对端轮询持续看到 working 是正确语义。用例 `test_a2a_project_with_inflight_task_stays_working`
- **接待 user 映射（已实施 2026-09-04）**：三层各一方法——`UserDao::find_reception_user`（SQL：org 内 role IN (SuperAdmin,Admin) 按 role+created_at 取最早，user 表不加字段，未来换专用接待用户只改此实现）→ `UserDal::find_reception_user` → 组织域 `UserManage::reception_user`（返回完整 UserPo，上游自取所需字段；无管理员 fail-closed NotFound）。落点在 `a2a_auth` 中间件第 7 步（合成身份的诞生点）：联邦路径 `X-User-Id` = 接待用户 ID，project root_user/owner、消息 from 全部落他名下；`X-Federation-Caller` 声明仅做一致性校验（401 fail-closed），留给审计/计量（见 §八）
- **项目来源审计（已实施 2026-09-05）**：联邦入口创建 project 时 tags 追加 `federation:<peer_org_id>`（保留基础 `a2a` tag；本地 JWT 路径无 caller 不受影响）。`projects_fts` 建表已含 tags 列（FTS5 表级 MATCH 覆盖），tag 天然可全文检索——回答「这个项目来自哪个组织」。精确索引（`project_tags` 规范化表）仅在项目量大 + 高频按 tag 过滤时再上（JSON 数组列无法走 B-tree/表达式索引）

### 与既有实现的关系

`resolve_agent(by_role)` / project tags=`a2a` / `X-Federation-Caller` 全部保留；**接待 user 映射与完成判定均已实施**，P6 收官。

---

## 七、网络拓扑：内外网可达性（P7，已实施 2026-09-05）

> 拍板：节点地址区分内网/公网；出站前探测，**内网优先、公网回退**。目标 = 把「可访问」与「不可访问」区分开。不做穿透/中继（YAGNI）。

- **多地址模型**：节点/组织目录条目携带地址列表 `[{url, scope: public|private}]`——建联时自报 + 目录同步累积。一台有公网 IP 的设备并入系统后，它的内外网地址都会被对端记录
- **可达性解析**：出站调用（capabilities / 委派 / 目录同步）前解析 endpoint——优先探测内网地址，**首个连通即停（first-match，不做全量扫描）**；内网候选全不通再回退公网，公网同理；两者皆不通 = 该对端暂不可达（按既有「单对端失败跳过」惯例降级，不阻塞其它对端）
- **探测缓存**：探测结果带 TTL，避免每次调用都付探测延迟；缓存过期或实际调用失败（连接拒绝/超时）时清缓存重探。**「不可达」结论本身同样入缓存**——否则对全不通的对端，每次出站都要白付一轮探测延迟
- **协议不变**：凭证、声明头、能力发现均与地址无关；`link.endpoint` 语义从「单地址」升级为「当前首选可达地址」（由解析器维护），上层零感知
- 纯内网且本端无任何可达地址的节点：暂不可达，等「方向性组网」（内网侧主动出站拉取的反向通道）有真实需求时再立项，见 §九

### 地址数据模型分层（2026-09-05 对齐）

> 原则：**可达性是「链路」的属性，不是「组织」的属性**。同一组织对不同邻居的可用地址不同（同内网邻居走 private、跨网邻居走 public），故「联通用地址」必须挂在 link，organization 只存自报全集。现状代码已暗合此分层（`base_url` 无通信用途，仅展示/影子同步复制；出站唯一取 `link.endpoint`），P7 将其显式化。

| 层 | 载体 | 语义 | 维护方 |
|---|---|---|---|
| ① 自报地址全集 | `organizations.addresses` JSON（`[{url, scope}]`，新列） | 对端自报的地址列表，**如实记录、只读参考** | 目录同步自动维护（serde 缺字段向后兼容） |
| ② 可达性解析 | 探测器（`dao/organization_link/resolver.rs`） | 从①候选池中探测，内网优先 → 公网回退 | 出站调用前 + TTL 缓存 |
| ③ 实际在用地址 | `organization_links.endpoint` | 本端此连接的**主地址**（公网组首位候选），上层零感知 | 建联写入，解析结果不回写（YAGNI，回写待前端展示需求） |

- **自报 ≠ 信任**：对端报的内网地址对本端未必有意义（`10.x` 网段可能撞车 / 不在同一内网），①只是候选池，②探测裁决，③才是被上层消费的事实

### 实施记录（2026-09-05）

| 项 | 内容 |
|---|---|
| 协议 | `PeerOrgDirectoryEntry` 加 `addresses: Option<Vec<FederationAddress>>`（serde default 向后兼容；`FederationAddress {url, scope}`，scope = `private`/`public`） |
| 存储 | migration `20260905000001`：organizations 加 `addresses` 列（默认 `'[]'`）；**裸 SQL 读写不进 PO**（先例 config 列）。`links.private_endpoints` override 列未加（YAGNI，人工修正候选的需求出现再加） |
| 自报来源 | Local 组织 = domain 组装点从 config 推导（`federation_base_url` → private + `public_base_url` → public，配置启动后不变），**目录导出时动态注入、即时生效无漂移**；影子组织 = 目录同步复制的原文 |
| 解析器 | `dao/organization_link/resolver.rs`：`resolve(primary, addresses)`——无自报地址走快速路径原样返回（零探测，P7 之前行为）；内网 first-match 探测（TCP connect 500ms）→ 公网回退（主地址为公网组首位）→ **全不通维持主地址**（交给出站 HTTP 自然失败，不改既有失败语义）；解析结果（含选定地址）TTL 60s 缓存 |
| 接入 | 出站全链路——委派（`send_federated_agent_task` DAL 内解析）、能力发现（`list_federation_agents`）、目录推送/对账（`active_peer_endpoints` 统一解析后去重）、建联后双向同步（`sync_directories_after_link`）。编排放 **org DAL**（`resolve_peer_endpoint`，组合影子地址查询 + 解析器单例），domain 只消费解析结果 |
| 单测 | resolver 5 用例：候选排序（内网优先/去重/空地址过滤）、host:port 解析（含 IPv6）、真实 TCP 探测的内网优先 + TTL 缓存（关 listener 后 TTL 内仍返回缓存值）、全不通回退主地址、快速路径 |

- **组件落位（2026-09-05 对齐）**：探测与地址解析属「对端通信基础设施」，与既有出站客户端同放 `src/service/dao/organization_link/`（`http.rs` 同级新增解析器）；上层 Domain/DAL 只消费解析后的首选地址，零感知。长连接管理分两处——**连接生命周期（WS 管理器）是 `pkg/` 通用组件，联邦侧如何用连接（寻址 / 路由 / 入出站事件）留在 `dao/organization_link/`**，详见 §九 落位表
- **前置改造（已实施 2026-09-05）**：联邦身份解析从 `a2a_auth` 中间件抽为纯函数 `resolve_federation_identity`（`src/middleware/federation_identity.rs`）——输入「凭证 + 已解析声明」、输出 `FederationIdentity`，不感知传输层。**P7 与 P8 的鉴权路径共用此实现**，避免两条链路的鉴权逻辑漂移（详见 §九）

---

## 八、审计与计量（结合 stats 体系，已实施 2026-09-05）

**审计需求结合既有 stats 体系实现，不建独立审计/计量表。** 两层分工：

| 层 | 载体 | 状态 | 回答的问题 |
|---|---|---|---|
| 项目级溯源 | project tags `federation:<peer_org_id>` | ✅ 已落地（2026-09-05） | 「这个项目来自哪个组织」 |
| 活动级审计 | DuckDB stats 事件 `caller_organization_id` 列 | ✅ 已落地（2026-09-05） | 「对端组织在本端产生了多少活动」 |

**实施内容（2026-09-05）**：
- 5 张专用事件表（project/task/model_call/tool_call/agent_awake）加 `caller_organization_id VARCHAR` tag 列；`CREATE TABLE` 含新列 + `ALTER TABLE ADD COLUMN IF NOT EXISTS` 兼容已有 DuckDB 文件（幂等）
- 自动注入：`StatEvent` trait 新增 `apply_caller_organization`（默认 no-op），derive 宏仅对声明了该字段的事件生成 override；`Stats::record` 统一从 `ctx.caller_organization_id()` 注入——**打点侧零改动**（`record_event!` 宏展开加 `..Default::default()` 兼容新增字段）
- 语义：联邦请求路径 = 对端 org（a2a_auth 中间件写入 ctx），本地 JWT 路径恒 None
- 审计查询 = DuckDB 按 `caller_organization_id × project_id` 聚合，同时覆盖计量（调用量/耗时/Token 按 org 汇总）——**F4 不再需要独立计量表，stats 查询即计量**
- 单测 `test_caller_organization_id_auto_injection`：联邦路径注入对端 org / 本地路径恒 None

**后续（按需）**：项目级 tags 若出现高频精确过滤需求，再上 `project_tags` 规范化表（可从现有 tags 回填）

---

## 九、方向性组网：WS 长连接（P8，设计已对齐 2026-09-05）

> 动机：短连接（HTTP 请求-响应）下「被访问」必然要求被调用方有可达地址——纯内网节点永远只能做发起方。长连接把可达性要求**从双向降为单向**：只需内网侧能拨出一次并维持连接，对端即可沿连接反向调用（与区块链 NAT 节点「主动拨号 + 保活」同构）。

- **与 P7 互补，不替代**：P7 解决「首次怎么拨通」（多地址探测），P8 解决「拨通之后双向可用」。P8 建联仍需 P7 解析出的可达地址，故实施顺序为 P7 → P8

### 架构：WS 管理器做 pkg 通用组件，AOP 双向事件解耦（2026-09-05 用户拍板）

> 核心洞察：**长连接就是 AOP 的一个生产者 + 一个消费者**。外部消息进来 → 转成内部事件 → 由业务 consumer 消费；业务要出去 → 发一个出站事件 → WS consumer 路由到连接 push 出去。**业务侧零感知长连接存在**。

```
入站：WS 收帧 → [联邦 adapter] publish(Inbound 事件) → 业务 consumer
                    ↳ 调 handler 核心函数 f(ctx, params)（推荐）
                    ↳ 或直接调 domain（无现成 handler 时）
出站：业务 publish(Outbound 事件) → WS 出站 consumer → 查连接注册表 → push 到对应连接
```

- **入站消费不会成环**：handler 内部不调 consumer，事件流单向，天然无回环
- **handler 可复用（架构已验证）**：`generate_http_handler` 宏**原样保留**核心函数 `f(ctx, params) -> Result<Resp>`，只额外生成 axum wrapper ⇒ consumer 拿到 ctx + 反序列化出的 params 即可**直接调用同一个业务函数**，domain/DAL 零改动

#### AOP 三处天然契合（实测确认）

| AOP 现有机制 | 对本设计的意义 |
|---|---|
| `Consumer::on_event(ctx, event: serde_json::Value)` | 事件本身就是 JSON Value，**天然即信封 payload**，不需要额外做类型擦除层 |
| `publish` 自动注入 `ctx.to_carrier()`，消费侧还原**同源 ctx** | **红线自动满足**：WS 会话构造一次 ctx（含接待用户 / caller_org），随事件流转到 consumer；信封压根不必带身份字段 |
| `Event` trait 自带 `order_key()` + `id()` | 长连接最需要的两样——**顺序保证**（同 peer 串行）与**幂等键**（重试去重）——框架已提供 |

`EventKind` 就是 `&'static str`（如 `"federation.inbound"`），新增种类零成本。

#### 命令分发：AOP 订阅即路由表

- ❌ 不做「一个 Inbound 事件 + consumer 内 switch cmd」
- ✅ **一种命令一个 `EventKind` + 一个 consumer**，由 `interested_events()` / `register_consumer` 完成分发

新增命令 = 加一个 consumer + 一个 EventKind，**零分发代码**。这直接消解了「手写命令路由表」的工作量。

#### 请求-响应配对（关键缺口，P8 必须解决）

A2A 委派是要结果的，但入站是单向事件，链条需闭环：

1. B 收帧 → `publish(FederationInbound { correlation_id, reply_to: A, ... })` → consumer 执行
2. 执行完 → `publish(FederationOutbound { target_peer_org: A, correlation_id, result })` → WS consumer 推回
3. **A 侧如何等响应**：出站前在 pending 表注册 `correlation_id → oneshot`；收到响应帧后由专门的响应 consumer 按 correlation_id 唤醒等待方
4. **pending 必须带超时清理**：事件是内存队列（`pkg/aop/queue/in_memory.rs`），进程重启即丢、连接也断，语义自洽；但挂起请求必须有超时兜底，否则 oneshot 永久悬挂

#### 入站 consumer 必须 Async

`ConsumeMode::Sync` 会在 `publish` 的调用线程里直接执行 `on_event`——而 publish 发生在 WS 读循环里。一个 120s 的 A2A 任务会**阻塞整条连接**。必须用 `ConsumeMode::Async`（入队 + worker 拉取）。代价：Async 支持 nack 重试 ⇒ 出站 push 需按 `event_id` 做幂等。

#### 出站可再进一层：`call_peer` facade

```rust
async fn call_peer(peer_org: &str, cmd: &str, payload: Value) -> Result<Value>
```

上层（委派逻辑）只调这个，不感知底层走 HTTP 还是 WS——WS 可用走长连接，不可用回退 HTTP。**P7 与 P8 在此统一**，业务侧连「有没有长连接」都不用知道。与「业务 publish 出站事件」不冲突：facade 内部即 publish + 等响应（pending 表）。

#### 鉴权前移到连接层

握手时用 link 凭证鉴权**一次**（复用 `resolve_federation_identity`），会话持有 `peer_org_id` / 本端 org / 接待用户，之后每条消息的 ctx 由会话构造。**不是取消鉴权，是把鉴权粒度从 request 换成 session**（同 TLS 会话复用 / HTTP2 连接承载多请求模型）。

- **红线**：信封 header 绝不可携带 `user_id` / `caller_organization_id` 等身份字段，身份一律由会话注入——否则任何能写帧的人都能提权到接待用户

#### pkg 通用性边界

WS 管理器放 `pkg/` 就该保持通用，**不该知道「联邦」「接待用户」**。pkg 只负责：连接生命周期 + 帧收发 + 心跳 + 重连退避 + 上下文透传；**帧 ⇄ 事件的转换由联邦侧 adapter 完成**（注册回调或实现 trait）。否则 pkg 会被业务语义污染。

### 落位表

| 组件 | 落位 | 职责 |
|---|---|---|
| WS 管理器 | `src/pkg/`（通用组件） | 连接生命周期 / 帧收发 / 心跳 / 重连退避 / 上下文透传，**无联邦语义** |
| 联邦侧 adapter | `src/service/dao/organization_link/` | 帧 ⇄ 事件转换、寻址、连接注册表、入出站事件路由 |
| P7 地址探测与解析 | `src/service/dao/organization_link/` | 多地址探测、内网优先回退、TTL 缓存（与 `http.rs` 同级） |
| 业务 consumer | `src/consumer/` | 消费入站事件，调 handler 核心函数或 domain |

> 注意：DAO 层目前是无状态的（每次开连接、用完即弃）；引入长连接后会持有活连接句柄，成为有状态组件。这是必然代价，可接受——但连接管理须单独成文件（如 `connection.rs`），不与 `http.rs` 混放，避免无状态出站调用被有状态组件污染语义。

### 风险清单

| 级别 | 风险 | 对策 |
|---|---|---|
| P0 | 信封携带身份 → 提权 | 会话注入；信封只放指令 / 追踪 / 幂等键 |
| P0 | 会话固定 | 重连必须重新握手鉴权，禁止「恢复会话沿用旧身份」 |
| P1 | **请求-响应无法配对** | correlation_id + 发起侧 pending 表（oneshot）+ **超时清理** |
| P1 | **入站 consumer 用 Sync 阻塞读循环** | 强制 `ConsumeMode::Async`；代价是需按 `event_id` 做出站幂等 |
| P1 | 幂等缺失 | 重连重发会重复建 project/task ⇒ 业务消息必带幂等键，受理侧去重 |
| P1 | 连接假死（TCP 半开） | ping/pong + 应用心跳，间隔小于企业代理 idle timeout（常见 60s） |
| P2 | 中间件旁路 | `a2a_auth` 是 axum `from_fn`，长连接不走它 ⇒ 已抽为纯函数共用（§七 前置改造） |
| P2 | pkg 被业务语义污染 | 联邦语义留在 adapter，pkg 只管连接与帧 |
| P2 | 可观测性 / 资源 | 长连接请求不进 HTTP access log，需独立埋点；连接数上限 + 单连接消息限流 |

### 分期

1. ~~阶段 0：身份解析抽纯函数 `resolve_federation_identity`~~ ✅ 已实施（2026-09-05）
2. P7：多地址 + 探测回退（P8 建联的前提）
3. P8 最小闭环：长连接**只承载 A2A 委派一条链路**，跑通模型后再谈泛化
4. YAGNI：不一次把所有 DTO 搬上长连接；不做打洞 / 中继

---

## 十、实施步骤（每步独立提交）

| 步骤 | 内容 | 验收 |
|------|------|------|
| ~~P1~~ | ~~入站可选鉴权~~（前提错误，作废，见 §四） | — |
| P1+P2 | `/a2a` 双模鉴权中间件（JWT 或联邦凭证 + 声明解析 + RequestContext 注入） | ✅ `federation_a2a_test` 6 用例：有效凭证+声明 / 无声明合成身份 / 错凭证 401 / 非法声明 401 / 冒充声明 401 / 无凭证 401；JWT 路径回归 `a2a_flow_test` 全绿 |
| P3 | 能力端点 `GET /links/capabilities` + organization_links.capabilities 列（默认 a2a_task）+ `/a2a` 白名单门禁（未开放 → 403） | ✅ `federation_a2a_test` 8 用例全绿（含 capabilities 端点 200 + 白名单外 403） |
| P4 | 发起侧：`agent@org` 提及扩展 + runtime 路由到对端 A2A + 结果回传 | ✅ `federation_delegation_test` 3 用例：端到端（A 用户 @ 对端 → B 侧真实 awaken 执行 → A 收到回复 + B 侧 A2A project 自动 complete）/ 无 Active 连接降级 / 能力白名单外降级；出站轮询单元测试 4 用例（同步型对端 / send+poll / failed 终态 / 超时） |
| P5 | 联邦 Agent 目录 + 前端 picker 接入 | ✅ `GET /links/federation-agents`（单对端失败跳过）+ MentionCandidate.org + 联邦候选/虚线 chip；`federation_delegation_test` 4 用例全绿 |
| P6 | 受理层身份模型：接待 user 映射 + project 生命周期完成判定（§六） | ✅ `federation_a2a_test` 8 用例：接待用户映射（含无声明路径）+ 做完才关（inflight task 保持 working）+ 来源 tag 断言；完成判定见 `federation_delegation_test` |
| P0 | 联邦身份解析抽纯函数 `resolve_federation_identity`（§七 前置改造 / §九 阶段 0） | ✅ `federation_a2a_test` 8 用例全绿；`a2a_auth` 中间件仅剩 HTTP 协议适配（提取 Bearer / 解析声明头 / 错误码映射状态码） |
| P7 | 内外网可达性：多地址 + 内网优先探测回退（§七） | ✅ 已实施（2026-09-05）：resolver 单测 5 用例（内网优先真实 TCP 探测 + TTL 缓存 + 快速路径回退）+ 联邦回归全绿；出站全链路（委派/能力发现/目录同步/对账）接入解析 |
| P8 | 方向性组网：WS 长连接（pkg 通用管理器 + AOP 双向事件解耦 + `call_peer` facade，§九） | 设计已对齐，待实施（依赖 P7） |

---

## 十一、风险与未决项

| # | 事项 | 建议 | 状态 |
|---|------|------|------|
| F1 | `X-Federation-Caller` 声明无密码学保护（对端节点可伪造发起者身份） | **已拍板接受**：信任模型 = 对端节点可信（与配对建联同级）；真需要时升级为签名声明（HMAC link token 即可，不必非对称）。已实现的中间件另含 fail-closed 校验：声明组织与连接归属不符即 401 |
| F2 | `/a2a` 现无鉴权，P1 之前跨组织化 = 裸奔 | ~~已纳入 P1~~ **设计稿误判**：`/a2a` 实际一直 JWT 严格保护；真正缺口（不认联邦凭证）已由 P1+P2 双模鉴权解决 |
| F3 | callback 回传 URL 暴露在内网之外 | callback 靠 task_id 随机性 + 终态幂等；如需收紧改为对端主动 polling | 待拍板 |
| F4 | 计量只有日志没有表 | ~~YAGNI，等结算需求~~ **已实施（2026-09-05）**：stats 事件加 `caller_organization_id` 审计维度列 + DuckDB 聚合即计量，不建独立计量表，见 §八 | 已完成 |
| F5 | 一次性 project 自动 complete 与接待员长任务模型冲突 | ✅ 已实施「做完才关」判定：无进行中 task 收口（同步路径保留），有则保持 working | 已完成 |
| F6 | P8 长连接的请求-响应配对 / 幂等 / 读循环阻塞 | 设计已对齐（§九 风险清单）：correlation_id + pending 表超时清理、强制 Async 消费、按 `event_id` 出站幂等 | 待实施 |

---

## 附：与既有讨论的关系

- 鉴权模型 ② 是 D6 凭证协议的自然延伸（每端生成的 token 给对端出站用）
- `X-Federation-Caller` 字段与 R2 Claims iss/aud 语义对齐，未来升级 token 交换时中间件可平替
- 能力端点放 `handlers/organization/links/`（D7 落位原则：机器侧同前缀 root 直挂）
