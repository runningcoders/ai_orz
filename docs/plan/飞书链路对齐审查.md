# 飞书链路对齐审查

> 🎯 **定位**：用微信 iLink 重构（[微信iLink协议对齐重构方案](微信iLink协议对齐重构方案.md)）沉淀的检查清单，逐项回扫飞书（Lark）链路与前端展示，列出**同一族的静默失效 / 自愈缺口 / 契约漂移**。
> 状态：**v3 —— 协议层已取证（实测证伪）+ 鉴权/凭据链路已对账**；待修复清单见 §六，施工分解见 [飞书链路对齐重构方案](飞书链路对齐重构方案.md)
> 触发场景：排查「飞书机器人没反应 / 群里 @ 不理 / 连上了收不到事件 / 扫码建了应用但没有凭据」时打开
>
> 关联文档：
> - [飞书链路对齐重构方案](飞书链路对齐重构方案.md) — **本文的施工版**（阶段 / 任务 / 风险 / 验收）
> - [微信iLink协议对齐重构方案](微信iLink协议对齐重构方案.md) — 检查清单来源（静默失效族 / 状态机穷尽性 / 前后端轮询模式 / 可观测性五层）
> - [AOP 生产者-消费者契约设计](../design/aop_producer_consumer_contract_design.md) — 飞书入站消费与 `on_consumed` 收尾归属
> - [微信渠道接入设计](../design/wechat_channel_integration_design.md) — 同层对照物（§5.6 容错表可与本文 F1/F2 对齐）
> - [CODE_STANDARDS.md](../CODE_STANDARDS.md) — 编码规范 SSOT

---

## 一、结论先行

### 1.0 先说最重的一条：飞书长连接**从来没有建立过**

`src/service/dao/lark/ws.rs:25` 硬编码的取连接地址端点是：

```rust
const PATH_WS_ENDPOINT: &str = "/open-apis/callback/ws/endpoints";
```

**真机探测（2026-09-20，本机直连官方域名、假凭据，只判路径是否存在）：**

| 探测 | 结果 |
|------|------|
| `POST https://open.feishu.cn/open-apis/callback/ws/endpoints`（我方路径，Bearer） | **HTTP 404 `page not found`** |
| `POST https://open.feishu.cn/callback/ws/endpoint`（官方路径，`{AppID,AppSecret}`） | **HTTP 200** + `{"code":1000040343,"msg":"internal error","data":{"URL":""}}` |

也就是说：**我方路径不存在（404），整条 WS 链路在「取连接地址」这一步就失败了**，`fetch_ws_endpoint` 抛 `ThirdPartyError`，supervisor 以 1s→60s 退避无限重试 —— 现象是「日志里一直在重连、永远收不到事件」。全仓只有这一处飞书 WS 实现（已核对）。

协议层还错的更多（格式、鉴权、心跳、重连口径、事件应答），逐条见 **§二**。

### 1.1 三类洞（微信那轮堵过、飞书一个都没堵）

1. **静默失效族**——解析失败 / 有意跳过 / 空字段当成功，全部只留 `debug` 甚至不留痕；
2. **「连上了但收不到」不可见且不自愈**——WS 死亡不改 `state`、不带原因、不触发告警；
3. **前端仍是「手动按钮」反模式**——device flow 要用户手点「已完成授权」，与微信改造前的「我已扫码完成」同构。

严重度分布：

| 级别 | 含义 | 条数 |
|------|------|------|
| **P0-0** | **协议层根本错**：连接无法建立 / 帧格式不符 / 缺事件应答 | 8（F0-1…F0-8） |
| **P0** | 直接表现为「连上了却收不到消息」，且线上不可诊断 | 3（F1–F3） |
| **P1** | 能通但不会自愈 / 排查黑洞 / 体验反模式 | 5（F4–F8） |
| **P2** | 契约与展示一致性（字段被忽略、降级被当正常） | 7（F9–F15） |
| **P0（凭据）** | **凭据获取链路断**：扫码建了应用，但 `appId` 被丢弃、`appSecret` 从未进库 | 3（F17–F19） |
| **P3** | 能力面（群聊 / @mention），登记不做 | 1（F16） |

> 教训（已回收进 `third-party-protocol-alignment-audit` 技能）：**协议形态（端点 / 传输编码）必须先证伪，再谈字段级对齐**。上一版本报告在没验证端点与帧形态的前提下逐条讨论 close 帧与心跳细节，方向是对的但顺序错了 —— 那些都是连接建立**之后**才谈得上的事。

### 1.2 第二条断点：凭据**压根没进我方库**（本轮新增）

协议层之外，还有一条**独立的**断点：即使用户走完「扫码建应用」，我方也拿不到可用凭据。

- `lark-cli config init --new` **会把 `appId` 明文打到 stdout**（`{"appId":..., "appSecret":"****", "brand":...}`），我方却把 `LarkBindStatusResponse.app_id` 硬编码为 `None`（`bind_status.rs`）；
- 明文 `appSecret` 只落在 **OS keychain**（`core.ForStorage`），我方作为外部子进程读不到；
- 于是「分支 A（自动写入 credential）」**从未实现**，前端 Done 分支只能无条件提示「请手动补填 App Secret」。

结论：**「不能用 access token 就用不了扫码」是伪命题**——扫码链路产出的 `AppID + AppSecret` 正是长连接所需的同一份凭据；真正的问题是**凭据的传递方向接反了**（想让 CLI→我方，实际应我方→CLI）。详见 **§2.5 / §2.6**。

---

## 二、协议层对账结论（依据：官方实现 + 真机探测）

### 2.1 取证三源

| 源 | 获取方式 | 用途 |
|----|---------|------|
| **官方 Node SDK `@larksuiteoapi/node-sdk@1.74.0`** | `npm pack`（已解到 `/tmp/lark_spec/package`） | 主 spec：WSClient 状态机、pb 帧结构、错误码、心跳与重连参数 |
| **官方 Go SDK `larksuite/oapi-sdk-go@v3_main`** | GitHub Contents API 取 `ws/*.go` | **第二官方实现互证**：`const.go: GenEndpointUri = "/callback/ws/endpoint"`、`client_transport.go: BootstrapRequest{AppID, AppSecret \| ClientAssertion}`、`pbbp2.pb.go` |
| **官方 `larksuite/cli`（Go）源码** | Contents API 取 `cmd/config/init.go`、`cmd/auth/login.go`、`internal/keychain/*`、`internal/binding/secret_resolve*` | 绑定/授权链路的**产出物与密钥落点**（见 §2.6） |
| **官方域名真机探测** | `curl` 两个端点（假凭据） | 端点存在性**硬证据** |
| 第三方 Rust 实现 `zeptoclaw` | docs.rs / crates.io | 旁证（同一套结论：protobuf 帧、`method=0/1`、WS base 不带 `/open-apis`） |

> **hermes 这轮复核结论（修正上一版的绝对化表述）**：`NousResearch/hermes-agent` 全仓**无 `gateway/platforms/feishu.py`**。它确实有 `Platform.FEISHU` 枚举、`hermes-feishu` 工具集（`toolsets.py:45-47`，内容是 `feishu_doc_read` / `feishu_drive_*` 文档评论类）、`COMPAT_MANIFEST.md` 里的 `plugins.platforms.feishu.*`，且 `gateway/platforms/webhook.py:50` 把 `"feishu"` 列在 **webhook 平台**名单里 —— 即 **hermes 的飞书是「webhook 插件 + 文档工具」，没有内置长连接实现**。作为长连接参照源**不成立**。
> ⚠️ 网上有文章声称「查看 Hermes Agent 源码 `gateway/platforms/feishu.py` 第 2108–2130 行」并给出代码片段——**该引用是编造的**（仓库里不存在这个文件）。它顺带讲的「3 秒超时重推 + 集群非广播」结论本身**与官方文档一致**（见 §2.8），但**引用不可信**。教训：二手文章里的「源码行号」必须回仓核实。
> **`lark-cli` 不含长连接**：官方 CLI（`github.com/larksuite/cli`，Go）确实开源，但 `cmd/event/` 下是**订阅底账检查 + stdin 消费**（`consume.go`），长连接事件接收仍由各语言 SDK 承担。故我方 WS 必须照官方 SDK 协议实现，CLI 只适合做身份授权（device flow）那一半。

### 2.2 我方 vs 官方：协议层对照

| 维度 | 官方（Node SDK 1.74.0 + 实测） | 我方现状 | 判定 |
|------|------------------------------|---------|------|
| **取连接地址端点** | `POST {domain}/callback/ws/endpoint`（**无 `/open-apis`**） | `POST https://open.feishu.cn/open-apis/callback/ws/endpoints` | ❌ **404**（实测） |
| **鉴权方式** | body `{AppID, AppSecret}`（或 clientAssertion）；**不用 Bearer** | `Authorization: Bearer {tenant_access_token}` + `{}` body | ❌ 前提就错（WS 模式不需要 tenant token） |
| **帧编码** | **protobuf 二进制**（pbbp2.Frame），`Frame.encode().finish()` | **JSON 文本** `{"type":"event"\|"pong"\|"close"}` | ❌ 传输层完全不同 |
| **帧类型区分** | `method`：`0`=control(ping/pong)、`1`=data(event/card)；`headers[].type` 细化 | 顶层 JSON `type` 字符串 | ❌ |
| **心跳帧** | protobuf control 帧：`headers=[{key:"type",value:"ping"}]`、`service=serviceId`、`method=0`、`SeqID=0`、`LogID=0` | JSON 文本 `{"type":"ping"}`，**硬编码 30s** | ❌ |
| **心跳间隔来源** | **服务端下发**：`ClientConfig.PingInterval`（秒，默认兜底 120s），且 **pong payload 会再更新一次** | 硬编码 `HEARTBEAT_INTERVAL = 30s` | ❌ 未消费服务端参数 |
| **重连参数** | **服务端下发** `ReconnectInterval`（默认 120s）/ `ReconnectCount`（-1=无限）/ `ReconnectNonce`（默认 30s，**仅首次**抖动 `nonce*random()`） | 指数退避 1s→60s，±20% 抖动 | ❌ 口径不同（我方更激进） |
| **事件应答（ACK）** | **必须回帧**：同 headers + `biz_rt` + payload `{"code":200}`；处理异常 `{"code":500}` | **无 ACK 概念**，`on_frame` 恒 `Continue` | ❌ 缺失 |
| **分片重组** | `message_id`+`sum`+`seq` 重组，到齐才解析 | 无 | ❌ 缺失（长事件会丢） |
| **握手字段** | headers 定义了 `handshake-status` / `handshake-msg` / `handshake-autherrcode` | 无 | ⚠️ 未处理 |
| **致命错误处置** | `code=internal_error(1000040343)` → **可重试**；其余非 0（含 `exceed_conn_limit=1000040350`）→ **不可重试 → onError + 状态 `failed`，停止重连** | 无区分，全部退避重连 | ❌ |
| **close 帧 code** | 官方 **不读** close code，`on('close')` 无条件重连 | 同样不读 | ✅ 一致（见 2.4 对 F1 的修正） |
| **连接数上限** | 官方错误码 `exceed_conn_limit = 1000040350`（**在「取连接地址」响应里**，不是 close 帧） | 未识别该码 | ❌ 新增项 |
| **appId 形态** | `start()` 前校验 `/^cli_[0-9a-fA-F]{16}$/` | 无校验 | ⚠️ 可选 |

### 2.3 官方关键参数（可直接照抄的定值）

```
# 取连接地址
POST {domain}/callback/ws/endpoint
headers: { locale: zh, User-Agent: <UA> }
body:    { AppID: "cli_xxxxxxxxxxxxxxxx", AppSecret: "..." }
timeout: 15000ms
resp:    { code, msg, data: { URL, ClientConfig } }
         URL 的 query 里有 device_id / service_id（我方需要 service_id 来发 ping）

# WSConfig 兜底默认（会被 ClientConfig / pong 覆盖）
pingInterval:    120s   (ClientConfig.PingInterval  × 1000)
reconnectInterval: 120s (ClientConfig.ReconnectInterval × 1000)
reconnectNonce:   30s   (ClientConfig.ReconnectNonce   × 1000)
reconnectCount:   -1    (负数 = 无限)
autoReconnect:    true

# 错误码
ok=0  system_busy=1  forbidden=403  auth_failed=514
internal_error=1000040343        → 可重试
exceed_conn_limit=1000040350     → 不可重试（致命，停止重连）

# 帧
FrameType:  control=0  data=1
MessageType: event / card / ping / pong
HeaderKey:  type / message_id / sum / seq / trace_id / biz_rt
            handshake-status / handshake-msg / handshake-autherrcode
pb Frame:   SeqID(long) LogID(long) service(int32) method(int32)
            headers(repeated Header{key,value}) payloadEncoding(string)
            payloadType(string) payload(bytes) LogIDNew(string)

# 可选保活（默认关闭）
pingTimeout: 0（默认 0 = 不启用）
            启用后：发 ping → 武装 watchdog；任何入站帧 clearLiveness；
            不重新武装（官方注释明确：re-arm 会误杀「ping 30s / 空闲健康」的连接）
```

### 2.4 对上一版报告的修正（原判断 → 修正后）

| 上一版判断 | 官方事实 | 修正 |
|-----------|---------|------|
| F1「**必须**读 close 帧 code 来区分被踢」 | 官方**不读** close code，冲突判据是取配置阶段的 `exceed_conn_limit=1000040350` | F1 降级为「可观测性改进」；**冲突识别的正确落点是取连接配置的响应码** |
| F1「冲突会导致两实例**互相踢 + 无限重连**」 | `exceed_conn_limit` 属**不可重试** → 官方会**停止重连**并置 `failed` | 我方应识别该码 → **停重连 + 健康标 `conflict`**（不是拉长退避） |
| F2「二手来源：`wsConfig { PingInterval: 30, PingTimeout: 5 }`」 | 默认 `pingInterval=120s`；`pingTimeout` **默认为 0（关闭）**；且两者实际由**服务端下发** | 二手来源数值**错误**；我方 30s 硬编码既不来自服务端也不是官方默认 |
| F2「应该依赖协议级 ping/pong 判死」 | 官方默认**不做**无帧判死（`pingTimeout=0`）；启用时语义是「发 ping 后等一个帧」，且不重新武装 | 修法应为：**照实现服务端下发的 pingInterval 发 ping**，无帧保活**按官方语义作为可选开关**（默认关），而不是自创 30s+判死 |
| 退避「1s→60s 指数」 | 官方为**固定间隔**（服务端下发，默认 120s）+ 首次 nonce 抖动 | 指数退避应改为照服务端参数；1s 起步的快速重连对飞书属激进行为 |

### 2.5 鉴权模式全景（回答「能不能继续用扫码授权链路」）

**结论：扫码链路完全可用，而且它产出的正是长连接要的东西。** 之前「不能用 access token 就用不了扫码」的推理，把两套鉴权混成了一套。

飞书有**两条互不相干的鉴权线**，各自有多种模式：

| 鉴权线 | 消费方 | 模式 | 我方现状 |
|--------|--------|------|---------|
| **应用凭证** | ① 长连接建连（WS）<br>② REST API（换 token 后调用） | (a) `{AppID, AppSecret}`<br>(b) `{AppID, ClientAssertion}`（JWT-bearer 客户端断言，**商店应用/ISV 模式**） | ②的实现有（`token.rs` 换 tenant token），①的两种都**没有**——我们把②的产物拿去喂① |
| **访问令牌** | REST API 调用身份 | `tenant_access_token` / `user_access_token` / `app_access_token` | 只用过 tenant |

**WS 长连接的鉴权只有 (a)/(b) 两种，两种都不接受 tenant_access_token**（Go SDK 侧有专门的错误 `ErrCodeAppSecretAndClientAssertionEmpty`；Node SDK 侧 `clientAssertionProvider` 存在时把 `body.AppSecret = ''`，二者互斥非叠加）。

所以：**`扫码建应用 → AppID + AppSecret` 与「WS 建连要 AppID + AppSecret」是同一份凭据**，链路完全对得上。断的不是鉴权模型，是**我们把凭据的用途接错了线**。

### 2.6 凭据获取链路：扫码到底能拿到什么（本方关键结论）

`lark-cli config init --new` 的官方实现（`cmd/config/init.go:417-437`）：

```go
// Mode 3: Create new app directly (--new)
result, err := runCreateAppFlow(ctx, f, brand, msg)          // 浏览器/扫码创建应用
secret, err := core.ForStorage(result.AppID, core.PlainSecret(result.AppSecret), f.Keychain)
saveInitConfig(..., result.AppID, secret, result.Brand, ...)
output.PrintJson(out, {"appId": result.AppID, "appSecret": "****", "brand": result.Brand})
```

| 事实 | 对我方的含义 |
|------|-------------|
| CLI **进程内确实拿到了明文 `result.AppSecret`** | 扫码链路不缺凭据，缺的是「怎么交给下游」 |
| 明文 secret 被 `core.ForStorage` **写进 OS keychain**（`zalando/go-keyring`，master key 也在 keychain；`keychain_darwin.go` 另有 `~/Library/Application Support/lark-cli/*.enc` 加密文件兜底） | 我们作为**外部子进程读不到**（且不该去读 keychain） |
| **stdout 上 `appId` 是明文**，只有 `appSecret` 被打成 `****` | 我方能白拿 `appId`（含 `brand`）——`bind_status.rs` 却硬编码 `app_id: None`，**是纯 bug，且契约字段早就为此留好了** |
| 官方支持 `config keychain-downgrade`：把 master key 从 keychain 落到本地文件，**明确面向 sandbox / automation / CI**（`cmd/config/keychain_downgrade.go`） | 说明「自动化进程读不到 keychain」是官方**预期场景**，降级后 CLI 自身可工作；但**仍不输出明文 secret**（`config show` 同样脱敏） |
| 官方另有 `--app-id` + `--app-secret-stdin` 非交互路径（`init.go:385-397`），且 secret ref 支持 `${ENV_VAR}` 模板 / file / exec provider（`internal/binding/secret_resolve*.go`） | **secret 的注入方向应当是「我方 → CLI」，而不是「CLI → 我方」** |

**由此得到的取舍**：凭据 SSOT 应落在我方加密库（现状如此，`ensure_cli_config` 已把库里的 secret 走 stdin 写入 CLI）。因此「扫码建应用」这一环的正确形态是 **只用来拿 `appId`（顺带建好应用），`appSecret` 由用户从开发者后台复制粘贴一次**；不应指望从 keychain 反读出 secret。

### 2.7 官方 SDK 语言支持（回答「有没有官方 Rust SDK」）

官方服务端 SDK 概览页（`open.feishu.cn/document/server-docs/server-side-sdk`）与 GitHub 组织页**只列四种**：

| 语言 | 仓库 | 要求 |
|------|------|------|
| Go | `larksuite/oapi-sdk-go` | Go ≥ 1.5（v3 分支 `v3_main`） |
| Python | `larksuite/oapi-sdk-python` | Python ≥ 3.8 |
| Java | `larksuite/oapi-sdk-java` | JDK ≥ 1.8 |
| Node.js | `larksuite/oapi-sdk-nodejs` | Node ≥ 10 |

**没有官方 Rust SDK。** crates.io 上的都不是官方：
- `open-lark`（`foxzool/open-lark`，32k 下载，0.14.0）——功能最全，可作**第三方旁证**
- `lark-oapi 0.1.0`（无仓库字段，1.5k 下载）——疑似抢名/半成品，**不建议依赖**
- `feishu-sdk 0.1.2`、`lark-channel`、`lark_bot_sdk_macros` 等均为社区小包

**选型建议**：不引入第三方 SDK 作为协议依据（版本与覆盖面都跟不上，且我们要自己控制帧/重连）。做法是**照官方 Node/Go SDK 自己实现**：`pbbp2` 的 `.proto` 从官方 SDK 抄字段（Node SDK 内含静态 pb 代码、Go SDK 有 `ws/pbbp2.pb.go`），用 `prost` 生成。这与我方现有「Adapter 只做协议语义、生命周期交 `pkg::ws`」的分层是相容的。

### 2.8 官方长连接的硬约束（影响部署与 ACK 设计）

来自官方《使用长连接接收事件》文档（`open.feishu.cn/document/ukTMukTMukTM/uYDNxYjL2QTM24iN0EjN/event-subscription-configure-`）：

| 约束 | 原文要点 | 对我方的含义 |
|------|---------|-------------|
| **仅企业自建应用** | 「长连接模式仅支持企业自建应用」，商店应用必须走 webhook | 我方定位一致 |
| **3 秒内处理完且不抛异常** | 否则**触发超时重推** | 我方读循环「只 publish、不做业务」是对的；但**必须补 ACK 帧**（F0-6），否则每次都会走重推 |
| **每应用最多 50 个连接** | 每初始化一个 client 算一个连接；超限 = `exceed_conn_limit` | 不能靠「多起几个实例分压」；该码要识别并停重连（F0-8） |
| **集群模式，不支持广播** | 「同一应用部署了多个客户端，只有其中**随机一个**客户端会收到消息」 | ⚠️ **多实例部署会随机分发事件** → 我方「同 order_key 串行」假设**跨进程不再成立**；且「超时重推」会把同一条事件扩散到不同实例 → **幂等键必须真的能吸收重复**（`event_id`/`message_id`） |
| **仅建连时鉴权，事件为明文** | 后续推送无需解密验签 | 我方无需实现 webhook 那套 Encrypt Key 验签 |

---

## 三、审查方法（清单来源）

按微信方案的四个维度逐项回扫，不是重新发明：

| 维度 | 微信那轮的具体教训 | 飞书侧对应检查点 | 结果 |
|------|-------------------|-----------------|------|
| **A. 静默失效族** | 「解析层 `.ok()` / `log_debug!` 组合 = 排查黑洞」；「解析失败必须留痕」 | 所有 `log_debug` 跳过点、`.ok()?` 降级点、`#[serde(default)]` 空值出口 | ❌ 见 F1–F3 |
| **B. 连接/会话自愈** | `-14` 暂停 + 凭证指纹变化清暂停两端闭合 | WS 死亡分类（close/半开/被踢）、心跳超时、暂停恢复 | ❌ 见 F0-4/F0-8/F1/F2 |
| **C. 状态机与轮询穷尽性** | 扫码只认 4 态 → **不是报错而是卡住**；前端必须「平铺 spawn + 代次守卫」 | 绑定/授权状态判定穷尽性、轮询循环启动条件与去重 | ⚠️ 见 F4–F5、F11 |
| **D. 可观测性五层** | DAO 快照 → trait → DAL → Domain → handler 逐层有判活字段；「句柄在册 ≠ 在跑」 | 健康指标字段是否足以回答「活没活」 | ❌ 见 F2/F6 |
| **E. 契约字段一致性** | D9 补 `created_at`/`updated_at`；响应字段必须真被消费 | DTO 字段是否恒 None、是否被前端忽略、降级是否可区分 | ❌ 见 F7–F10 |
| **F. 协议字段对齐** | 以官方实现为 spec（文本字段名 / 枚举类型 / 取码方法） | 官方 SDK / 真机探测核对 | ❌ **见 §二（已取证：端点 404 + 帧格式错）** |

---

## 四、发现清单

### P0-0 —— 协议层（连接建立不了 / 说不上话）

#### F0-1｜WS 取连接地址端点不存在（404）

- **位置**：`src/service/dao/lark/ws.rs:25`（`PATH_WS_ENDPOINT`）、`:104`（拼接）、`:98-131`（`fetch_ws_endpoint`）
- **事实**：我方 `/open-apis/callback/ws/endpoints` **实测 404**；官方 `/callback/ws/endpoint` 实测 200。
- **影响**：**连接从未建立**。所有飞书入站事件、所有依赖入站的功能（Agent 接待、消息落库）在飞书上全部无效。
- **修法**：端点改为官方路径（去掉 `/open-apis`，单数 `endpoint`）。

#### F0-2｜鉴权方式错：WS 模式不需要 tenant_access_token

- **位置**：`src/service/dao/lark/ws.rs:98-114`
- **事实**：官方用 `{AppID, AppSecret}` 作为 body 换连接配置；我方取 tenant token 后塞 `Authorization: Bearer`。
- **影响**：即使端点改对，鉴权仍会被拒（且 `WsTokenSource` 这层抽象本身就是错前提）。
- **修法**：改传 AppID/AppSecret；若保留 `WsTokenSource` 抽象，其语义应改为「提供 app 凭据」而非「提供 token」。
- **顺带**：官方响应里的 `ClientConfig` 必须被消费（F0-4）。

#### F0-3｜帧编码错：飞书是 protobuf 二进制帧，我方按 JSON 文本处理

- **位置**：`src/service/dao/lark/ws.rs:54-82`（`WsIncoming` / `WsPing`）、`:152-211`（`on_frame(text: String)` / `heartbeat_frame() -> Some(String)`）
- **事实**：官方全部帧是 `pbbp2.Frame` protobuf；`method=0` control / `method=1` data；事件 payload 才是 JSON。我方 `pkg::ws` 的读循环只接 `Message::Text`（`src/pkg/ws/mod.rs:398`），**连二进制分支都没有**。
- **影响**：即使端点与鉴权修好，服务端发来的二进制帧会被 `pkg::ws` 直接忽略（`Message::Binary` 不匹配任何分支）→ 仍然「连上但一条事件收不到」。
- **修法**：需要引入 prost 定义（上方 §2.3 的 `pb Frame` 字段）+ 在 `pkg::ws` 增加二进制帧通路（adapter trait 需要能收 `Bytes`，不只是 `String`）。
- **说明**：这是一次**接口层改造**（`WsClientAdapter` 的 `on_frame(String)` → 能带 payload 编码标记），不是改几行。

#### F0-4｜心跳参数未按服务端下发（且间隔错）

- **事实**：`ClientConfig.PingInterval/ReconnectInterval/ReconnectNonce/ReconnectCount`（秒）由服务端下发；**pong payload 还会再次更新**这套参数。官方默认兜底 120s。
- **我方**：`HEARTBEAT_INTERVAL = 30s` 硬编码（`src/pkg/ws/mod.rs:36`），且 pong 里若带参数会被丢弃（`WsIncoming::Pong` 无字段）。
- **修法**：取配置时保存 `ClientConfig`，pong 时按其更新；心跳间隔从硬编码改为运行时参数。

#### F0-5｜重连口径错：应为「固定间隔 + 首次抖动」，非指数退避

- **位置**：`src/pkg/ws/mod.rs:32-36`、`:209-214`（`next_backoff`）、`:266`
- **事实**：官方 `reconnectInterval` 固定（默认 120s），仅**首次**加 `nonce*random()`（≤30s）抖动；`reconnectCount<0` 无限。
- **修法**：换成服务端参数驱动的固定间隔；首次抖动保留。

#### F0-6｜事件缺 ACK（服务端会认为投递失败）

- **事实**：官方收到 data 帧后**必须回一帧**（同 headers + `biz_rt` + payload `{"code":200}`；处理异常 `{"code":500}`）。
- **我方**：无 ACK 概念，`on_frame` 恒 `Continue`。
- **影响**：飞书可能按「未确认」处理（重推 / 标记投递失败）。
- **修法**：`on_frame` 返回语义扩展为「是否 ACK + 结果码」，由 pkg 负责回帧。

#### F0-7｜长事件分片未重组

- **事实**：官方按 `message_id` + `sum` + `seq` 重组，到齐才解析；非法元数据要**报错留痕**（官方 issue #201 的教训）。
- **我方**：无。
- **修法**：加一个带 TTL 的分片缓存；元数据非法 → `log_warn` + 丢弃该 `message_id`。

#### F0-8｜致命错误码未识别（连接冲突会无限重试）

- **事实**：`exceed_conn_limit=1000040350` 从**取连接地址**响应返回；官方归入「不可重试」→ `onError` + `state='failed'` + **停止重连**。`internal_error=1000040343` 才可重试。
- **我方**：`fetch_ws_endpoint` 对 `code != 0` 一律返回错误 → supervisor 一律退避重连。
- **修法**：错误分类（可重试 / 致命）；致命 → 停止重连 + 健康快照标 `conflict` + `warn` 日志给出码与 msg。

### P0 —— 「连上了却收不到」且不可诊断（连接修好后仍存在）

#### F1｜WS 服务端 close 帧的 code/reason 被丢弃

- **现象**：`state` 显示 `connected`，日志只有 `lark ws received close from server` + 重连循环。
- **根因**（`src/pkg/ws/mod.rs:395-416`）：

  ```rust
  frame = read.next() => {
      match frame {
          Some(Ok(msg)) => {
              if let Message::Text(text) = msg          // ← 只匹配 Text
                  && adapter.on_frame(text).await == FrameAction::Reconnect
              { break; }
          }
          ...
          None => { log_info!("{} ws stream closed by server", name); break; }
      }
  }
  ```

  服务端下发的 `Message::Close(Some(CloseFrame { code, reason }))` 不匹配，被静默忽略；随后流返回 `None`，只记一句 `stream closed by server`。
- **修正后的定位**（见 §2.4）：官方自身也不读 close code，所以这条**不再主张「必须按 close code 做冲突判定」**；它的价值是**可观测性**——被服务端主动关闭是必须能看见原因的事件。冲突判定的正确落点是 F0-8。
- **修法**：读循环补 `Message::Close` / `Ping` / `Pong` 分支，`warn` 记录 code/reason；同步处理 F0-3 的二进制分支。

#### F2｜心跳只发不收：无帧超时、停发协议级 Ping

- **现象**：网络抖动 / NAT / 代理静默丢包后，连接对象还在、`state` 还是 `connected`，但一条事件都收不到，只能重启进程。
- **根因**：
  1. `src/pkg/ws/mod.rs:356-379`：心跳任务按 `heartbeat_frame()` 做**二选一**——飞书 adapter 实现了它（`:206-211`），于是**协议级 Ping 完全不发**；
  2. 读循环不记录任何帧时间，`pong` 被 `Continue` 吞掉，**没有「多久没收到任何帧」的判据**；
  3. `WsConnState`（`:122-130`）只有 `phase`/`reconnect_count`/`last_connected_at`，**没有 `last_frame_at`**。
- **修正后的定位**（见 §2.4）：官方**默认关闭**无帧判死（`pingTimeout=0`），启用时语义是「发 ping 后等一个入站帧」，且**不重新武装**。所以我方不该自创「30s 心跳 + 判死」，而应先照 F0-4 对齐心跳，再把「无帧超时」按官方语义做成**可选**（默认关），并把 `last_frame_at` 作为**观测字段**常开。
- **修法**：`last_frame_at` 进 `WsConnState`（常开，纯观测）；无帧超时按官方语义可选启用。

#### F3｜入站「有意跳过」四级过滤里三级无 `info` 证据

- **现象**：给机器人发消息没回应；日志里既没有收帧记录也没有跳过记录。
- **根因**（逐级）：

  | 过滤点 | 代码位置 | 现有日志 |
  |--------|---------|---------|
  | 非 P2P / 非文本（**含群消息**） | `src/service/dal/lark/impl.rs:195-205` | `log_debug!` |
  | 文本内容解析失败 | `src/service/dal/lark/impl.rs:208-219` + `src/models/events/lark.rs:157` 的 `.ok()?` | `log_debug!` |
  | 内容为空串 | `src/service/dal/lark/impl.rs:220-222` | **无任何日志** |
  | 消费侧拿到 `None` | `src/consumer/lark_inbound.rs:73-80` | **无任何日志** |
  | 未绑定渠道 | `src/service/dal/lark/impl.rs:233-240` | `log_info!` ✅（唯一有证据的） |

- **影响**：与微信那轮「解析失败被 `.ok()` 吞掉 → 连收帧日志都不打」同一族。`header.event_id` 在出问题时**不会出现在任何一行日志里**。
- **修法**：按微信口径统一——**「有意跳过」必须是 `info` 级单行证据**（带 `event_id` / `chat_type` / `message_type` / 原因枚举，形如 `lark inbound adapted to nothing: reason=group_message event_id=...`）；`parse_text` 失败要区分「非文本」与「文本但 JSON 不合法」；空内容也留痕。
- **附带物证**：`src/service/dao/lark/error.rs:66-95` 定义了 `LarkWsError`（含 `EventParse`）与 `from_serde`（:103），**全仓零引用**（因 `pub` 不触发 `dead_code`）。「事件解析失败」被设计了却没有调用点，正是这一族静默的物证。

### P1 —— 能通但不会自愈 / 排查黑洞 / 体验反模式

#### F4｜出站空 `message_id` 被当成功，写出脏键 `"lark:"`

- **根因链**：
  1. `src/service/dao/lark/http.rs:120-124`：`SendMessageData.message_id` 带 `#[serde(default)]`；
  2. `:144` `Ok(data.message_id)`——字段缺失时返回**空串而非报错**；
  3. `:300` `Ok(Some(message_id))`——**恒为 `Some`**；
  4. `src/service/dal/message_channel.rs:470-471` `format!("lark:{}", lark_message_id)` → `"lark:"`；
  5. 该列只有普通索引（`migrations/20260909000004_add_external_key_to_messages.sql:15,17`，**非唯一**）→ 多条出站消息共享同一键。
- **影响**：飞书响应若改字段名或返回异常体，**消息其实发出去了但链路显示一切正常、且平台 ID 静默丢失**（`external_key` 语义污染）。将来加唯一约束会立刻冲突。
- **对照**：微信侧 `send_text` 用 **`Result<Option<String>>`**（`None` = 无平台 ID，语义显式）。
- **修法**：`push` 里空串归一为 `None`，或 `send_text_message` 直接报错。

#### F5｜device flow 仍是「我已扫码完成」两步式 + 300s 单请求阻塞

- **根因**：
  1. `frontend/src/pages/finance/identity.rs:294-318`：`handle_auth_complete` 由**用户点按钮**触发；
  2. `:591-606` Modal 里是「打开授权链接」+「**已完成授权**」两个按钮；
  3. `src/pkg/lark_integration.rs:33` `AUTH_COMMAND_TIMEOUT = 300s`、`:283` 调用——`--device-code` 是**阻塞轮询**，前端这一个请求最长挂 300s；
  4. `frontend/src/api/mod.rs:29-31` `Client::new()` 无超时 → 网关 idle timeout 会先切断，用户看到网络错误而非授权结果。
- **影响**：用户不点按钮后端永远不知道；300s 后报 `lark-cli timed out`，toast 说「完成授权失败」而非「授权未完成」——**把「还没做」说成「失败」**。
- **对照**：**就是微信改造前的「我已扫码完成」按钮**（当时明确删掉换自动轮询）。
- **补充事实**：`lark-cli`（官方 Go CLI）的 `auth login` / `auth status` 就是 device flow 工具链，**它本身不提供长连接**，所以「授权用 CLI、收事件自建 WS」的分工是对的，只是两端都要按官方口径实现。
- **修法**：`auth/start` 后**平铺 spawn** 自动发起 `complete`（后台任务），前端同时以 3s 间隔轮询轻量 `auth/status` 收敛 UI，用户零点击；或把 `complete` 改成会话式（仿 `bind` session）由前端轮询。
- **待确认**：并发调用 `lark-cli`（`--device-code` + `auth status`）是否安全，需实测；不行则退化为「自动发起 complete + 界面显示等待中」并保留取消。

#### F6｜健康指标缺「收帧判据」，飞书页只能看「在不在册」

- **根因**：
  1. `common/src/api/system.rs:52-60` `LarkWsAppMetrics` 只有 `app_id`/`state`/`reconnect_count`；
  2. 对照 `:78-90+` `WechatPollChannelMetrics` 有 `state`/`rounds`/`last_poll_at_ms`/失败计数；
  3. `frontend/src/pages/system/health.rs:435` 飞书 Gauge 告警条件只有 `a.state == "reconnecting"`；对照微信有 `wechat_channel_stale`（`last_poll_at_ms` 超 90s 视为疑似卡死）。
- **影响**：F0-1 的 404 死循环、F1/F2 的两种死亡，在健康页上**显示为完全健康或仅仅「重连中」**。这正是微信那轮写在红线里的话——**「句柄在册 ≠ 在跑」**。
- **修法**：`WsConnState` 加 `last_frame_at`（F2）后，`LarkWsAppMetrics` 同步补 `last_frame_at_ms`（+ 可选 `frames_received` + `terminal_reason`，承载 F0-8 的致命码），健康页给出与微信同构的 stale 判定。

#### F7｜绑定轮询循环「无条件 spawn」且缺代次守卫

- **根因**（`frontend/src/pages/finance/identity.rs:199-263`）：

  ```rust
  let handle_bind_start = move |_| {
      spawn(async move { ...lark_bind_start... });   // 任务 A：仅成功时 bind_polling.set(true)
      let running = bind_poll_running.read().clone();
      spawn(async move { loop { ... } });            // 任务 B：无条件启动
  };
  ```

- **影响**：重复点击会叠加轮询循环（同一 `session_id` 被两条循环同时轮询 → `Done` 分支重复触发、Modal 重复弹）。当前靠「`bind_polling` 为 false 时任务 B 下一轮自行退出」兜住，属侥幸。
- **对照**：微信侧用 **`poll_gen` 代次守卫** + `AtomicBool` 卸载守卫；飞书只有后半截。

#### F8｜绑定会话注册竞态：5s 启动窗口内「查不到、取消不掉、进程泄漏」

- **根因**（`src/pkg/lark_integration.rs:526-536`）：先跑「最多 5s 等验证 URL」的循环，**之后**才 `registry.write().await.insert(session_id, session)`。窗口内：
  - `bind/status` → 注册表里没有 → 返回 `NotFound`；
  - `bind/cancel`（`:592-604`）→ `remove` 返回 `None` → `Ok(false)`，**不杀进程**；而 `child` 被局部变量持有，5s 后照样注册进表 —— 用户点了「取消」，进程仍在跑。
- **修法**：**先 `insert` 再等 URL**（5s 等待挪到 insert 之后，URL 由前端轮询补取，这条路径本来已实现）。

### P2 —— 契约与展示一致性

#### F9｜`LarkBindStatusResponse` 三个字段恒 `None`

`src/handlers/finance/lark_integration/bind_status.rs` 返回体硬编码 `credential_id: None, channel_id: None, app_id: None`。后果：

- 前端 `BindPollOutcome::Done`（`frontend/src/api/lark_integration.rs:109-130`）**不携带数据**，无法区分「分支 A 已自动建凭证」与「分支 B 需补填」；
- `frontend/src/pages/finance/identity.rs:241-252` 的 Done 分支**无条件**弹补填 Modal + toast「请手动补填 App Secret」，**即使后端确实已写入凭证**；
- `bind/status` 返回的 `app_id` 预填字段（`common/src/api/lark_integration.rs:271-272` 注释写明「分支 B，app_id 预填」）**从未被前端使用**。

→ 二选一：**实现分支 A**，或按 YAGNI **删掉这三个恒 None 的字段**并把前端 Done 文案写成唯一事实。

#### F10｜凭证快照缺绑定/轮换时间

`common/src/api/lark_integration.rs:193-207` 的 `LarkCredentialSnapshot` 只有 `credential_id`/`name`/`app_id`/`is_default`/`channels`；对照微信 D9 已补 `user_id`/`base_url`/`created_at`/`updated_at`。数据现成（`UserCredentialPo` 有时间戳），前端凭证卡看不到任何时间信息。

#### F11｜`judge_bind_status` 把未知状态当失败终止

`frontend/src/api/lark_integration.rs:119-130`：

```rust
match status {
    "pending" => Continue,
    "done" => Done,
    _ => Failed(...),          // ← 含未知状态
}
```

与微信「只认 4 态 → 空转到超时」是**方向相反的同一种穷尽性缺陷**。未识别状态应**按可继续处理但留证据**，终局态才收敛。

#### F12｜聚合状态两处 `.unwrap_or_default()` 静默降级

`src/handlers/finance/lark_integration/get_status.rs:47-49`：查询失败与「确实没有数据」在前端**完全同形**。`:30-34` 凭证列表同样。另：该 handler 直接跨两个 domain 拼装 DTO（微信侧走 `wechat_integration_status(ctx, user_id)` Domain 包装），属编排上浮。

#### F13｜前端把「加载失败」当成「未绑定」

`frontend/src/pages/finance/identity.rs`：`integration_snapshot` 为 `None` 时走 `:376` else 分支 → 应用卡显示「尚未绑定飞书应用凭证」、**「用户身份」区块整块空白**（`:501-507`）。用户会以为「本来就没有」，实际是请求失败。

#### F14｜入站渠道定位每条消息 N+1 查询

`src/service/dal/lark/impl.rs:409-445` `find_channel_by_lark_identity`：遍历全部启用飞书渠道，**每个渠道都调一次 `resolve_channel_app_id` → `load_credential_row` 查库**（未复用同文件 `:117-130` 已有的 `cached_credential`）。该函数在**每一条入站消息**上执行。

#### F15｜接入域硬编码，不支持 Lark 国际版

`src/service/dao/lark/http.rs:28` `API_BASE = "https://open.feishu.cn"`、`src/service/dao/lark/ws.rs:104` 同一硬编码；无配置项。微信侧本轮刚补了 `base_url`（D9）。若目标用户含 Lark 国际版（`open.larksuite.com`）则完全不可用——**属能力缺口还是有意限定，需拍板**（见 §五）。

### P3 —— 能力面（登记不做）

**F16｜群聊与 @mention 完全不支持**：`src/models/events/lark.rs:143-145` `is_p2p()` 只放 `chat_type == "p2p"`，事件结构**没有 `mentions` 字段**。与飞书机器人最常见的用法（群内 @ 触发）不符，属能力面扩展。

---

## 五、待拍板

| # | 决策项 | 选项 | 倾向 |
|---|--------|------|------|
| **D0** | 协议层重建范围 | (a) **全量对齐官方**（端点+鉴权+protobuf+ACK+分片+参数下发+错误码分类）；(b) 先最小修通（端点+鉴权+protobuf+ACK），参数/分片随后；(c) 暂不动，只登记 | **(a)**：零件都耦合在同一处，分两次改要重复动 `pkg::ws` 接口 |
| D1 | 流程层范围 | (a) 只做 P0+P1 的「可观测与静默」；(b) 连 P2 契约清理一起；(c) 只做 F3 + 可观测 | (b)：本轮已在改同一批文件，一次做完省回扫 |
| D2 | 心跳策略 | (a) 照官方：消费服务端 `PingInterval` + protobuf ping（**默认不做无帧判死**）；(b) (a) 之上额外启用 `pingTimeout` 等价物 | **(a)**：与官方一致；无帧保活作为观测字段（`last_frame_at`）而非判死 |
| D3 | 致命码处置 | (a) 仅记录；(b) 识别 `exceed_conn_limit` → **停止重连 + 健康标 `conflict`**（官方语义） | **(b)**：官方即「不可重试」，且能解释「两实例互相顶」 |
| D4 | 飞书接入域 | (a) 维持只支持 `open.feishu.cn`；(b) 抽 `base_url` 配置（对齐微信 D9） | 若目标用户只在国内 → (a) 并登记 |
| D5 | `lark-cli` 分工 | (a) 授权仍走 CLI（device flow）、收事件自建 WS（现状分工，保留）；(b) 改为纯 SDK 方案 | **(a)**：CLI 做授权是官方设计，只是要按 F5 改成零点击 |
| **D6** | **`appSecret` 从哪来**（本轮新增，见 §2.6） | (a) **扫码建应用只取 `appId`**，secret 由用户在开发者后台复制、前端粘贴一次（我们已有 `--app-secret-stdin` 落库路径）；(b) 弃用扫码建应用，引导用户自行到后台建应用后粘贴 AppID+Secret；(c) 走 `keychain-downgrade` 后尝试反读 secret | **(a)**：唯一「无公网、零登录后台」的顺滑路径，且与现状落库方向一致；`appId` 白拿却被丢掉（F9）是必须先修的 bug |
| **D7** | 接入模式 | (a) 坚持**长连接**（本地部署无需公网，符合产品定位）；(b) 增加 webhook 模式作为可选项 | **(a)**：长连接的「集群随机分发 + 50 连接上限」是约束不是阻塞，但要按 D8 处理 |
| **D8** | 多实例部署语义（见 §2.8） | (a) 明确「**仅一个实例订阅**，其余实例靠 DB/AOP 分发」并写进文档与启动自检；(b) 接受随机分发，强化幂等与去重 | **(a)**：与 AOP「同 order_key 同消费者」不变量一致；否则跨实例串行假设静默失效 |
| **D9** | `LarkBindStatusResponse.channel_id`（本轮新增） | (a) **删掉该字段**（绑定链路不建渠道，渠道由既有入口创建）；(b) 在绑定链路里联动创建渠道后回填 | **(a)**：与 F9 同规——恒 `None` 的字段要么实现要么删；渠道创建涉及项目归属/身份映射，不该塞进绑定会话 |

> **已拍板**（2026-09-20）：D0=(a) 全量对齐官方、D1=(b) 连契约清理一起、D2=(a) 照服务端参数（不做无帧判死）、D3=(b) 致命码停重连、D5=(a) 授权走 CLI / 收事件自建 WS、D6=(a) 扫码只取 `appId`、D7=(a) 坚持长连接、D8=(a) 单实例订阅 + 幂等。
> **仍开放**：D4（接入域是否配置化 → 本轮维持 `open.feishu.cn` 并抽常量）、D9（上面新增）。

---

## 六、待修复登记（**待一并修复**）

> 用户指示：已发现的问题先记录、**后面一并修复**。本节即执行清单，按「协议层 → 流程层」分组，每条含位置与验收。
> 协议层（F0-1…F0-8）是一条**不可再拆的最小链条**：只修端点不修帧格式，等于没修。
> **施工分解（阶段划分 / 关键设计 / 风险回滚 / 分批提交）见 [飞书链路对齐重构方案](飞书链路对齐重构方案.md)。**

### 6.1 协议层（P0-0，必须一起改）

| # | 修复 | 位置 |
|---|------|------|
| F0-1 | 端点改 `/callback/ws/endpoint`（去 `/open-apis`，单数） | `src/service/dao/lark/ws.rs:25,104` |
| F0-2 | 鉴权改 `{AppID, AppSecret}`，去 Bearer；消费响应 `ClientConfig` | `src/service/dao/lark/ws.rs:98-131` |
| F0-3 | 引入 pbbp2 prost 定义；`pkg::ws` 支持二进制帧；adapter 能收 `Bytes` | `src/pkg/ws/mod.rs:84-90,398`、`lark/ws.rs:152-211` |
| F0-4 | 心跳间隔/重连参数按服务端下发，pong 更新参数 | `src/pkg/ws/mod.rs:32-36,356-379` |
| F0-5 | 重连改固定间隔 + 首次抖动 | `src/pkg/ws/mod.rs:209-214,266` |
| F0-6 | 回 ACK 帧（`code:200`/`500` + `biz_rt`），**必须 <3s**（官方超时重推窗口） | `src/pkg/ws/mod.rs`（帧发送路径） |
| F0-7 | 分片重组（`message_id`+`sum`+`seq`，非法元数据留痕） | `lark/ws.rs` 或 `pkg/ws` |
| F0-8 | 错误码分类：`internal_error` 可重试、`exceed_conn_limit` 致命停重连（官方 `retryable` 口径） | `lark/ws.rs:116-123` + supervisor |
| F0-9 | 取端点请求补官方头 `locale: zh` + `User-Agent`（网关消费），超时 15s | `lark/ws.rs:104-114` |
| F0-10 | 握手字段 `handshake-status`/`handshake-msg`/`handshake-autherrcode` 解析并 info 留痕（鉴权失败时它是唯一解释） | `lark/ws.rs`（帧头处理） |
| F0-11 | `appId` 形态校验 `/^cli_[0-9a-fA-F]{16}$/`，不合规**本地即拒**（省一次必然失败的建连） | `dao/lark/ws.rs` 建连前 |
| F0-12 | 多实例订阅自检：同 app 已有多实例在线时告警/拒绝（D8 落地；官方为集群随机分发，静默分错实例） | `dao/lark/listener.rs` + 健康面 |

### 6.2 可观测与静默（P0）

| # | 修复 | 位置 |
|---|------|------|
| F1 | 读循环补 `Close`/`Ping`/`Pong` 分支并记录 code/reason | `src/pkg/ws/mod.rs:395-416` |
| F2 | `WsConnState` 加 `last_frame_at`（常开观测） | `src/pkg/ws/mod.rs:122-130` |
| F3 | 入站四级过滤补 `info` 级「有意跳过」证据（含 `event_id`） | `dal/lark/impl.rs:195-222`、`consumer/lark_inbound.rs:73-80` |
| F6 | `LarkWsAppMetrics` 加 `last_frame_at_ms` / `frames_received` / `terminal_reason`；健康页补 stale 判定 | `common/src/api/system.rs:52-60`、`frontend/src/pages/system/health.rs:435` |

### 6.3 自愈与竞态（P1）

| # | 修复 | 位置 |
|---|------|------|
| F4 | 出站空 `message_id` 归一 `None`（对齐微信 `Result<Option<String>>`） | `dao/lark/http.rs:120-144,300` |
| F5 | device flow 零点击：自动发起 complete + 轮询 `auth/status` | `frontend/.../identity.rs:279-318,591-606`、`pkg/lark_integration.rs:283` |
| F7 | 绑定轮询加 `poll_gen` 代次守卫 | `frontend/.../identity.rs:199-263` |
| F8 | 绑定会话**先 insert 再等 URL** | `src/pkg/lark_integration.rs:526-536` |
| **F20** | **入站幂等缺失**（本轮新增，见下）：落库前用 `find_id_by_external_key("lark:{message_id}")` 反查，命中则 info 留痕 + 跳过 | `src/service/dal/lark/impl.rs:313` |

> **F20 为什么是硬前置**：官方长连接有「**3s 未 ACK 即重推**」与「**集群模式不支持广播、同应用只有随机一个客户端收到**」两条硬约束（§2.8）。而 `messages.external_key` **只是普通索引不是唯一索引**，入站路径**落库前不反查**（`adapt_lark` 仅把 `external_key` 写进落库命令，见 `impl.rs:313`），AOP 层也无事件级去重。⇒ 一次重推或多实例分发**会重复落库**，对用户表现为「同一条消息被 Agent 回了两次」。**D8 的「单实例订阅」假设成立前，这条必须先修。**

### 6.4 契约与展示（P2，随 D1 拍板）

| # | 修复 | 位置 |
|---|------|------|
| F9 | 实现分支 A **或** 删掉三个恒 `None` 字段 + 统一前端文案 | `handlers/.../bind_status.rs`、`frontend/.../identity.rs:241-252` |
| F10 | `LarkCredentialSnapshot` 补 `created_at`/`updated_at` | `common/src/api/lark_integration.rs:193-207` |
| F11 | `judge_bind_status` 未知态按可继续处理并留证据 | `frontend/src/api/lark_integration.rs:119-130` |
| F12 | 去 `.unwrap_or_default()`，区分「查询失败」与「空」；状态聚合下沉 Domain | `handlers/.../get_status.rs:30-49` |
| F13 | 加载失败与「未绑定」在 UI 上可区分 | `frontend/.../identity.rs:376,501-507` |
| F14 | 入站渠道定位复用 `cached_credential`，去 N+1 | `src/service/dal/lark/impl.rs:409-445` |
| F15 | 按 D4 决定是否抽 `base_url` | `dao/lark/http.rs:28`、`dao/lark/ws.rs:104` |

### 6.5 能力面（P3，单独立项）

| # | 修复 |
|---|------|
| F16 | 群聊支持 + `mentions` 解析（@ 机器人触发） |

### 6.6 凭据获取链路（**本轮新增，见 §2.6**）

> 这组是「扫码建应用」这条链路的修复。当前形态下即使协议层全修好，用户也**拿不到可用凭据**（分支 A 从未实现，`appId` 白拿却丢掉），因此必须与协议层同批处理。

| # | 修复 | 位置 |
|---|------|------|
| F17 | `config init --new` 加 `--json`，**解析 stdout 的 `{appId, appSecret:"****", brand}`**，把 `appId` 回填到 `LarkBindStatusResponse.app_id`（现在是硬编码 `None`） | `pkg/lark_integration.rs:479`、`handlers/.../bind_status.rs`、`frontend/.../identity.rs:241-252` |
| F18 | 落地分支 A：绑定成功后（拿到 `appId` + 用户粘贴的 secret）**直接写入 credential** 并把 `credential_id`/`channel_id` 回填 → 前端 Done 分支不再无条件说「请手动补填 App Secret」 | `service/domain/finance/identity_credential.rs`、`handlers/.../bind_status.rs` |
| F19 | secret 注入方向固定为「我方加密库 → CLI」（`--app-secret-stdin`，`ensure_cli_config` 已是此形态）；**不实现** keychain 反读；文档写明 secret 不可从 CLI 取回 | `pkg/tool_registry/lark_cli.rs:251`、`docs/design/*` |

---

## 七、执行顺序（已升格为施工方案）

本节原为「建议顺序」，现已按 D0/D1 拍板结果展开成可执行方案：**[飞书链路对齐重构方案](飞书链路对齐重构方案.md)**。

| 阶段 | 内容 | 对应条目 |
|------|------|---------|
| **S0** | 取证与骨架（官方 SDK 锚点落注释、`prost` 依赖） | — |
| **S1** | 传输层加法（`pkg::ws` 二进制帧 / close 留痕 / 参数下发 / 终局停机，**联邦零改动**） | F0-3、F0-4、F0-5、F0-6、F0-8、F1、F2 |
| **S2** | 协议层重建（端点 / 鉴权 / protobuf / ACK / 分片 / 致命码 / appId 校验） | F0-1、F0-2、F0-7、F0-9、F0-10、F0-11 |
| **S3** | 可观测五层 + 入站幂等 + 入站跳过留痕 | F6、F3、**F20** |
| **S4** | 自愈与竞态 | F8、F7、F5、F4、F13、F11 |
| **S5** | 凭据链路（`appId` 回填 → 分支 A 落库） | F17、F18、F19、F9 |
| **S6** | 契约清理 | F10、F12、F14、F15 |
| **S7** | 文档与验收 | — |
| 后续 | 能力面 | F16（单独立项）、F0-12（多实例自检，随 D8 文档化） |

> ⚠️ **F0-1…F0-12 不可拆**：只修端点不修帧格式，等于把链路留在「看着修过、其实连不上」的更差状态。

---

## 八、验收清单（待实现后勾选）

**协议层（真机）**

- [ ] `POST /callback/ws/endpoint` 返回 200 且拿到非空 `data.URL`
- [ ] 连接建立后服务端 `method=1` 的 data 帧能被解析（日志有 `event_id`）
- [ ] 事件处理成功后回出 `{"code":200}` ACK 帧，**且从收帧到回 ACK <3s**（官方超时重推窗口），服务端不再重推
- [ ] 收到 `exceed_conn_limit(1000040350)` 时停止重连并在健康页标 `conflict`
- [ ] 心跳间隔与重连间隔与 `ClientConfig` 一致（日志可核对）
- [ ] 长事件（>分片阈值）能完整重组并落库
- [ ] 双实例同时启动时，F0-12 自检能指出「事件会随机分发到任一实例」而非静默

**可观测与静默**

- [ ] `Message::Close` 帧的 code/reason 出现在日志与健康快照中
- [ ] 半开连接（`kill -STOP` 对端）能被现有机制观察到（`last_frame_at` 停走）
- [ ] 健康页飞书面板在「`connected` 但长时间无帧」时给出与微信同构的告警
- [ ] 群里 @ 机器人 / 发图片 / 发空文本，日志各有一行 `info` 级跳过证据（含 `event_id`）
- [ ] 同一 `message_id` 重复投递（模拟官方重推）**只落库一次**且 info 留痕（F20）

**前端与契约**

- [ ] 出站响应缺 `message_id` 时不再写出 `"lark:"` 脏键
- [ ] device flow 全程零点击（发起后用户只在浏览器完成授权，弹窗自动收敛）
- [ ] 连续快速点击「自动建应用」不产生第二条轮询循环
- [ ] 发起绑定后 5s 内点「取消」能真正杀掉子进程
- [ ] 凭证卡展示绑定时间 / 最后轮换时间

**凭据链路（真机，见 §2.6 / §6.6）**

- [ ] `config init --new` 建成应用后，`bind/status` 返回**非空 `app_id`** 且与开发者后台一致
- [ ] 粘贴 secret 后 credential 落库，`bind/status` 的 `credential_id`/`channel_id` 非空，前端**不再**提示「请手动补填 App Secret」
- [ ] 落库后 `ensure_cli_config` 可正常 spawn（`lark-cli config show` 显示 appId，secret 为 `****`）
- [ ] 用该凭据能成功取到 WS 连接地址（闭环证明「扫码链路 → 长连接」这一整条是通的）
