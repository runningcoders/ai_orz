工作区成员：`.`, `frontend`, `common`, `ai-orz-macros`；构建脚本 `build.sh` 与 `start.sh` 位于仓库根；前端构建产物路径由环境变量 `FRONTEND_DIST_DIR` 覆盖；测试通过 `[[test]]` 段逐一声明集成测试二进制（如 `auth_sysinit_test`、`message_delivery_test`、`a2a_flow_test` 等）；数据库迁移位于 `migrations/` 目录，由 SQLx migrate 管理。