-- 组织集团名（纯展示标签）
--
-- 集团（group）在本方案中是纯展示概念：不参与任何逻辑判断（不用于鉴权、
-- 不用于通信判定、不作为信任边界），仅用于「关联组织」界面的归组展示。
--
-- 同步规则：建联时若本端为空且对端非空，则抄录对端值；两端各自保留、可重名、
-- 允许不一致（不一致只影响显示，无逻辑后果）。
ALTER TABLE organizations ADD COLUMN group_name TEXT NULL;
