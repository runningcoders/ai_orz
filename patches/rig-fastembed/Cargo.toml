[package]
name = "rig-fastembed"
version = "0.41.0"
edition = "2024"
license = "MIT"
description = "Stub crate to bypass fastembed version conflict (project uses its own fastembed integration)"

[dependencies]

[features]
default = ["hf-hub", "ort-download-binaries", "hf-hub-rustls-tls"]
hf-hub = []
hf-hub-native-tls = []
hf-hub-rustls-tls = []
ort-download-binaries = []
